import {
  isMaterial,
  isObject3D
} from "./chunk-TWJFBIIG.js";
import {
  Box3,
  Color,
  EventDispatcher,
  Group,
  MathUtils,
  Raycaster,
  Vector2
} from "./chunk-UG26MVNH.js";
import {
  __publicField
} from "./chunk-WOOG5QLI.js";

// node_modules/@giro3d/giro3d/core/picking/PickingCircle.js
function traversePickingCircle(radius, callback) {
  let prevSq = void 0;
  for (let r = 0; r <= radius; r++) {
    const sq = r * r;
    for (let x = -r; x <= r; x++) {
      const sqx = x * x;
      for (let y = -r; y <= r; y++) {
        const dist = sqx + y * y;
        if (dist > sq) {
          continue;
        }
        if (prevSq != null && dist <= prevSq) {
          continue;
        }
        const realX = radius + x;
        const realY = radius + y;
        if (callback(realX, realY, realY * (2 * radius) + realX) === false) {
          return;
        }
      }
    }
    prevSq = sq;
  }
}
var PickingCircle_default = traversePickingCircle;

// node_modules/@giro3d/giro3d/core/picking/PickObjectsAt.js
var BLACK = new Color(0, 0, 0);
var defaultRaycaster = new Raycaster();
function findEntityInParent(obj) {
  var _a;
  if (((_a = obj.userData) == null ? void 0 : _a.parentEntity) != null) {
    return obj.userData.parentEntity;
  }
  if (obj.parent) {
    return findEntityInParent(obj.parent);
  }
  return null;
}
function pickObjectsAt(instance, canvasCoords, object, options = {}) {
  const radius = Math.max(options.radius ?? 0, 0);
  const limit = options.limit ?? Infinity;
  const filter = options.filter;
  const target = [];
  let pixels;
  const clearColor = BLACK;
  const clearR = Math.round(255 * clearColor.r);
  const clearG = Math.round(255 * clearColor.g);
  const clearB = Math.round(255 * clearColor.b);
  if (options.gpuPicking === true) {
    const zone = {
      x: canvasCoords.x - radius,
      y: canvasCoords.y - radius,
      width: 1 + radius * 2,
      height: 1 + radius * 2
    };
    pixels = instance.engine.renderToBuffer({
      scene: object,
      camera: instance.view.camera,
      zone,
      clearColor
    });
  }
  const vec2 = new Vector2();
  const normalized = instance.canvasToNormalizedCoords(canvasCoords, vec2);
  const tmp = normalized.clone();
  PickingCircle_default(radius, (x, y) => {
    const offset = ((y + radius) * (radius * 2 + 1) + (x + radius)) * 4;
    if (options.gpuPicking === true) {
      const r = pixels[offset];
      const g = pixels[offset + 1];
      const b = pixels[offset + 2];
      if (Math.abs(clearR - r) <= 1 && Math.abs(clearG - g) <= 1 && Math.abs(clearB - b) <= 1) {
        return null;
      }
    }
    tmp.setX(normalized.x + x / instance.view.width).setY(normalized.y + y / instance.view.height);
    const raycaster = options.raycaster ?? defaultRaycaster;
    raycaster.setFromCamera(tmp, instance.view.camera);
    const intersects = raycaster.intersectObject(object, true);
    for (const inter of intersects) {
      inter.entity = findEntityInParent(inter.object);
      if (!filter || filter(inter)) {
        target.push(inter);
        if (target.length >= limit) {
          return false;
        }
      }
    }
    return target.length === 0;
  });
  return target;
}
var PickObjectsAt_default = pickObjectsAt;

// node_modules/@giro3d/giro3d/entities/Entity.js
var Entity = class extends EventDispatcher {
  /**
   * The name of the type of this object.
   */
  /**
   * Creates an entity with the specified unique identifier.
   */
  constructor() {
    super();
    /**
     * The unique identifier of this entity.
     */
    __publicField(this, "_ready", false);
    /**
     * The name of this entity.
     */
    /**
     * An object that can be used to store custom data about the {@link Entity}.
     */
    /**
     * Read-only flag to check if a given object is of type Entity.
     */
    __publicField(this, "isEntity", true);
    this.id = MathUtils.generateUUID();
    this.type = "Entity";
    this._frozen = false;
    this.userData = {};
  }
  /**
   * Determine if this entity is ready to use.
   */
  get ready() {
    return this._ready;
  }
  get instance() {
    if (!this._instance) {
      throw new Error("This entity has not been added to an instance or its initialization is not finished.\nTo check if the entity is ready, use the .ready property");
    }
    return this._instance;
  }
  /**
   * Gets or sets the frozen status of this entity. A frozen entity is still visible
   * but will not be updated automatically.
   *
   * Useful for debugging purposes.
   */
  get frozen() {
    return this._frozen;
  }
  set frozen(v) {
    if (this._frozen !== v) {
      this._frozen = v;
      this.dispatchEvent({
        type: "frozen-property-changed",
        frozen: v
      });
    }
  }
  /**
   * Gets whether this entity is currently loading data.
   */
  get loading() {
    return false;
  }
  /**
   * Gets the current loading progress (between 0 and 1).
   * Note: This property is only meaningful if {@link loading} is `true`.
   */
  get progress() {
    return 1;
  }
  /**
   * Asynchronously preprocess the entity. This method may be overriden to perform
   * any operation that must be done before the entity can be used in the scene, such
   * as fetching metadata about a dataset, etc.
   *
   * @param opts - The preprocess options.
   *
   * @returns A promise that resolves when the entity is ready to be used.
   */
  preprocess() {
    return Promise.resolve();
  }
  /**
   * @internal
   */
  async initialize(opts) {
    this._instance = opts.instance;
    await this.preprocess(opts);
    this._ready = true;
    this.dispatchEvent({
      type: "initialized"
    });
  }
  /**
   * This method is called before `update` to check if the MainLoop
   * should try to update this entity or not. For better performances,
   * it should return `false` if the entity has no impact on the
   * rendering (e.g. the element is not visible).
   *
   * The inherited child _can_ completely ignore this value if it makes sense.
   *
   * @returns `true` if should check for update
   */
  shouldCheckForUpdate() {
    return this._ready;
  }
  /**
   * This method is called at the beginning of the `update` step to determine
   * if we should do a full render of the object. This should be the case if, for
   * instance, the source is the camera.
   *
   * You can override this depending on your needs. The inherited child should
   * not ignore this value, it should do a boolean OR, e.g.:
   * `return super.shouldFullUpdate(updateSource) || this.contains(updateSource);`
   *
   * @param updateSource - Source of change
   * @returns `true` if requires a full update of this object
   */
  shouldFullUpdate(updateSource) {
    return updateSource === this || updateSource.isCamera;
  }
  /**
   * This method is called at the beginning of the `update` step to determine
   * if we should re-render `updateSource`.
   * Not used when `shouldFullUpdate` returns `true`.
   *
   * You can override this depending on your needs.  The inherited child should
   * not ignore this value, it should do a boolean OR, e.g.:
   * `return super.shouldUpdate(updateSource) || this.contains(updateSource);`
   *
   * @param updateSource - Source of change
   * @returns `true` if requires an update of `updateSource`
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  shouldUpdate() {
    return false;
  }
  /**
   * Filters what objects need to be updated, based on `updatedSources`.
   * The returned objects are then passed to {@link preUpdate} and {@link postUpdate}.
   *
   * Inherited classes should override {@link shouldFullUpdate} and {@link shouldUpdate}
   * if they need to change this behavior.
   *
   * @param updateSources - Sources that triggered an update
   * @returns Set of objects to update
   */
  filterChangeSources(updateSources) {
    let fullUpdate = false;
    const filtered = /* @__PURE__ */ new Set();
    updateSources.forEach((src) => {
      fullUpdate = fullUpdate || this.shouldFullUpdate(src);
      if (this.shouldUpdate(src)) {
        filtered.add(src);
      }
    });
    return fullUpdate ? /* @__PURE__ */ new Set([this]) : filtered;
  }
  /**
   * This method is called just before `update()` to filter and select
   * which _elements_ should be actually updated. For example, in the
   * case of complex entities made of a hierarchy of elements, the entire
   * hierarchy may not need to be updated.
   *
   * Use this method to optimize the update step by reducing the number
   * of elements to process.
   *
   * Note: if this functions returns nothing, `update()` will not be called.
   *
   * @param context - the update context.
   * @param changeSources - the objects that triggered an update step.
   * This is useful to filter out unnecessary updates if no sources are
   * relevant to this entity. For example, if one of the sources is a
   * camera that moved during the previous frame, any entity that depends
   * on the camera's field of view should be updated.
   * @returns the _elements_ to update during `update()`.
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  preUpdate() {
    return null;
  }
  /**
   * Performs an update on an _element_ of the entity.
   *
   * Note: this method will be called for each element returned by `preUpdate()`.
   *
   * @param context - the update context.
   * This is the same object that the entity whose `update()` is being called.
   * @param element - the element to update.
   * This is one of the elements returned by {@link preUpdate}.
   * @returns New elements to update
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  update() {
    return void 0;
  }
  /**
   * Method called after {@link Entity.update}.
   *
   * @param context - the update context.
   * @param changeSources - the objects that triggered an update step.
   * This is useful to filter out unnecessary updates if no sources are
   * relevant to this entity. For example, if one of the sources is a
   * camera that moved during the previous frame, any entity that depends
   * on the camera's field of view should be updated.
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  postUpdate() {
  }
  /**
   * Disposes this entity and all resources associated with it.
   *
   * The default implementation of this method does nothing.
   * You should implement it in your custom entities to handle any special logic of disposal.
   *
   * For example: disposing materials, geometries, stopping HTTP requests, etc.
   *
   */
  dispose() {
  }
  /**
   * Notifies the parent instance that a change occured in the scene.
   */
  notifyChange(source) {
    var _a;
    (_a = this._instance) == null ? void 0 : _a.notifyChange(source);
  }
};
function isEntity(o) {
  return o == null ? void 0 : o.isEntity;
}
var Entity_default = Entity;

// node_modules/@giro3d/giro3d/entities/Entity3D.js
var Entity3D = class extends Entity_default {
  /**
   * Creates a Entity3D with the specified parameters.
   *
   * @param object3d - the root Three.js of this entity
   */
  constructor(options) {
    super();
    __publicField(this, "isMemoryUsage", true);
    __publicField(this, "hasDefaultPointOfView", true);
    __publicField(this, "type", "Entity3D");
    __publicField(this, "isPickable", true);
    /**
     * Read-only flag to check if a given object is of type Entity3D.
     */
    __publicField(this, "isEntity3D", true);
    if ((options == null ? void 0 : options.object3d) != null && !isObject3D(options.object3d)) {
      throw new Error("Incorrect root object type (must be a three.js Object3D instance)");
    }
    const rootObj = (options == null ? void 0 : options.object3d) ?? new Group();
    if (rootObj.type === "Group" && rootObj.name === "") {
      rootObj.name = this.id;
    }
    this._visible = true;
    this._opacity = 1;
    this._object3d = rootObj;
    this.name = (options == null ? void 0 : options.name) ?? void 0;
    this._distance = {
      min: Infinity,
      max: 0
    };
    this._clippingPlanes = null;
    this._renderOrder = 0;
    this.onObjectCreated(this._object3d);
  }
  get distance() {
    return this._distance;
  }
  getMemoryUsage() {
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  onRenderingContextLost() {
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  onRenderingContextRestored() {
  }
  /**
   * Returns the root object of this entity.
   */
  get object3d() {
    return this._object3d;
  }
  /**
   * Gets or sets the visibility of this entity.
   * A non-visible entity will not be automatically updated.
   */
  get visible() {
    return this._visible;
  }
  set visible(v) {
    if (this._visible !== v) {
      this._visible = v;
      this.updateVisibility();
      this.dispatchEvent({
        type: "visible-property-changed",
        visible: v
      });
    }
  }
  /**
   * Gets or sets the render order of this entity.
   */
  get renderOrder() {
    return this._renderOrder;
  }
  set renderOrder(v) {
    if (v !== this._renderOrder) {
      this._renderOrder = v;
      this.updateRenderOrder();
      this.dispatchEvent({
        type: "renderOrder-property-changed",
        renderOrder: v
      });
    }
  }
  /**
   * Gets or sets the opacity of this entity.
   */
  get opacity() {
    return this._opacity;
  }
  set opacity(v) {
    if (this._opacity !== v) {
      this._opacity = v;
      this.updateOpacity();
      this.dispatchEvent({
        type: "opacity-property-changed",
        opacity: v
      });
    }
  }
  /**
   * Gets or sets the clipping planes set on this entity. Default is `null` (no clipping planes).
   *
   * Note: custom entities must ensure that the materials and shaders used do support
   * the [clipping plane feature](https://threejs.org/docs/index.html?q=materi#api/en/materials/Material.clippingPlanes) of three.js.
   * Refer to the three.js documentation for more information.
   */
  get clippingPlanes() {
    return this._clippingPlanes;
  }
  set clippingPlanes(planes) {
    this._clippingPlanes = planes;
    this.updateClippingPlanes();
    this.dispatchEvent({
      type: "clippingPlanes-property-changed",
      clippingPlanes: planes
    });
  }
  /**
   * Updates the visibility of the entity.
   * Note: this method can be overriden for custom implementations.
   *
   */
  updateVisibility() {
    this.object3d.visible = this.visible;
  }
  /**
   * Updates the opacity of the entity.
   * Note: this method can be overriden for custom implementations.
   */
  updateOpacity() {
    this.traverseMaterials((material) => {
      if (material.opacity != null) {
        const currentTransparent = material.transparent;
        material.transparent = this.opacity < 1;
        if (currentTransparent !== material.transparent) {
          material.needsUpdate = true;
        }
        material.opacity = this.opacity;
      }
    });
  }
  /**
   * Updates the render order of the entity.
   * Note: this method can be overriden for custom implementations.
   */
  updateRenderOrder() {
    this.traverse((o) => {
      o.renderOrder = this.renderOrder;
    });
  }
  /**
   * Updates the clipping planes of all objects under this entity.
   */
  updateClippingPlanes() {
    this.traverseMaterials((mat) => {
      mat.clippingPlanes = this._clippingPlanes;
      mat.clipShadows = true;
    });
  }
  shouldCheckForUpdate() {
    return super.shouldCheckForUpdate() && this._visible;
  }
  shouldFullUpdate(updateSource) {
    return super.shouldFullUpdate(updateSource) || this.contains(updateSource);
  }
  shouldUpdate(updateSource) {
    return super.shouldUpdate(updateSource) || this.isOwned(updateSource);
  }
  /**
   * Returns true if this object belongs to this entity.
   *
   * @param obj - The object to test.
   */
  isOwned(obj) {
    var _a;
    if (obj.isObject3D) {
      if (((_a = obj.userData) == null ? void 0 : _a.parentEntity) === this) {
        return true;
      }
    }
    return false;
  }
  preUpdate(context, changeSources) {
    if (changeSources.size > 0) {
      this._distance.min = Infinity;
      this._distance.max = 0;
    }
    return null;
  }
  /**
   * Returns an approximated bounding box of this entity in the scene.
   *
   * @returns the resulting bounding box, or `null` if it could not be computed.
   */
  getBoundingBox() {
    const box = new Box3().setFromObject(this.object3d);
    return box;
  }
  /**
   * Applies entity-level setup on new object's material.
   *
   * Subclasses can override this to setup custom logic, for instance if the entity can produce
   * objects that are naturally transparent.
   *
   * @param material - the material of the newly created object
   */
  setupMaterial(material) {
    material.clippingPlanes = this._clippingPlanes;
    material.clipShadows = true;
    material.opacity = this._opacity;
    if (material.opacity < 1) {
      material.transparent = true;
    }
  }
  /**
   * Applies entity-level setup on a new object.
   *
   * Note: this method should be called from the subclassed entity to notify the parent
   * class that a new 3D object has just been created, so that it can be setup with entity-wide
   * parameters.
   *
   * @example
   * // In the subclass
   * const obj = new Object3D();
   *
   * // Notify the parent class
   * this.onObjectCreated(obj);
   * @param obj - The object to prepare.
   */
  onObjectCreated(obj) {
    this.traverse((o) => {
      o.userData.parentEntity = this;
      this.assignRenderOrder(obj);
    }, obj);
    this.traverseMaterials((m) => this.setupMaterial(m), obj);
    this.dispatchEvent({
      type: "object-created",
      obj
    });
  }
  /**
   * Assigns the render order of this object.
   *
   * This may be overriden to perform custom logic.
   */
  assignRenderOrder(obj) {
    obj.renderOrder = this.renderOrder;
  }
  /**
   * Test whether this entity contains the given object.
   *
   * The object may be a component of the entity, or a 3D object.
   *
   * @param obj - The object to test.
   * @returns true if the entity contains the object.
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  contains() {
    return false;
  }
  /**
   * Traverses all materials in the hierarchy of this entity.
   *
   * @param callback - The callback.
   * @param root - The traversal root. If undefined, the traversal starts at the root
   * object of this entity.
   */
  traverseMaterials(callback, root = void 0) {
    this.traverse((o) => {
      if ("material" in o) {
        if (Array.isArray(o.material)) {
          o.material.forEach((m) => {
            if (isMaterial(m)) {
              callback(m);
            }
          });
        } else if (isMaterial(o.material)) {
          callback(o.material);
        }
      }
    }, root);
  }
  /**
   * Traverses all meshes in the hierarchy of this entity.
   *
   * @param callback - The callback.
   * @param root - The raversal root. If undefined, the traversal starts at the root
   * object of this entity.
   */
  traverseMeshes(callback, root = void 0) {
    const origin = root ?? this.object3d;
    origin.traverse((o) => {
      if (o.isMesh) {
        callback(o);
      }
    });
  }
  /**
   * Traverses all objects in the hierarchy of this entity.
   *
   * @param callback - The callback.
   * @param root - The traversal root. If undefined, the traversal starts at the root
   * object of this entity.
   */
  traverse(callback, root = void 0) {
    const origin = root ?? this.object3d;
    origin.traverse(callback);
  }
  pick(canvasCoords, options) {
    return PickObjectsAt_default(this.instance, canvasCoords, this.object3d, options);
  }
  /**
   * Default implementation that computes a point of view from the bounding box of the entity, if any.
   */
  getDefaultPointOfView(params) {
    const box = this.getBoundingBox();
    if (box == null) {
      return null;
    }
    return this.instance.view.getDefaultPointOfView(box, params);
  }
};
function isEntity3D(o) {
  return o.isEntity3D;
}
var Entity3D_default = Entity3D;

export {
  PickingCircle_default,
  PickObjectsAt_default,
  isEntity,
  isEntity3D,
  Entity3D_default
};
//# sourceMappingURL=chunk-K75DSCFH.js.map
