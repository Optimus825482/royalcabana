import {
  Vector2,
  Vector4
} from "./chunk-UG26MVNH.js";
import {
  __publicField
} from "./chunk-WOOG5QLI.js";

// node_modules/@giro3d/giro3d/utils/tsutils.js
function nonNull(obj, msg) {
  if (obj == null) {
    throw new Error(msg ?? "non-null assertion failed");
  }
  return obj;
}

// node_modules/@giro3d/giro3d/core/OffsetScale.js
var OffsetScale = class _OffsetScale extends Vector4 {
  constructor(offsetX, offsetY, scaleX, scaleY) {
    super(offsetX, offsetY, scaleX, scaleY);
    __publicField(this, "isOffsetScale", true);
  }
  get offsetX() {
    return this.x;
  }
  get offsetY() {
    return this.y;
  }
  get scaleX() {
    return this.z;
  }
  get scaleY() {
    return this.w;
  }
  static identity() {
    return new _OffsetScale(0, 0, 1, 1);
  }
  /**
   * Transforms the point.
   * @param point - The point to transform.
   * @param target - The target to fill with the transformed point.
   * @returns The transformed point.
   */
  transform(point, target = new Vector2()) {
    target.x = point.x * this.scaleX + this.offsetX;
    target.y = point.y * this.scaleY + this.offsetY;
    return target;
  }
  combine(offsetScale, target = new _OffsetScale()) {
    target.copy(this);
    target.x += offsetScale.x * target.z;
    target.y += offsetScale.y * target.w;
    target.z *= offsetScale.z;
    target.w *= offsetScale.w;
    return target;
  }
};

export {
  nonNull,
  OffsetScale
};
//# sourceMappingURL=chunk-JWK6KB2K.js.map
