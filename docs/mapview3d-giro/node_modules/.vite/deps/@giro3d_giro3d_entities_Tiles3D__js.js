import {
  DRACOLoader,
  GLTFLoader,
  KTX2Loader,
  estimateBytesUsed
} from "./chunk-AEEV5T7Y.js";
import {
  defaultColorimetryOptions
} from "./chunk-PDJGP4SC.js";
import {
  enablePointCloudPostProcessing
} from "./chunk-V6BLO63J.js";
import {
  FullScreenQuad
} from "./chunk-TWQOLE4F.js";
import {
  ASPRS_CLASSIFICATIONS,
  ColorMap_default,
  MODE,
  PointCloudMaterial_default
} from "./chunk-5NP37DHT.js";
import "./chunk-N5KHZSPQ.js";
import {
  Entity3D_default
} from "./chunk-K75DSCFH.js";
import {
  getGeometryMemoryUsage
} from "./chunk-RQV2KR3X.js";
import {
  Fetcher_default
} from "./chunk-OUJR5ZQB.js";
import {
  Extent_default
} from "./chunk-RJV5WS67.js";
import {
  isBufferGeometry,
  isObject3D
} from "./chunk-TWJFBIIG.js";
import "./chunk-TW2BW7IN.js";
import "./chunk-KKYLY5RZ.js";
import {
  nonNull
} from "./chunk-JWK6KB2K.js";
import "./chunk-JTPK7CNI.js";
import "./chunk-QFCIXVZ3.js";
import "./chunk-GX66IZMZ.js";
import "./chunk-5RHQVMYD.js";
import {
  OperationCounter_default
} from "./chunk-WZOWKTBK.js";
import {
  Box2,
  Box3,
  Box3Helper,
  BoxGeometry,
  BufferAttribute,
  BufferGeometry,
  Color,
  CustomBlending,
  DataTexture,
  DefaultLoadingManager,
  DoubleSide,
  EdgesGeometry,
  Euler,
  EventDispatcher,
  FileLoader,
  Float32BufferAttribute,
  FloatType,
  Frustum,
  Group,
  InstancedMesh,
  Int16BufferAttribute,
  Int32BufferAttribute,
  Int8BufferAttribute,
  IntType,
  LineBasicMaterial,
  LineSegments,
  LoadingManager,
  MathUtils,
  Matrix2,
  Matrix3,
  Matrix4,
  Mesh,
  MeshBasicMaterial,
  MeshStandardMaterial,
  OneFactor,
  OrthographicCamera,
  Plane,
  Points,
  PointsMaterial,
  Quaternion,
  REVISION,
  Ray,
  Raycaster,
  ShaderMaterial,
  Sphere,
  Spherical,
  TextureUtils,
  Triangle,
  Uint16BufferAttribute,
  Uint32BufferAttribute,
  Uint8BufferAttribute,
  Vector2,
  Vector3,
  Vector4,
  WebGLRenderTarget,
  WebGLRenderer,
  ZeroFactor
} from "./chunk-UG26MVNH.js";
import {
  __publicField
} from "./chunk-WOOG5QLI.js";

// node_modules/@giro3d/giro3d/node_modules/3d-tiles-renderer/build/constants-Cj07Qhs1.js
var z = class {
  get unloadPriorityCallback() {
    return this._unloadPriorityCallback;
  }
  set unloadPriorityCallback(t) {
    t.length === 1 ? (console.warn('LRUCache: "unloadPriorityCallback" function has been changed to take two arguments.'), this._unloadPriorityCallback = (a, e) => {
      const s = t(a), o = t(e);
      return s < o ? -1 : s > o ? 1 : 0;
    }) : this._unloadPriorityCallback = t;
  }
  constructor() {
    this.minSize = 6e3, this.maxSize = 8e3, this.minBytesSize = 0.3 * 1073741824, this.maxBytesSize = 0.4 * 1073741824, this.unloadPercent = 0.05, this.autoMarkUnused = true, this.itemSet = /* @__PURE__ */ new Map(), this.itemList = [], this.usedSet = /* @__PURE__ */ new Set(), this.callbacks = /* @__PURE__ */ new Map(), this.unloadingHandle = -1, this.cachedBytes = 0, this.bytesMap = /* @__PURE__ */ new Map(), this.loadedSet = /* @__PURE__ */ new Set(), this._unloadPriorityCallback = null;
    const t = this.itemSet;
    this.defaultPriorityCallback = (a) => t.get(a);
  }
  // Returns whether or not the cache has reached the maximum size
  isFull() {
    return this.itemSet.size >= this.maxSize || this.cachedBytes >= this.maxBytesSize;
  }
  getMemoryUsage(t) {
    return this.bytesMap.get(t) || 0;
  }
  setMemoryUsage(t, a) {
    const { bytesMap: e, itemSet: s } = this;
    s.has(t) && (this.cachedBytes -= e.get(t) || 0, e.set(t, a), this.cachedBytes += a);
  }
  add(t, a) {
    const e = this.itemSet;
    if (e.has(t) || this.isFull())
      return false;
    const s = this.usedSet, o = this.itemList, i = this.callbacks;
    return o.push(t), s.add(t), e.set(t, Date.now()), i.set(t, a), true;
  }
  has(t) {
    return this.itemSet.has(t);
  }
  remove(t) {
    const a = this.usedSet, e = this.itemSet, s = this.itemList, o = this.bytesMap, i = this.callbacks, c2 = this.loadedSet;
    if (e.has(t)) {
      this.cachedBytes -= o.get(t) || 0, o.delete(t), i.get(t)(t);
      const d3 = s.indexOf(t);
      return s.splice(d3, 1), a.delete(t), e.delete(t), i.delete(t), c2.delete(t), true;
    }
    return false;
  }
  // Marks whether tiles in the cache have been completely loaded or not. Tiles that have not been completely
  // loaded are subject to being disposed early if the cache is full above its max size limits, even if they
  // are marked as used.
  setLoaded(t, a) {
    const { itemSet: e, loadedSet: s } = this;
    e.has(t) && (a === true ? s.add(t) : s.delete(t));
  }
  markUsed(t) {
    const a = this.itemSet, e = this.usedSet;
    a.has(t) && !e.has(t) && (a.set(t, Date.now()), e.add(t));
  }
  markUnused(t) {
    this.usedSet.delete(t);
  }
  markAllUnused() {
    this.usedSet.clear();
  }
  isUsed(t) {
    return this.usedSet.has(t);
  }
  // TODO: this should be renamed because it's not necessarily unloading all unused content
  // Maybe call it "cleanup" or "unloadToMinSize"
  unloadUnusedContent() {
    const {
      unloadPercent: t,
      minSize: a,
      maxSize: e,
      itemList: s,
      itemSet: o,
      usedSet: i,
      loadedSet: c2,
      callbacks: d3,
      bytesMap: u2,
      minBytesSize: h2,
      maxBytesSize: y4
    } = this, b4 = s.length - i.size, B4 = s.length - c2.size, S2 = Math.max(Math.min(s.length - a, b4), 0), k3 = this.cachedBytes - h2, M5 = this.unloadPriorityCallback || this.defaultPriorityCallback;
    let f2 = false;
    const P4 = S2 > 0 && b4 > 0 || B4 && s.length > e;
    if (b4 && this.cachedBytes > h2 || B4 && this.cachedBytes > y4 || P4) {
      s.sort((n, r2) => {
        const U4 = i.has(n), L3 = i.has(r2);
        if (U4 === L3) {
          const x5 = c2.has(n), v2 = c2.has(r2);
          return x5 === v2 ? -M5(n, r2) : x5 ? 1 : -1;
        } else
          return U4 ? 1 : -1;
      });
      const A3 = Math.max(a * t, S2 * t), p3 = Math.ceil(Math.min(A3, b4, S2)), E2 = Math.max(t * k3, t * h2), w6 = Math.min(E2, k3);
      let l = 0, m2 = 0;
      for (; this.cachedBytes - m2 > y4 || s.length - l > e; ) {
        const n = s[l], r2 = u2.get(n) || 0;
        if (i.has(n) && c2.has(n) || this.cachedBytes - m2 - r2 < y4 && s.length - l <= e)
          break;
        m2 += r2, l++;
      }
      for (; m2 < w6 || l < p3; ) {
        const n = s[l], r2 = u2.get(n) || 0;
        if (i.has(n) || this.cachedBytes - m2 - r2 < h2 && l >= p3)
          break;
        m2 += r2, l++;
      }
      s.splice(0, l).forEach((n) => {
        this.cachedBytes -= u2.get(n) || 0, d3.get(n)(n), u2.delete(n), o.delete(n), d3.delete(n), c2.delete(n), i.delete(n);
      }), f2 = l < S2 || m2 < k3 && l < b4, f2 = f2 && l > 0;
    }
    f2 && (this.unloadingHandle = requestAnimationFrame(() => this.scheduleUnload()));
  }
  scheduleUnload() {
    cancelAnimationFrame(this.unloadingHandle), this.scheduled || (this.scheduled = true, queueMicrotask(() => {
      this.scheduled = false, this.unloadUnusedContent();
    }));
  }
};
var C = class extends Error {
  constructor() {
    super("PriorityQueue: Item removed"), this.name = "PriorityQueueItemRemovedError";
  }
};
var G = class {
  // returns whether tasks are queued or actively running
  get running() {
    return this.items.length !== 0 || this.currJobs !== 0;
  }
  constructor() {
    this.maxJobs = 6, this.items = [], this.callbacks = /* @__PURE__ */ new Map(), this.currJobs = 0, this.scheduled = false, this.autoUpdate = true, this.priorityCallback = null, this.schedulingCallback = (t) => {
      requestAnimationFrame(t);
    }, this._runjobs = () => {
      this.scheduled = false, this.tryRunJobs();
    };
  }
  sort() {
    const t = this.priorityCallback, a = this.items;
    t !== null && a.sort(t);
  }
  has(t) {
    return this.callbacks.has(t);
  }
  add(t, a) {
    const e = {
      callback: a,
      reject: null,
      resolve: null,
      promise: null
    };
    return e.promise = new Promise((s, o) => {
      const i = this.items, c2 = this.callbacks;
      e.resolve = s, e.reject = o, i.unshift(t), c2.set(t, e), this.autoUpdate && this.scheduleJobRun();
    }), e.promise;
  }
  remove(t) {
    const a = this.items, e = this.callbacks, s = a.indexOf(t);
    if (s !== -1) {
      const o = e.get(t);
      o.promise.catch((i) => {
        if (!(i instanceof C))
          throw i;
      }), o.reject(new C()), a.splice(s, 1), e.delete(t);
    }
  }
  removeByFilter(t) {
    const { items: a } = this;
    for (let e = 0; e < a.length; e++) {
      const s = a[e];
      t(s) && (this.remove(s), e--);
    }
  }
  tryRunJobs() {
    this.sort();
    const t = this.items, a = this.callbacks, e = this.maxJobs;
    let s = 0;
    const o = () => {
      this.currJobs--, this.autoUpdate && this.scheduleJobRun();
    };
    for (; e > this.currJobs && t.length > 0 && s < e; ) {
      this.currJobs++, s++;
      const i = t.pop(), { callback: c2, resolve: d3, reject: u2 } = a.get(i);
      a.delete(i);
      let h2;
      try {
        h2 = c2(i);
      } catch (y4) {
        u2(y4), o();
      }
      h2 instanceof Promise ? h2.then(d3).catch(u2).finally(o) : (d3(h2), o());
    }
  }
  scheduleJobRun() {
    this.scheduled || (this.schedulingCallback(this._runjobs), this.scheduled = true);
  }
};
var J = -1;
var I = 0;
var R = 1;
var _ = 2;
var D = 3;
var F = 4;
var N = 6378137;
var j = 1 / 298.257223563;
var Q = 6356752314245179e-9;

// node_modules/@giro3d/giro3d/node_modules/3d-tiles-renderer/build/LoaderBase-2yhE3Jur.js
function h(r2, e = null, t = null) {
  const n = [];
  for (n.push(r2), n.push(null), n.push(0); n.length > 0; ) {
    const o = n.pop(), i = n.pop(), s = n.pop();
    if (e && e(s, i, o)) {
      t && t(s, i, o);
      return;
    }
    const a = s.children;
    if (a)
      for (let l = a.length - 1; l >= 0; l--)
        n.push(a[l]), n.push(s), n.push(o + 1);
    t && t(s, i, o);
  }
}
function u(r2, e = null) {
  let t = r2;
  for (; t; ) {
    const n = t.internal.depth, o = t.parent;
    e && e(t, o, n), t = o;
  }
}
var g = Object.freeze(Object.defineProperty({
  __proto__: null,
  traverseAncestors: u,
  traverseSet: h
}, Symbol.toStringTag, { value: "Module" }));
function d(r2) {
  if (r2 === null || r2.byteLength < 4)
    return "";
  let e;
  if (r2 instanceof DataView ? e = r2 : e = new DataView(r2), String.fromCharCode(e.getUint8(0)) === "{")
    return null;
  let t = "";
  for (let n = 0; n < 4; n++)
    t += String.fromCharCode(e.getUint8(n));
  return t;
}
var p = new TextDecoder();
function f(r2) {
  return p.decode(r2);
}
function c(r2) {
  return r2.replace(/[\\/][^\\/]+$/, "") + "/";
}
var w = Object.freeze(Object.defineProperty({
  __proto__: null,
  arrayToString: f,
  getWorkingPath: c,
  readMagicBytes: d
}, Symbol.toStringTag, { value: "Module" }));
var y = class {
  constructor() {
    this.fetchOptions = {}, this.workingPath = "";
  }
  load(...e) {
    return console.warn('Loader: "load" function has been deprecated in favor of "loadAsync".'), this.loadAsync(...e);
  }
  loadAsync(e) {
    return fetch(e, this.fetchOptions).then((t) => {
      if (!t.ok)
        throw new Error(`Failed to load file "${e}" with status ${t.status} : ${t.statusText}`);
      return t.arrayBuffer();
    }).then((t) => (this.workingPath === "" && (this.workingPath = c(e)), this.parse(t)));
  }
  resolveExternalURL(e) {
    return new URL(e, this.workingPath).href;
  }
  parse(e) {
    throw new Error("LoaderBase: Parse not implemented.");
  }
};

// node_modules/@giro3d/giro3d/node_modules/3d-tiles-renderer/build/B3DMLoaderBase-Cwfi38VH.js
function q(t) {
  if (!t)
    return null;
  let e = t.length;
  const s = t.indexOf("?"), n = t.indexOf("#");
  s !== -1 && (e = Math.min(e, s)), n !== -1 && (e = Math.min(e, n));
  const r2 = t.lastIndexOf(".", e), a = t.lastIndexOf("/", e), i = t.indexOf("://");
  return i !== -1 && i + 2 === a || r2 === -1 || r2 < a ? null : t.substring(r2 + 1, e) || null;
}
var R2 = {
  inView: false,
  error: 1 / 0,
  distanceFromCamera: 1 / 0
};
function E(t) {
  return t === F || t === J;
}
function C2(t, e) {
  return j2(t) && t.traversal.lastFrameVisited === e && t.traversal.used;
}
function j2(t) {
  return !!t.traversal;
}
function P(t) {
  const e = t.children.length === 0 || !!t.children[0].internal, s = !t.internal.hasUnrenderableContent || E(t.internal.loadingState);
  return e && s;
}
function w2(t) {
  return t.internal.hasUnrenderableContent || t.parent && t.parent.geometricError < t.geometricError;
}
function A(t, e) {
  e.ensureChildrenArePreprocessed(t), t.traversal.lastFrameVisited !== e.frameCount && (t.traversal.lastFrameVisited = e.frameCount, t.traversal.used = false, t.traversal.inFrustum = false, t.traversal.isLeaf = false, t.traversal.visible = false, t.traversal.active = false, t.traversal.error = 1 / 0, t.traversal.distanceFromCamera = 1 / 0, t.traversal.allChildrenReady = false, t.traversal.kicked = false, t.traversal.allUsedChildrenProcessed = false, e.calculateTileViewErrorWithPlugin(t, R2), t.traversal.inFrustum = R2.inView, t.traversal.error = R2.error, t.traversal.distanceFromCamera = R2.distanceFromCamera);
}
function x(t, e, s = false) {
  if (A(t, e), s ? e.markTileUsed(t) : k(t), w2(t) && P(t)) {
    const n = t.children;
    for (let r2 = 0, a = n.length; r2 < a; r2++)
      x(n[r2], e, s);
  }
}
function G2(t, e) {
  if (A(t, e), t.traversal.usedLastFrame && (k(t), t.traversal.wasSetActive && (t.traversal.active = true), (!t.traversal.active || w2(t)) && P(t))) {
    const s = t.children;
    for (let n = 0, r2 = s.length; n < r2; n++)
      G2(s[n], e);
  }
}
function k(t) {
  t.traversal.used = true;
}
function fe(t, e) {
  return !(t.traversal.error <= e.errorTarget && !w2(t) || e.maxDepth > 0 && t.internal.depth + 1 >= e.maxDepth || !P(t));
}
function H(t, e) {
  const { frameCount: s } = e, { children: n } = t;
  for (let r2 = 0, a = n.length; r2 < a; r2++) {
    const i = n[r2];
    C2(i, s) && (i.traversal.active && (i.traversal.kicked = true, i.traversal.active = false), H(i, e));
  }
}
function Q2(t) {
  return !w2(t) && (!t.internal.hasContent || E(t.internal.loadingState));
}
function J2(t, e) {
  if (A(t, e), !t.traversal.inFrustum)
    return;
  if (!fe(t, e)) {
    k(t);
    return;
  }
  let s = false, n = false;
  const r2 = t.children;
  for (let a = 0, i = r2.length; a < i; a++) {
    const o = r2[a];
    J2(o, e), s = s || C2(o, e.frameCount), n = n || o.traversal.inFrustum;
  }
  if (t.refine === "REPLACE" && !n && r2.length !== 0) {
    t.traversal.inFrustum = false, e.markTileUsed(t);
    for (let a = 0, i = r2.length; a < i; a++)
      x(r2[a], e, true);
    return;
  }
  if (k(t), t.refine === "REPLACE" && s && e.loadSiblings)
    for (let a = 0, i = r2.length; a < i; a++)
      x(r2[a], e);
}
function z2(t, e) {
  const s = e.frameCount;
  if (!C2(t, s))
    return;
  const n = t.children;
  let r2 = false;
  for (let i = 0, o = n.length; i < o; i++) {
    const l = n[i];
    r2 = r2 || C2(l, s);
  }
  if (!r2)
    t.traversal.isLeaf = true;
  else
    for (let i = 0, o = n.length; i < o; i++)
      z2(n[i], e);
  let a = true;
  for (let i = 0, o = n.length; i < o; i++) {
    const l = n[i];
    C2(l, e.frameCount) && !l.traversal.allUsedChildrenProcessed && (a = false);
  }
  t.traversal.allUsedChildrenProcessed = a && P(t);
}
function W(t, e) {
  if (!C2(t, e.frameCount))
    return;
  const s = t.internal.hasContent, n = E(t.internal.loadingState) && s, r2 = t.children;
  if (t.traversal.isLeaf) {
    if (!w2(t) && (t.traversal.active = true, P(t) && (!t.internal.hasContent || !E(t.internal.loadingState))))
      for (let o = 0, l = r2.length; o < l; o++)
        G2(r2[o], e);
    return;
  }
  let a = r2.length > 0;
  for (let o = 0, l = r2.length; o < l; o++) {
    const d3 = r2[o];
    W(d3, e), C2(d3, e.frameCount) && !(d3.traversal.active && Q2(d3)) && !d3.traversal.allChildrenReady && (a = false);
  }
  t.traversal.allChildrenReady = a;
  const i = t.traversal.active && Q2(t);
  !w2(t) && !a && !i && t.traversal.wasSetActive && (n || !t.internal.hasContent) && (t.traversal.active = true, H(t, e));
}
function K(t, e) {
  var n;
  const s = C2(t, e.frameCount);
  if (s && ((t.internal.hasUnrenderableContent || t.internal.hasRenderableContent && t.refine === "ADD") && (t.traversal.active = true), (t.traversal.active || t.traversal.kicked) && t.internal.hasContent ? (e.markTileUsed(t), (t.internal.hasUnrenderableContent || t.traversal.allUsedChildrenProcessed) && e.queueTileForDownload(t), t.internal.loadingState !== F && (t.traversal.active = false)) : t.traversal.active = false, t.traversal.visible = t.internal.hasRenderableContent && t.traversal.active && t.traversal.inFrustum && t.internal.loadingState === F, e.stats.used++, t.traversal.inFrustum && e.stats.inFrustum++), s || j2(t) && ((n = t.traversal) != null && n.usedLastFrame)) {
    let r2 = false, a = false;
    s ? (r2 = t.traversal.active, e.displayActiveTiles ? a = t.traversal.active || t.traversal.visible : a = t.traversal.visible) : A(t, e), t.internal.hasRenderableContent && t.internal.loadingState === F && (t.traversal.wasSetActive !== r2 && (e.stats.active += r2 ? 1 : -1, e.invokeOnePlugin((o) => o.setTileActive && o.setTileActive(t, r2))), t.traversal.wasSetVisible !== a && (e.stats.visible += a ? 1 : -1, e.invokeOnePlugin((o) => o.setTileVisible && o.setTileVisible(t, a)))), t.traversal.wasSetActive = r2, t.traversal.wasSetVisible = a, t.traversal.usedLastFrame = s;
    const i = t.children;
    for (let o = 0, l = i.length; o < l; o++) {
      const d3 = i[o];
      K(d3, e);
    }
  }
}
function ve(t, e) {
  J2(t, e), z2(t, e), W(t, e), K(t, e);
}
var L = {
  inView: false,
  error: 1 / 0,
  distanceFromCamera: 1 / 0
};
var Y = true;
function X(t) {
  return t === F || t === J;
}
function b(t, e) {
  return Z(t) && t.traversal.lastFrameVisited === e && t.traversal.used;
}
function Z(t) {
  return !!t.traversal;
}
function N2(t) {
  return t.children.length === 0 || !!t.children[0].internal;
}
function _2(t) {
  return t.internal.hasUnrenderableContent || t.parent && t.parent.geometricError < t.geometricError;
}
function $(t, e) {
  t.traversal.lastFrameVisited !== e.frameCount && (t.traversal.lastFrameVisited = e.frameCount, t.traversal.used = false, t.traversal.inFrustum = false, t.traversal.isLeaf = false, t.traversal.visible = false, t.traversal.active = false, t.traversal.error = 1 / 0, t.traversal.distanceFromCamera = 1 / 0, t.traversal.allChildrenReady = false, e.calculateTileViewErrorWithPlugin(t, L), t.traversal.inFrustum = L.inView, t.traversal.error = L.error, t.traversal.distanceFromCamera = L.distanceFromCamera);
}
function O(t, e, s = false) {
  if (e.ensureChildrenArePreprocessed(t), $(t, e), B(t, e, s), _2(t) && N2(t)) {
    const n = t.children;
    for (let r2 = 0, a = n.length; r2 < a; r2++)
      O(n[r2], e, s);
  }
}
function ee(t, e) {
  if (e.ensureChildrenArePreprocessed(t), b(t, e.frameCount) && (t.internal.hasContent && e.queueTileForDownload(t), N2(t))) {
    const s = t.children;
    for (let n = 0, r2 = s.length; n < r2; n++)
      ee(s[n], e);
  }
}
function B(t, e, s = false) {
  t.traversal.used || (s || (t.traversal.used = true, e.stats.used++), e.markTileUsed(t), t.traversal.inFrustum === true && e.stats.inFrustum++);
}
function pe(t, e) {
  return !(t.traversal.error <= e.errorTarget && !_2(t) || e.maxDepth > 0 && t.internal.depth + 1 >= e.maxDepth || !N2(t));
}
function te(t, e) {
  if (e.ensureChildrenArePreprocessed(t), $(t, e), !t.traversal.inFrustum)
    return;
  if (!pe(t, e)) {
    B(t, e);
    return;
  }
  let s = false, n = false;
  const r2 = t.children;
  for (let a = 0, i = r2.length; a < i; a++) {
    const o = r2[a];
    te(o, e), s = s || b(o, e.frameCount), n = n || o.traversal.inFrustum;
  }
  if (t.refine === "REPLACE" && !n && r2.length !== 0) {
    t.traversal.inFrustum = false;
    for (let a = 0, i = r2.length; a < i; a++)
      O(r2[a], e, true);
    return;
  }
  if (B(t, e), t.refine === "REPLACE" && (s && t.internal.depth !== 0 || Y))
    for (let a = 0, i = r2.length; a < i; a++)
      O(r2[a], e);
}
function se(t, e) {
  const s = e.frameCount;
  if (!b(t, s))
    return;
  const n = t.children;
  let r2 = false;
  for (let a = 0, i = n.length; a < i; a++) {
    const o = n[a];
    r2 = r2 || b(o, s);
  }
  if (!r2)
    t.traversal.isLeaf = true;
  else {
    let a = true;
    for (let i = 0, o = n.length; i < o; i++) {
      const l = n[i];
      if (se(l, e), b(l, s)) {
        const d3 = !_2(l);
        let h2 = !l.internal.hasContent || l.internal.hasRenderableContent && X(l.internal.loadingState) || l.internal.hasUnrenderableContent && l.internal.loadingState === J;
        h2 = d3 && h2 || l.traversal.allChildrenReady, a = a && h2;
      }
    }
    t.traversal.allChildrenReady = a;
  }
}
function re(t, e) {
  const s = e.stats;
  if (!b(t, e.frameCount))
    return;
  if (t.traversal.isLeaf) {
    t.internal.loadingState === F ? (t.traversal.inFrustum && (t.traversal.visible = true, s.visible++), t.traversal.active = true, s.active++) : t.internal.hasContent && e.queueTileForDownload(t);
    return;
  }
  const n = t.children, r2 = t.internal.hasContent, a = X(t.internal.loadingState) && r2, i = (e.errorTarget + 1) * e.errorThreshold, o = t.traversal.error <= i, l = t.refine === "ADD", d3 = t.traversal.allChildrenReady || t.internal.depth === 0 && !Y;
  if (r2 && (o || l) && e.queueTileForDownload(t), (o && a && !d3 || a && l) && (t.traversal.inFrustum && (t.traversal.visible = true, s.visible++), t.traversal.active = true, s.active++), !l && o && !d3)
    for (let h2 = 0, v2 = n.length; h2 < v2; h2++) {
      const f2 = n[h2];
      b(f2, e.frameCount) && ee(f2, e);
    }
  else
    for (let h2 = 0, v2 = n.length; h2 < v2; h2++)
      re(n[h2], e);
}
function ne(t, e) {
  const s = b(t, e.frameCount);
  if (s || Z(t) && t.traversal.usedLastFrame) {
    let n = false, r2 = false;
    s ? (n = t.traversal.active, e.displayActiveTiles ? r2 = t.traversal.active || t.traversal.visible : r2 = t.traversal.visible) : $(t, e), t.internal.hasRenderableContent && t.internal.loadingState === F && (t.traversal.wasSetActive !== n && e.invokeOnePlugin((i) => i.setTileActive && i.setTileActive(t, n)), t.traversal.wasSetVisible !== r2 && e.invokeOnePlugin((i) => i.setTileVisible && i.setTileVisible(t, r2))), t.traversal.wasSetActive = n, t.traversal.wasSetVisible = r2, t.traversal.usedLastFrame = s;
    const a = t.children;
    for (let i = 0, o = a.length; i < o; i++) {
      const l = a[i];
      ne(l, e);
    }
  }
}
function me(t, e) {
  te(t, e), se(t, e), re(t, e), ne(t, e);
}
function ge(t) {
  let e = null;
  return () => {
    e === null && (e = requestAnimationFrame(() => {
      e = null, t();
    }));
  };
}
var M = Symbol("PLUGIN_REGISTERED");
var y2 = {
  inView: true,
  error: 0,
  distance: 1 / 0
};
var V = (t, e) => {
  const s = t.priority || 0, n = e.priority || 0;
  return s !== n ? s > n ? 1 : -1 : !t.traversal || !e.traversal ? 0 : t.traversal.used !== e.traversal.used ? t.traversal.used ? 1 : -1 : t.traversal.error !== e.traversal.error ? t.traversal.error > e.traversal.error ? 1 : -1 : t.traversal.distanceFromCamera !== e.traversal.distanceFromCamera ? t.traversal.distanceFromCamera > e.traversal.distanceFromCamera ? -1 : 1 : t.internal.depthFromRenderedParent !== e.internal.depthFromRenderedParent ? t.internal.depthFromRenderedParent > e.internal.depthFromRenderedParent ? -1 : 1 : 0;
};
var Te = (t, e) => {
  const s = t.priority || 0, n = e.priority || 0;
  return s !== n ? s > n ? 1 : -1 : !t.traversal || !e.traversal ? 0 : t.traversal.used !== e.traversal.used ? t.traversal.used ? 1 : -1 : t.traversal.inFrustum !== e.traversal.inFrustum ? t.traversal.inFrustum ? 1 : -1 : t.internal.hasUnrenderableContent !== e.internal.hasUnrenderableContent ? t.internal.hasUnrenderableContent ? 1 : -1 : t.traversal.distanceFromCamera !== e.traversal.distanceFromCamera ? t.traversal.distanceFromCamera > e.traversal.distanceFromCamera ? -1 : 1 : 0;
};
var ye = (t, e) => {
  const s = t.priority || 0, n = e.priority || 0;
  return s !== n ? s > n ? 1 : -1 : !t.traversal || !e.traversal ? 0 : t.traversal.lastFrameVisited !== e.traversal.lastFrameVisited ? t.traversal.lastFrameVisited > e.traversal.lastFrameVisited ? -1 : 1 : t.internal.depthFromRenderedParent !== e.internal.depthFromRenderedParent ? t.internal.depthFromRenderedParent > e.internal.depthFromRenderedParent ? 1 : -1 : t.internal.loadingState !== e.internal.loadingState ? t.internal.loadingState > e.internal.loadingState ? -1 : 1 : t.internal.hasUnrenderableContent !== e.internal.hasUnrenderableContent ? t.internal.hasUnrenderableContent ? -1 : 1 : t.traversal.error !== e.traversal.error ? t.traversal.error > e.traversal.error ? -1 : 1 : 0;
};
var Pe = class {
  get root() {
    const e = this.rootTileset;
    return e ? e.root : null;
  }
  get rootTileSet() {
    return console.warn('TilesRenderer: "rootTileSet" has been deprecated. Use "rootTileset" instead.'), this.rootTileset;
  }
  get loadProgress() {
    const { stats: e, isLoading: s } = this, n = e.queued + e.downloading + e.parsing, r2 = e.inCacheSinceLoad + (s ? 1 : 0);
    return r2 === 0 ? 1 : 1 - n / r2;
  }
  get errorThreshold() {
    return this._errorThreshold;
  }
  set errorThreshold(e) {
    console.warn('TilesRenderer: The "errorThreshold" option has been deprecated.'), this._errorThreshold = e;
  }
  constructor(e = null) {
    this.rootLoadingState = I, this.rootTileset = null, this.rootURL = e, this.fetchOptions = {}, this.plugins = [], this.queuedTiles = [], this.cachedSinceLoadComplete = /* @__PURE__ */ new Set(), this.isLoading = false;
    const s = new z();
    s.unloadPriorityCallback = ye;
    const n = new G();
    n.maxJobs = 25, n.priorityCallback = V;
    const r2 = new G();
    r2.maxJobs = 5, r2.priorityCallback = V;
    const a = new G();
    a.maxJobs = 25, a.priorityCallback = (i, o) => {
      const l = i.parent, d3 = o.parent;
      return l === d3 ? 0 : l ? d3 ? n.priorityCallback(l, d3) : -1 : 1;
    }, this.processedTiles = /* @__PURE__ */ new WeakSet(), this.visibleTiles = /* @__PURE__ */ new Set(), this.activeTiles = /* @__PURE__ */ new Set(), this.usedSet = /* @__PURE__ */ new Set(), this.loadingTiles = /* @__PURE__ */ new Set(), this.lruCache = s, this.downloadQueue = n, this.parseQueue = r2, this.processNodeQueue = a, this.stats = {
      inCacheSinceLoad: 0,
      inCache: 0,
      queued: 0,
      downloading: 0,
      parsing: 0,
      loaded: 0,
      failed: 0,
      inFrustum: 0,
      used: 0,
      active: 0,
      visible: 0,
      tilesProcessed: 0
    }, this.frameCount = 0, this._dispatchNeedsUpdateEvent = ge(() => {
      this.dispatchEvent({ type: "needs-update" });
    }), this.errorTarget = 16, this._errorThreshold = 1 / 0, this.displayActiveTiles = false, this.maxDepth = 1 / 0, this.optimizedLoadStrategy = false, this.loadSiblings = true, this.maxTilesProcessed = 250;
  }
  // Plugins
  registerPlugin(e) {
    if (e[M] === true)
      throw new Error("TilesRendererBase: A plugin can only be registered to a single tileset");
    e.loadRootTileSet && !e.loadRootTileset && (console.warn('TilesRendererBase: Plugin implements deprecated "loadRootTileSet" method. Please rename to "loadRootTileset".'), e.loadRootTileset = e.loadRootTileSet), e.preprocessTileSet && !e.preprocessTileset && (console.warn('TilesRendererBase: Plugin implements deprecated "preprocessTileSet" method. Please rename to "preprocessTileset".'), e.preprocessTileset = e.preprocessTileSet);
    const s = this.plugins, n = e.priority || 0;
    let r2 = s.length;
    for (let a = 0; a < s.length; a++)
      if ((s[a].priority || 0) > n) {
        r2 = a;
        break;
      }
    s.splice(r2, 0, e), e[M] = true, e.init && e.init(this);
  }
  unregisterPlugin(e) {
    const s = this.plugins;
    if (typeof e == "string" && (e = this.getPluginByName(e)), s.includes(e)) {
      const n = s.indexOf(e);
      return s.splice(n, 1), e.dispose && e.dispose(), true;
    }
    return false;
  }
  getPluginByName(e) {
    return this.plugins.find((s) => s.name === e) || null;
  }
  invokeOnePlugin(e) {
    const s = [...this.plugins, this];
    for (let n = 0; n < s.length; n++) {
      const r2 = e(s[n]);
      if (r2)
        return r2;
    }
    return null;
  }
  invokeAllPlugins(e) {
    const s = [...this.plugins, this], n = [];
    for (let r2 = 0; r2 < s.length; r2++) {
      const a = e(s[r2]);
      a && n.push(a);
    }
    return n.length === 0 ? null : Promise.all(n);
  }
  // Public API
  traverse(e, s, n = true) {
    this.root && h(this.root, (r2, ...a) => (n && this.ensureChildrenArePreprocessed(r2, true), e ? e(r2, ...a) : false), s);
  }
  getAttributions(e = []) {
    return this.invokeAllPlugins((s) => s !== this && s.getAttributions && s.getAttributions(e)), e;
  }
  update() {
    const { lruCache: e, usedSet: s, stats: n, root: r2, downloadQueue: a, parseQueue: i, processNodeQueue: o, optimizedLoadStrategy: l } = this;
    if (this.rootLoadingState === I && (this.rootLoadingState = _, this.invokeOnePlugin((u2) => u2.loadRootTileset && u2.loadRootTileset()).then((u2) => {
      let c2 = this.rootURL;
      c2 !== null && this.invokeAllPlugins((p3) => c2 = p3.preprocessURL ? p3.preprocessURL(c2, null) : c2), this.rootLoadingState = F, this.rootTileset = u2, this.dispatchEvent({ type: "needs-update" }), this.dispatchEvent({ type: "load-content" }), this.dispatchEvent({
        type: "load-tileset",
        tileset: u2,
        url: c2
      }), this.dispatchEvent({
        type: "load-root-tileset",
        tileset: u2,
        url: c2
      });
    }).catch((u2) => {
      this.rootLoadingState = J, console.error(u2), this.rootTileset = null, this.dispatchEvent({
        type: "load-error",
        tile: null,
        error: u2,
        url: this.rootURL
      });
    })), !r2)
      return;
    let d3 = null;
    if (this.invokeAllPlugins((u2) => {
      if (u2.doTilesNeedUpdate) {
        const c2 = u2.doTilesNeedUpdate();
        d3 === null ? d3 = c2 : d3 = !!(d3 || c2);
      }
    }), d3 === false) {
      this.dispatchEvent({ type: "update-before" }), this.dispatchEvent({ type: "update-after" });
      return;
    }
    this.dispatchEvent({ type: "update-before" }), n.inFrustum = 0, n.used = 0, n.active = 0, n.visible = 0, n.tilesProcessed = 0, this.frameCount++, s.forEach((u2) => e.markUnused(u2)), s.clear();
    const h2 = l ? Te : V;
    a.priorityCallback = h2, i.priorityCallback = h2, this.prepareForTraversal(), l ? ve(r2, this) : me(r2, this), this.removeUnusedPendingTiles();
    const v2 = this.queuedTiles;
    v2.sort(e.unloadPriorityCallback);
    for (let u2 = 0, c2 = v2.length; u2 < c2 && !e.isFull(); u2++)
      this.requestTileContents(v2[u2]);
    v2.length = 0, e.scheduleUnload(), (a.running || i.running || o.running) === false && this.isLoading === true && (this.cachedSinceLoadComplete.clear(), n.inCacheSinceLoad = 0, this.dispatchEvent({ type: "tiles-load-end" }), this.isLoading = false), this.dispatchEvent({ type: "update-after" });
  }
  resetFailedTiles() {
    this.rootLoadingState === J && (this.rootLoadingState = I);
    const e = this.stats;
    e.failed !== 0 && (this.traverse((s) => {
      s.internal.loadingState === J && (s.internal.loadingState = I);
    }, null, false), e.failed = 0);
  }
  calculateTileViewErrorWithPlugin(e, s) {
    this.calculateTileViewError(e, s);
    let n = null, r2 = 0, a = 1 / 0;
    this.invokeAllPlugins((i) => {
      i !== this && i.calculateTileViewError && (y2.inView = true, y2.error = 0, y2.distance = 1 / 0, i.calculateTileViewError(e, y2) && (n === null && (n = true), n = n && y2.inView, y2.inView && (a = Math.min(a, y2.distance), r2 = Math.max(r2, y2.error))));
    }), s.inView && n !== false ? (s.error = Math.max(s.error, r2), s.distanceFromCamera = Math.min(s.distanceFromCamera, a)) : n ? (s.inView = true, s.error = r2, s.distanceFromCamera = a) : s.inView = false;
  }
  dispose() {
    [...this.plugins].forEach((r2) => {
      this.unregisterPlugin(r2);
    });
    const s = this.lruCache, n = [];
    this.traverse((r2) => (n.push(r2), false), null, false);
    for (let r2 = 0, a = n.length; r2 < a; r2++)
      s.remove(n[r2]);
    this.stats = {
      queued: 0,
      parsing: 0,
      downloading: 0,
      failed: 0,
      inFrustum: 0,
      traversed: 0,
      used: 0,
      active: 0,
      visible: 0
    }, this.frameCount = 0, this.loadingTiles.clear();
  }
  // Overrideable
  calculateBytesUsed(e, s) {
    return 0;
  }
  dispatchEvent(e) {
  }
  addEventListener(e, s) {
  }
  removeEventListener(e, s) {
  }
  parseTile(e, s, n) {
    return null;
  }
  prepareForTraversal() {
  }
  disposeTile(e) {
    e.traversal.visible && (this.invokeOnePlugin((n) => n.setTileVisible && n.setTileVisible(e, false)), e.traversal.visible = false), e.traversal.active && (this.invokeOnePlugin((n) => n.setTileActive && n.setTileActive(e, false)), e.traversal.active = false);
    const { scene: s } = e.engineData;
    s && this.dispatchEvent({
      type: "dispose-model",
      scene: s,
      tile: e
    });
  }
  preprocessNode(e, s, n = null) {
    var r2;
    if (this.processedTiles.add(e), this.stats.tilesProcessed++, e.content && (!("uri" in e.content) && "url" in e.content && (e.content.uri = e.content.url, delete e.content.url), e.content.boundingVolume && !("box" in e.content.boundingVolume || "sphere" in e.content.boundingVolume || "region" in e.content.boundingVolume) && delete e.content.boundingVolume), e.parent = n, e.children = e.children || [], e.internal = {
      hasContent: false,
      hasRenderableContent: false,
      hasUnrenderableContent: false,
      loadingState: I,
      basePath: s,
      depth: -1,
      depthFromRenderedParent: -1
    }, (r2 = e.content) != null && r2.uri) {
      const a = q(e.content.uri), i = !!(a && /json$/.test(a));
      e.internal.hasContent = true, e.internal.hasUnrenderableContent = i, e.internal.hasRenderableContent = !i;
    } else
      e.internal.hasContent = false, e.internal.hasUnrenderableContent = false, e.internal.hasRenderableContent = false;
    n ? (e.internal.depth = n.internal.depth + 1, e.internal.depthFromRenderedParent = n.internal.depthFromRenderedParent + (e.internal.hasRenderableContent ? 1 : 0)) : (e.internal.depth = 0, e.internal.depthFromRenderedParent = e.internal.hasRenderableContent ? 1 : 0), e.traversal = {
      distanceFromCamera: 1 / 0,
      error: 1 / 0,
      inFrustum: false,
      isLeaf: false,
      used: false,
      usedLastFrame: false,
      visible: false,
      wasSetVisible: false,
      active: false,
      wasSetActive: false,
      allChildrenReady: false,
      kicked: false,
      allUsedChildrenProcessed: false,
      lastFrameVisited: -1
    }, n === null ? e.refine = e.refine || "REPLACE" : e.refine = e.refine || n.refine, e.engineData = {
      scene: null,
      metadata: null,
      boundingVolume: null
    }, Object.defineProperty(e, "cached", {
      get() {
        return console.warn('TilesRenderer: "tile.cached" field has been renamed to "tile.engineData".'), this.engineData;
      },
      enumerable: false,
      configurable: true
    }), this.invokeAllPlugins((a) => {
      a !== this && a.preprocessNode && a.preprocessNode(e, s, n);
    });
  }
  setTileActive(e, s) {
    s ? this.activeTiles.add(e) : this.activeTiles.delete(e);
  }
  setTileVisible(e, s) {
    s ? this.visibleTiles.add(e) : this.visibleTiles.delete(e), this.dispatchEvent({
      type: "tile-visibility-change",
      scene: e.engineData.scene,
      tile: e,
      visible: s
    });
  }
  calculateTileViewError(e, s) {
  }
  removeUnusedPendingTiles() {
    const { lruCache: e, loadingTiles: s } = this, n = [];
    for (const r2 of s)
      !e.isUsed(r2) && r2.internal.loadingState === R && n.push(r2);
    for (let r2 = 0; r2 < n.length; r2++)
      e.remove(n[r2]);
  }
  // Private Functions
  queueTileForDownload(e) {
    e.internal.loadingState !== I || this.lruCache.isFull() || this.queuedTiles.push(e);
  }
  markTileUsed(e) {
    this.usedSet.add(e), this.lruCache.markUsed(e);
  }
  fetchData(e, s) {
    return fetch(e, s);
  }
  ensureChildrenArePreprocessed(e, s = this.stats.tilesProcessed < this.maxTilesProcessed) {
    const n = e.children;
    if (n.length === 0 || n[0].internal)
      return;
    const r2 = (a) => {
      for (let i = 0, o = a.length; i < o; i++)
        this.preprocessNode(a[i], e.internal.basePath, e);
    };
    s ? (this.processNodeQueue.remove(e), r2(n)) : this.processNodeQueue.has(e) || this.processNodeQueue.add(e, (a) => {
      r2(a.children), this._dispatchNeedsUpdateEvent();
    });
  }
  // returns the total bytes used for by the given tile as reported by all plugins
  getBytesUsed(e) {
    let s = 0;
    return this.invokeAllPlugins((n) => {
      n.calculateBytesUsed && (s += n.calculateBytesUsed(e, e.engineData.scene) || 0);
    }), s;
  }
  // force a recalculation of the tile or all tiles if no tile is provided
  recalculateBytesUsed(e = null) {
    const { lruCache: s, processedTiles: n } = this;
    e === null ? s.itemSet.forEach((r2) => {
      n.has(r2) && s.setMemoryUsage(r2, this.getBytesUsed(r2));
    }) : s.setMemoryUsage(e, this.getBytesUsed(e));
  }
  preprocessTileset(e, s, n = null) {
    const r2 = Object.getPrototypeOf(this);
    Object.hasOwn(r2, "preprocessTileSet") && console.warn(`${r2.constructor.name}: Class overrides deprecated "preprocessTileSet" method. Please rename to "preprocessTileset".`);
    const a = e.asset.version, [i, o] = a.split(".").map((d3) => parseInt(d3));
    console.assert(
      i <= 1,
      "TilesRenderer: asset.version is expected to be a 1.x or a compatible version."
    ), i === 1 && o > 0 && console.warn("TilesRenderer: tiles versions at 1.1 or higher have limited support. Some new extensions and features may not be supported.");
    let l = s.replace(/\/[^/]*$/, "");
    l = new URL(l, window.location.href).toString(), this.preprocessNode(e.root, l, n);
  }
  preprocessTileSet(...e) {
    return console.warn('TilesRenderer: "preprocessTileSet" has been deprecated. Use "preprocessTileset" instead.'), this.preprocessTileset(...e);
  }
  loadRootTileset() {
    const e = Object.getPrototypeOf(this);
    Object.hasOwn(e, "loadRootTileSet") && console.warn(`${e.constructor.name}: Class overrides deprecated "loadRootTileSet" method. Please rename to "loadRootTileset".`);
    let s = this.rootURL;
    return this.invokeAllPlugins((r2) => s = r2.preprocessURL ? r2.preprocessURL(s, null) : s), this.invokeOnePlugin((r2) => r2.fetchData && r2.fetchData(s, this.fetchOptions)).then((r2) => {
      if (r2 instanceof Response) {
        if (r2.ok)
          return r2.json();
        throw new Error(`TilesRenderer: Failed to load tileset "${s}" with status ${r2.status} : ${r2.statusText}`);
      } else return r2;
    }).then((r2) => (this.preprocessTileset(r2, s), r2));
  }
  loadRootTileSet(...e) {
    return console.warn('TilesRenderer: "loadRootTileSet" has been deprecated. Use "loadRootTileset" instead.'), this.loadRootTileSet(...e);
  }
  requestTileContents(e) {
    if (e.internal.loadingState !== I)
      return;
    let s = false, n = null, r2 = new URL(e.content.uri, e.internal.basePath + "/").toString();
    this.invokeAllPlugins((c2) => r2 = c2.preprocessURL ? c2.preprocessURL(r2, e) : r2);
    const a = this.stats, i = this.lruCache, o = this.downloadQueue, l = this.parseQueue, d3 = this.loadingTiles, h2 = q(r2), v2 = new AbortController(), f2 = v2.signal;
    if (i.add(e, (c2) => {
      v2.abort(), s ? c2.children.length = 0 : this.invokeAllPlugins((p3) => {
        p3.disposeTile && p3.disposeTile(c2);
      }), a.inCache--, this.cachedSinceLoadComplete.has(e) && (this.cachedSinceLoadComplete.delete(e), a.inCacheSinceLoad--), c2.internal.loadingState === R ? a.queued-- : c2.internal.loadingState === _ ? a.downloading-- : c2.internal.loadingState === D ? a.parsing-- : c2.internal.loadingState === F && a.loaded--, c2.internal.loadingState = I, l.remove(c2), o.remove(c2), d3.delete(c2);
    }))
      return this.isLoading || (this.isLoading = true, this.dispatchEvent({ type: "tiles-load-start" })), i.setMemoryUsage(e, this.getBytesUsed(e)), this.cachedSinceLoadComplete.add(e), a.inCacheSinceLoad++, a.inCache++, a.queued++, e.internal.loadingState = R, d3.add(e), o.add(e, (c2) => {
        if (f2.aborted)
          return Promise.resolve();
        e.internal.loadingState = _, a.downloading++, a.queued--;
        const p3 = this.invokeOnePlugin((g3) => g3.fetchData && g3.fetchData(r2, { ...this.fetchOptions, signal: f2 }));
        return this.dispatchEvent({ type: "tile-download-start", tile: e, uri: r2 }), p3;
      }).then((c2) => {
        if (!f2.aborted)
          if (c2 instanceof Response) {
            if (c2.ok)
              return h2 === "json" ? c2.json() : c2.arrayBuffer();
            throw new Error(`Failed to load model with error code ${c2.status}`);
          } else return c2;
      }).then((c2) => {
        if (!f2.aborted)
          return a.downloading--, a.parsing++, e.internal.loadingState = D, l.add(e, (p3) => f2.aborted ? Promise.resolve() : h2 === "json" && c2.root ? (this.preprocessTileset(c2, r2, e), e.children.push(c2.root), n = c2, s = true, Promise.resolve()) : this.invokeOnePlugin((g3) => g3.parseTile && g3.parseTile(c2, p3, h2, r2, f2)));
      }).then(() => {
        if (f2.aborted)
          return;
        a.parsing--, a.loaded++, e.internal.loadingState = F, d3.delete(e), i.setLoaded(e, true);
        const c2 = this.getBytesUsed(e);
        if (i.getMemoryUsage(e) === 0 && c2 > 0 && i.isFull()) {
          i.remove(e);
          return;
        }
        i.setMemoryUsage(e, c2), this.dispatchEvent({ type: "needs-update" }), this.dispatchEvent({ type: "load-content" }), s && this.dispatchEvent({
          type: "load-tileset",
          tileset: n,
          url: r2
        }), e.engineData.scene && this.dispatchEvent({
          type: "load-model",
          scene: e.engineData.scene,
          tile: e,
          url: r2
        });
      }).catch((c2) => {
        f2.aborted || (c2.name !== "AbortError" ? (l.remove(e), o.remove(e), e.internal.loadingState === R ? a.queued-- : e.internal.loadingState === _ ? a.downloading-- : e.internal.loadingState === D ? a.parsing-- : e.internal.loadingState === F && a.loaded--, a.failed++, console.error(`TilesRenderer : Failed to load tile at url "${e.content.uri}".`), console.error(c2), e.internal.loadingState = J, d3.delete(e), i.setLoaded(e, true), this.dispatchEvent({
          type: "load-error",
          tile: e,
          error: c2,
          url: r2
        })) : i.remove(e));
      });
  }
};
function ae(t, e, s, n, r2, a) {
  let i;
  switch (n) {
    case "SCALAR":
      i = 1;
      break;
    case "VEC2":
      i = 2;
      break;
    case "VEC3":
      i = 3;
      break;
    case "VEC4":
      i = 4;
      break;
    default:
      throw new Error(`FeatureTable : Feature type not provided for "${a}".`);
  }
  let o;
  const l = s * i;
  switch (r2) {
    case "BYTE":
      o = new Int8Array(t, e, l);
      break;
    case "UNSIGNED_BYTE":
      o = new Uint8Array(t, e, l);
      break;
    case "SHORT":
      o = new Int16Array(t, e, l);
      break;
    case "UNSIGNED_SHORT":
      o = new Uint16Array(t, e, l);
      break;
    case "INT":
      o = new Int32Array(t, e, l);
      break;
    case "UNSIGNED_INT":
      o = new Uint32Array(t, e, l);
      break;
    case "FLOAT":
      o = new Float32Array(t, e, l);
      break;
    case "DOUBLE":
      o = new Float64Array(t, e, l);
      break;
    default:
      throw new Error(`FeatureTable : Feature component type not provided for "${a}".`);
  }
  return o;
}
var ie = class {
  constructor(e, s, n, r2) {
    this.buffer = e, this.binOffset = s + n, this.binLength = r2;
    let a = null;
    if (n !== 0) {
      const i = new Uint8Array(e, s, n);
      a = JSON.parse(f(i));
    } else
      a = {};
    this.header = a;
  }
  getKeys() {
    return Object.keys(this.header).filter((e) => e !== "extensions");
  }
  getData(e, s, n = null, r2 = null) {
    const a = this.header;
    if (!(e in a))
      return null;
    const i = a[e];
    if (i instanceof Object) {
      if (Array.isArray(i))
        return i;
      {
        const { buffer: o, binOffset: l, binLength: d3 } = this, h2 = i.byteOffset || 0, v2 = i.type || r2, f2 = i.componentType || n;
        if ("type" in i && r2 && i.type !== r2)
          throw new Error("FeatureTable: Specified type does not match expected type.");
        const u2 = l + h2, c2 = ae(o, u2, s, v2, f2, e);
        if (u2 + c2.byteLength > l + d3)
          throw new Error("FeatureTable: Feature data read outside binary body length.");
        return c2;
      }
    } else return i;
  }
  getBuffer(e, s) {
    const { buffer: n, binOffset: r2 } = this;
    return n.slice(r2 + e, r2 + e + s);
  }
};
var Ce = class {
  constructor(e) {
    this.batchTable = e;
    const s = e.header.extensions["3DTILES_batch_table_hierarchy"];
    this.classes = s.classes;
    for (const r2 of this.classes) {
      const a = r2.instances;
      for (const i in a)
        r2.instances[i] = this._parseProperty(a[i], r2.length, i);
    }
    if (this.instancesLength = s.instancesLength, this.classIds = this._parseProperty(s.classIds, this.instancesLength, "classIds"), s.parentCounts ? this.parentCounts = this._parseProperty(s.parentCounts, this.instancesLength, "parentCounts") : this.parentCounts = new Array(this.instancesLength).fill(1), s.parentIds) {
      const r2 = this.parentCounts.reduce((a, i) => a + i, 0);
      this.parentIds = this._parseProperty(s.parentIds, r2, "parentIds");
    } else
      this.parentIds = null;
    this.instancesIds = [];
    const n = {};
    for (const r2 of this.classIds)
      n[r2] = n[r2] ?? 0, this.instancesIds.push(n[r2]), n[r2]++;
  }
  _parseProperty(e, s, n) {
    if (Array.isArray(e))
      return e;
    {
      const { buffer: r2, binOffset: a } = this.batchTable, i = e.byteOffset, o = e.componentType || "UNSIGNED_SHORT", l = a + i;
      return ae(r2, l, s, "SCALAR", o, n);
    }
  }
  getDataFromId(e, s = {}) {
    const n = this.parentCounts[e];
    if (this.parentIds && n > 0) {
      let l = 0;
      for (let d3 = 0; d3 < e; d3++)
        l += this.parentCounts[d3];
      for (let d3 = 0; d3 < n; d3++) {
        const h2 = this.parentIds[l + d3];
        h2 !== e && this.getDataFromId(h2, s);
      }
    }
    const r2 = this.classIds[e], a = this.classes[r2].instances, i = this.classes[r2].name, o = this.instancesIds[e];
    for (const l in a)
      s[i] = s[i] || {}, s[i][l] = a[l][o];
    return s;
  }
};
var be = class extends ie {
  get batchSize() {
    return console.warn("BatchTable.batchSize has been deprecated and replaced with BatchTable.count."), this.count;
  }
  constructor(e, s, n, r2, a) {
    super(e, n, r2, a), this.count = s, this.extensions = {};
    const i = this.header.extensions;
    i && i["3DTILES_batch_table_hierarchy"] && (this.extensions["3DTILES_batch_table_hierarchy"] = new Ce(this));
  }
  getData(e, s = null, n = null) {
    return console.warn("BatchTable: BatchTable.getData is deprecated. Use BatchTable.getDataFromId to get allproperties for an id or BatchTable.getPropertyArray for getting an array of value for a property."), super.getData(e, this.count, s, n);
  }
  getDataFromId(e, s = {}) {
    if (e < 0 || e >= this.count)
      throw new Error(`BatchTable: id value "${e}" out of bounds for "${this.count}" features number.`);
    for (const n of this.getKeys())
      s[n] = super.getData(n, this.count)[e];
    for (const n in this.extensions) {
      const r2 = this.extensions[n];
      r2.getDataFromId instanceof Function && (s[n] = s[n] || {}, r2.getDataFromId(e, s[n]));
    }
    return s;
  }
  getPropertyArray(e) {
    return super.getData(e, this.count);
  }
};
var Fe = class extends y {
  parse(e) {
    const s = new DataView(e), n = d(s);
    console.assert(n === "b3dm");
    const r2 = s.getUint32(4, true);
    console.assert(r2 === 1);
    const a = s.getUint32(8, true);
    console.assert(a === e.byteLength);
    const i = s.getUint32(12, true), o = s.getUint32(16, true), l = s.getUint32(20, true), d3 = s.getUint32(24, true), h2 = 28, v2 = e.slice(
      h2,
      h2 + i + o
    ), f2 = new ie(
      v2,
      0,
      i,
      o
    ), u2 = h2 + i + o, c2 = e.slice(
      u2,
      u2 + l + d3
    ), p3 = new be(
      c2,
      f2.getData("BATCH_LENGTH"),
      0,
      l,
      d3
    ), g3 = u2 + l + d3, oe3 = new Uint8Array(e, g3, a - g3);
    return {
      version: r2,
      featureTable: f2,
      batchTable: p3,
      glbBytes: oe3
    };
  }
};

// node_modules/@giro3d/giro3d/node_modules/3d-tiles-renderer/build/index.core.js
var x2 = class extends y {
  parse(t) {
    const e = new DataView(t), L3 = d(e);
    console.assert(L3 === "i3dm");
    const i = e.getUint32(4, true);
    console.assert(i === 1);
    const T2 = e.getUint32(8, true);
    console.assert(T2 === t.byteLength);
    const n = e.getUint32(12, true), a = e.getUint32(16, true), s = e.getUint32(20, true), r2 = e.getUint32(24, true), o = e.getUint32(28, true), l = 32, g3 = t.slice(
      l,
      l + n + a
    ), c2 = new ie(
      g3,
      0,
      n,
      a
    ), h2 = l + n + a, y4 = t.slice(
      h2,
      h2 + s + r2
    ), E2 = new be(
      y4,
      c2.getData("INSTANCES_LENGTH"),
      0,
      s,
      r2
    ), m2 = h2 + s + r2, S2 = new Uint8Array(t, m2, T2 - m2);
    let B4 = null, U4 = null, D3 = null;
    if (o)
      B4 = S2, U4 = Promise.resolve();
    else {
      const d3 = this.resolveExternalURL(f(S2));
      D3 = c(d3), U4 = fetch(d3, this.fetchOptions).then((u2) => {
        if (!u2.ok)
          throw new Error(`I3DMLoaderBase : Failed to load file "${d3}" with status ${u2.status} : ${u2.statusText}`);
        return u2.arrayBuffer();
      }).then((u2) => {
        B4 = new Uint8Array(u2);
      });
    }
    return U4.then(() => ({
      version: i,
      featureTable: c2,
      batchTable: E2,
      glbBytes: B4,
      gltfWorkingPath: D3
    }));
  }
};
var G3 = class extends y {
  parse(t) {
    const e = new DataView(t), L3 = d(e);
    console.assert(L3 === "pnts");
    const i = e.getUint32(4, true);
    console.assert(i === 1);
    const T2 = e.getUint32(8, true);
    console.assert(T2 === t.byteLength);
    const n = e.getUint32(12, true), a = e.getUint32(16, true), s = e.getUint32(20, true), r2 = e.getUint32(24, true), o = 28, l = t.slice(
      o,
      o + n + a
    ), g3 = new ie(
      l,
      0,
      n,
      a
    ), c2 = o + n + a, h2 = t.slice(
      c2,
      c2 + s + r2
    ), y4 = new be(
      h2,
      g3.getData("BATCH_LENGTH") || g3.getData("POINTS_LENGTH"),
      0,
      s,
      r2
    );
    return Promise.resolve({
      version: i,
      featureTable: g3,
      batchTable: y4
    });
  }
};
var M2 = class extends y {
  parse(t) {
    const e = new DataView(t), L3 = d(e);
    console.assert(L3 === "cmpt", 'CMPTLoader: The magic bytes equal "cmpt".');
    const i = e.getUint32(4, true);
    console.assert(i === 1, 'CMPTLoader: The version listed in the header is "1".');
    const T2 = e.getUint32(8, true);
    console.assert(T2 === t.byteLength, "CMPTLoader: The contents buffer length listed in the header matches the file.");
    const n = e.getUint32(12, true), a = [];
    let s = 16;
    for (let r2 = 0; r2 < n; r2++) {
      const o = new DataView(t, s, 12), l = d(o), g3 = o.getUint32(4, true), c2 = o.getUint32(8, true), h2 = new Uint8Array(t, s, c2);
      a.push({
        type: l,
        buffer: h2,
        version: g3
      }), s += c2;
    }
    return {
      version: i,
      tiles: a
    };
  }
};

// node_modules/@giro3d/giro3d/node_modules/3d-tiles-renderer/build/MemoryUtils-BD0AS93P.js
var b2 = new Spherical();
var et = new Vector3();
var _t = {};
function xt(c2) {
  const { x: t, y: i, z: o } = c2;
  c2.x = o, c2.y = t, c2.z = i;
}
function Ct(c2) {
  const { x: t, y: i, z: o } = c2;
  c2.z = t, c2.x = i, c2.y = o;
}
function gt(c2) {
  return -(c2 - Math.PI / 2);
}
function J3(c2) {
  return -c2 + Math.PI / 2;
}
function Rt(c2, t, i = {}) {
  return b2.theta = t, b2.phi = J3(c2), et.setFromSpherical(b2), b2.setFromVector3(et), i.lat = gt(b2.phi), i.lon = b2.theta, i;
}
function st(c2, t = "E", i = "W") {
  const o = c2 < 0 ? i : t;
  c2 = Math.abs(c2);
  const e = ~~c2, s = (c2 - e) * 60, n = ~~s, l = ~~((s - n) * 60);
  return `${e}° ${n}' ${l}" ${o}`;
}
function bt(c2, t, i = false) {
  const o = Rt(c2, t, _t);
  let e, s;
  return i ? (e = `${(MathUtils.RAD2DEG * o.lat).toFixed(4)}°`, s = `${(MathUtils.RAD2DEG * o.lon).toFixed(4)}°`) : (e = st(MathUtils.RAD2DEG * o.lat, "N", "S"), s = st(MathUtils.RAD2DEG * o.lon, "E", "W")), `${e} ${s}`;
}
var Ut = Object.freeze(Object.defineProperty({
  __proto__: null,
  latitudeToSphericalPhi: J3,
  sphericalPhiToLatitude: gt,
  swapToGeoFrame: xt,
  swapToThreeFrame: Ct,
  toLatLonString: bt
}, Symbol.toStringTag, { value: "Module" }));
var nt = new Spherical();
var z3 = new Vector3();
var x3 = new Vector3();
var Y2 = new Vector3();
var P2 = new Matrix4();
var S = new Matrix4();
var rt = new Matrix4();
var Z2 = new Sphere();
var g2 = new Euler();
var at = new Vector3();
var lt = new Vector3();
var ct = new Vector3();
var _3 = new Vector3();
var O2 = new Ray();
var vt = 1e-12;
var At = 0.1;
var q2 = 0;
var ht = 1;
var U = 2;
var Mt = class {
  constructor(t = 1, i = 1, o = 1) {
    this.name = "", this.radius = new Vector3(t, i, o);
  }
  intersectRay(t, i) {
    return P2.makeScale(...this.radius).invert(), Z2.center.set(0, 0, 0), Z2.radius = 1, O2.copy(t).applyMatrix4(P2), O2.intersectSphere(Z2, i) ? (P2.makeScale(...this.radius), i.applyMatrix4(P2), i) : null;
  }
  // returns a frame with Z indicating altitude, Y pointing north, X pointing east
  getEastNorthUpFrame(t, i, o, e) {
    return o.isMatrix4 && (e = o, o = 0, console.warn('Ellipsoid: The signature for "getEastNorthUpFrame" has changed.')), this.getEastNorthUpAxes(t, i, at, lt, ct), this.getCartographicToPosition(t, i, o, _3), e.makeBasis(at, lt, ct).setPosition(_3);
  }
  // returns a frame with z indicating altitude and az, el, roll rotation within that frame
  // - azimuth: measured off of true north, increasing towards "east" (z-axis)
  // - elevation: measured off of the horizon, increasing towards sky (x-axis)
  // - roll: rotation around northern axis (y-axis)
  getOrientedEastNorthUpFrame(t, i, o, e, s, n, a) {
    return this.getObjectFrame(t, i, o, e, s, n, a, q2);
  }
  // returns a frame similar to the ENU frame but rotated to match three.js object and camera conventions
  // OBJECT_FRAME: oriented such that "+Y" is up and "+Z" is forward.
  // CAMERA_FRAME: oriented such that "+Y" is up and "-Z" is forward.
  getObjectFrame(t, i, o, e, s, n, a, l = U) {
    return this.getEastNorthUpFrame(t, i, o, P2), g2.set(s, n, -e, "ZXY"), a.makeRotationFromEuler(g2).premultiply(P2), l === ht ? (g2.set(Math.PI / 2, 0, 0, "XYZ"), S.makeRotationFromEuler(g2), a.multiply(S)) : l === U && (g2.set(-Math.PI / 2, 0, Math.PI, "XYZ"), S.makeRotationFromEuler(g2), a.multiply(S)), a;
  }
  getCartographicFromObjectFrame(t, i, o = U) {
    return o === ht ? (g2.set(-Math.PI / 2, 0, 0, "XYZ"), S.makeRotationFromEuler(g2).premultiply(t)) : o === U ? (g2.set(-Math.PI / 2, 0, Math.PI, "XYZ"), S.makeRotationFromEuler(g2).premultiply(t)) : S.copy(t), _3.setFromMatrixPosition(S), this.getPositionToCartographic(_3, i), this.getEastNorthUpFrame(i.lat, i.lon, 0, P2).invert(), S.premultiply(P2), g2.setFromRotationMatrix(S, "ZXY"), i.azimuth = -g2.z, i.elevation = g2.x, i.roll = g2.y, i;
  }
  getEastNorthUpAxes(t, i, o, e, s, n = _3) {
    this.getCartographicToPosition(t, i, 0, n), this.getCartographicToNormal(t, i, s), o.set(-n.y, n.x, 0).normalize(), e.crossVectors(s, o).normalize();
  }
  // azimuth: measured off of true north, increasing towards "east"
  // elevation: measured off of the horizon, increasing towards sky
  // roll: rotation around northern axis
  getAzElRollFromRotationMatrix(t, i, o, e, s = q2) {
    return console.warn('Ellipsoid: "getAzElRollFromRotationMatrix" is deprecated. Use "getCartographicFromObjectFrame", instead.'), this.getCartographicToPosition(t, i, 0, _3), rt.copy(o).setPosition(_3), this.getCartographicFromObjectFrame(rt, e, s), delete e.height, delete e.lat, delete e.lon, e;
  }
  getRotationMatrixFromAzElRoll(t, i, o, e, s, n, a = q2) {
    return console.warn('Ellipsoid: "getRotationMatrixFromAzElRoll" function has been deprecated. Use "getObjectFrame", instead.'), this.getObjectFrame(t, i, 0, o, e, s, n, a), n.setPosition(0, 0, 0), n;
  }
  getFrame(t, i, o, e, s, n, a, l = q2) {
    return console.warn('Ellipsoid: "getFrame" function has been deprecated. Use "getObjectFrame", instead.'), this.getObjectFrame(t, i, n, o, e, s, a, l);
  }
  getCartographicToPosition(t, i, o, e) {
    this.getCartographicToNormal(t, i, z3);
    const s = this.radius;
    x3.copy(z3), x3.x *= s.x ** 2, x3.y *= s.y ** 2, x3.z *= s.z ** 2;
    const n = Math.sqrt(z3.dot(x3));
    return x3.divideScalar(n), e.copy(x3).addScaledVector(z3, o);
  }
  getPositionToCartographic(t, i) {
    this.getPositionToSurfacePoint(t, x3), this.getPositionToNormal(t, z3);
    const o = Y2.subVectors(t, x3);
    return i.lon = Math.atan2(z3.y, z3.x), i.lat = Math.asin(z3.z), i.height = Math.sign(o.dot(t)) * o.length(), i;
  }
  getCartographicToNormal(t, i, o) {
    return nt.set(1, J3(t), i), o.setFromSpherical(nt).normalize(), xt(o), o;
  }
  getPositionToNormal(t, i) {
    const o = this.radius;
    return i.copy(t), i.x /= o.x ** 2, i.y /= o.y ** 2, i.z /= o.z ** 2, i.normalize(), i;
  }
  getPositionToSurfacePoint(t, i) {
    const o = this.radius, e = 1 / o.x ** 2, s = 1 / o.y ** 2, n = 1 / o.z ** 2, a = t.x * t.x * e, l = t.y * t.y * s, p3 = t.z * t.z * n, h2 = a + l + p3, w6 = Math.sqrt(1 / h2), f2 = x3.copy(t).multiplyScalar(w6);
    if (h2 < At)
      return isFinite(w6) ? i.copy(f2) : null;
    const E2 = Y2.set(
      f2.x * e * 2,
      f2.y * s * 2,
      f2.z * n * 2
    );
    let m2 = (1 - w6) * t.length() / (0.5 * E2.length()), y4 = 0, k3, K3, v2, A3, N4, L3, V2, X2, Q4, tt2, it3;
    do {
      m2 -= y4, v2 = 1 / (1 + m2 * e), A3 = 1 / (1 + m2 * s), N4 = 1 / (1 + m2 * n), L3 = v2 * v2, V2 = A3 * A3, X2 = N4 * N4, Q4 = L3 * v2, tt2 = V2 * A3, it3 = X2 * N4, k3 = a * L3 + l * V2 + p3 * X2 - 1, K3 = a * Q4 * e + l * tt2 * s + p3 * it3 * n;
      const Pt2 = -2 * K3;
      y4 = k3 / Pt2;
    } while (Math.abs(k3) > vt);
    return i.set(
      t.x * v2,
      t.y * A3,
      t.z * N4
    );
  }
  calculateHorizonDistance(t, i) {
    const o = this.calculateEffectiveRadius(t);
    return Math.sqrt(2 * o * i + i ** 2);
  }
  calculateEffectiveRadius(t) {
    const i = this.radius.x, e = 1 - this.radius.z ** 2 / i ** 2, s = t * MathUtils.DEG2RAD, n = Math.sin(s) ** 2;
    return i / Math.sqrt(1 - e * n);
  }
  getPositionElevation(t) {
    this.getPositionToSurfacePoint(t, x3);
    const i = Y2.subVectors(t, x3);
    return Math.sign(i.dot(t)) * i.length();
  }
  // Returns an estimate of the closest point on the ellipsoid to the ray. Returns
  // the surface intersection if they collide.
  closestPointToRayEstimate(t, i) {
    return this.intersectRay(t, i) ? i : (P2.makeScale(...this.radius).invert(), O2.copy(t).applyMatrix4(P2), x3.set(0, 0, 0), O2.closestPointToPoint(x3, i).normalize(), P2.makeScale(...this.radius), i.applyMatrix4(P2));
  }
  copy(t) {
    return this.radius.copy(t.radius), this;
  }
  clone() {
    return new this.constructor().copy(this);
  }
};
var Nt = new Mt(N, N, Q);
Nt.name = "WGS84 Earth";
var j3 = new Vector3();
var $2 = new Vector3();
var M3 = new Vector3();
var G4 = new Ray();
var jt = class {
  constructor(t = new Box3(), i = new Matrix4()) {
    this.box = t.clone(), this.transform = i.clone(), this.inverseTransform = new Matrix4(), this.points = new Array(8).fill().map(() => new Vector3()), this.planes = new Array(6).fill().map(() => new Plane());
  }
  copy(t) {
    return this.box.copy(t.box), this.transform.copy(t.transform), this.update(), this;
  }
  clone() {
    return new this.constructor().copy(this);
  }
  /**
   * Clamps the given point within the bounds of this OBB
   * @param {Vector3} point
   * @param {Vector3} result
   * @returns {Vector3}
   */
  clampPoint(t, i) {
    return i.copy(t).applyMatrix4(this.inverseTransform).clamp(this.box.min, this.box.max).applyMatrix4(this.transform);
  }
  /**
   * Returns the distance from any edge of this OBB to the specified point.
   * If the point lies inside of this box, the distance will be 0.
   * @param {Vector3} point
   * @returns {number}
   */
  distanceToPoint(t) {
    return this.clampPoint(t, M3).distanceTo(t);
  }
  containsPoint(t) {
    return M3.copy(t).applyMatrix4(this.inverseTransform), this.box.containsPoint(M3);
  }
  // returns boolean indicating whether the ray has intersected the obb
  intersectsRay(t) {
    return G4.copy(t).applyMatrix4(this.inverseTransform), G4.intersectsBox(this.box);
  }
  // Sets "target" equal to the intersection point.
  // Returns "null" if no intersection found.
  intersectRay(t, i) {
    return G4.copy(t).applyMatrix4(this.inverseTransform), G4.intersectBox(this.box, i) ? (i.applyMatrix4(this.transform), i) : null;
  }
  update() {
    const { points: t, inverseTransform: i, transform: o, box: e } = this;
    i.copy(o).invert();
    const { min: s, max: n } = e;
    let a = 0;
    for (let l = -1; l <= 1; l += 2)
      for (let p3 = -1; p3 <= 1; p3 += 2)
        for (let h2 = -1; h2 <= 1; h2 += 2)
          t[a].set(
            l < 0 ? s.x : n.x,
            p3 < 0 ? s.y : n.y,
            h2 < 0 ? s.z : n.z
          ).applyMatrix4(o), a++;
    this.updatePlanes();
  }
  updatePlanes() {
    j3.copy(this.box.min).applyMatrix4(this.transform), $2.copy(this.box.max).applyMatrix4(this.transform), M3.set(0, 0, 1).transformDirection(this.transform), this.planes[0].setFromNormalAndCoplanarPoint(M3, j3), this.planes[1].setFromNormalAndCoplanarPoint(M3, $2).negate(), M3.set(0, 1, 0).transformDirection(this.transform), this.planes[2].setFromNormalAndCoplanarPoint(M3, j3), this.planes[3].setFromNormalAndCoplanarPoint(M3, $2).negate(), M3.set(1, 0, 0).transformDirection(this.transform), this.planes[4].setFromNormalAndCoplanarPoint(M3, j3), this.planes[5].setFromNormalAndCoplanarPoint(M3, $2).negate();
  }
  intersectsSphere(t) {
    return this.clampPoint(t.center, M3), M3.distanceToSquared(t.center) <= t.radius * t.radius;
  }
  intersectsFrustum(t) {
    return this._intersectsPlaneShape(t.planes, t.points);
  }
  intersectsOBB(t) {
    return this._intersectsPlaneShape(t.planes, t.points);
  }
  // takes a series of 6 planes that define and enclosed shape and the 8 points that lie at the corners
  // of that shape to determine whether the OBB is intersected with.
  _intersectsPlaneShape(t, i) {
    const o = this.points, e = this.planes;
    for (let s = 0; s < 6; s++) {
      const n = t[s];
      let a = -1 / 0;
      for (let l = 0; l < 8; l++) {
        const p3 = o[l], h2 = n.distanceToPoint(p3);
        a = a < h2 ? h2 : a;
      }
      if (a < 0)
        return false;
    }
    for (let s = 0; s < 6; s++) {
      const n = e[s];
      let a = -1 / 0;
      for (let l = 0; l < 8; l++) {
        const p3 = i[l], h2 = n.distanceToPoint(p3);
        a = a < h2 ? h2 : a;
      }
      if (a < 0)
        return false;
    }
    return true;
  }
};
var W2 = 1e-13;
var I2 = Math.PI;
var H2 = I2 / 2;
var B2 = new Vector3();
var C3 = new Vector3();
var T = new Vector3();
var r = new Vector3();
var d2 = new Matrix4();
var Bt = new Box3();
var pt = new Matrix4();
function F2(c2, t) {
  t.radius = Math.max(t.radius, c2.distanceToSquared(t.center));
}
function ut(c2) {
  return c2.x !== c2.y;
}
var $t = class extends Mt {
  constructor(t = 1, i = 1, o = 1, e = -H2, s = H2, n = 0, a = 2 * I2, l = 0, p3 = 0) {
    super(t, i, o), this.latStart = e, this.latEnd = s, this.lonStart = n, this.lonEnd = a, this.heightStart = l, this.heightEnd = p3;
  }
  getBoundingBox(t, i) {
    ut(this.radius) && console.warn("EllipsoidRegion: Triaxial ellipsoids are not supported.");
    const {
      latStart: o,
      latEnd: e,
      lonStart: s,
      lonEnd: n,
      heightStart: a,
      heightEnd: l
    } = this, p3 = (o + e) * 0.5, h2 = (s + n) * 0.5, w6 = o > 0, f2 = e < 0;
    let E2;
    w6 ? E2 = o : f2 ? E2 = e : E2 = 0;
    const { min: m2, max: y4 } = t;
    m2.setScalar(1 / 0), y4.setScalar(-1 / 0), n - s <= I2 ? (this.getCartographicToNormal(p3, h2, T), C3.set(0, 0, 1), B2.crossVectors(C3, T).normalize(), C3.crossVectors(T, B2).normalize(), i.makeBasis(B2, C3, T), d2.copy(i).invert(), this.getCartographicToPosition(E2, s, l, r).applyMatrix4(d2), y4.x = Math.abs(r.x), m2.x = -y4.x, this.getCartographicToPosition(e, s, l, r).applyMatrix4(d2), y4.y = r.y, this.getCartographicToPosition(e, h2, l, r).applyMatrix4(d2), y4.y = Math.max(r.y, y4.y), this.getCartographicToPosition(o, s, l, r).applyMatrix4(d2), m2.y = r.y, this.getCartographicToPosition(o, h2, l, r).applyMatrix4(d2), m2.y = Math.min(r.y, m2.y), this.getCartographicToPosition(p3, h2, l, r).applyMatrix4(d2), y4.z = r.z, this.getCartographicToPosition(o, s, a, r).applyMatrix4(d2), m2.z = r.z, this.getCartographicToPosition(e, s, a, r).applyMatrix4(d2), m2.z = Math.min(r.z, m2.z)) : (this.getCartographicToPosition(E2, h2, l, T), T.z = 0, T.length() < 1e-10 ? T.set(1, 0, 0) : T.normalize(), C3.set(0, 0, 1), B2.crossVectors(T, C3).normalize(), i.makeBasis(B2, C3, T), d2.copy(i).invert(), this.getCartographicToPosition(E2, h2 + H2, l, r).applyMatrix4(d2), y4.x = Math.abs(r.x), m2.x = -y4.x, this.getCartographicToPosition(e, 0, f2 ? a : l, r).applyMatrix4(d2), y4.y = r.y, this.getCartographicToPosition(o, 0, w6 ? a : l, r).applyMatrix4(d2), m2.y = r.y, this.getCartographicToPosition(E2, h2, l, r).applyMatrix4(d2), y4.z = r.z, this.getCartographicToPosition(E2, n, l, r).applyMatrix4(d2), m2.z = r.z), t.getCenter(r), t.min.sub(r).multiplyScalar(1 + W2), t.max.sub(r).multiplyScalar(1 + W2), r.applyMatrix4(i), i.setPosition(r);
  }
  getBoundingSphere(t) {
    ut(this.radius) && console.warn("EllipsoidRegion: Triaxial ellipsoids are not supported."), this.getBoundingBox(Bt, pt), t.center.setFromMatrixPosition(pt), t.radius = 0;
    const {
      latStart: i,
      latEnd: o,
      lonStart: e,
      lonEnd: s,
      heightStart: n,
      heightEnd: a
    } = this, l = (i + o) * 0.5, p3 = (e + s) * 0.5, h2 = i > 0, w6 = o < 0;
    let f2;
    h2 ? f2 = i : w6 ? f2 = o : f2 = 0, this.getCartographicToPosition(f2, e, a, r), F2(r, t), this.getCartographicToPosition(o, e, a, r), F2(r, t), this.getCartographicToPosition(o, p3, a, r), F2(r, t), this.getCartographicToPosition(i, e, a, r), F2(r, t), this.getCartographicToPosition(i, p3, a, r), F2(r, t), this.getCartographicToPosition(l, p3, a, r), F2(r, t), this.getCartographicToPosition(i, e, n, r), F2(r, t), s - e > I2 && (this.getCartographicToPosition(f2, p3 + I2, a, r), F2(r, t)), t.radius = Math.sqrt(t.radius) * (1 + W2);
  }
};
function ft(c2) {
  if (!c2)
    return 0;
  const { format: t, type: i, image: o } = c2, { width: e, height: s } = o;
  let n = TextureUtils.getByteLength(e, s, t, i);
  return n *= c2.generateMipmaps ? 4 / 3 : 1, n;
}
function Dt(c2) {
  const t = /* @__PURE__ */ new Set();
  let i = 0;
  return c2.traverse((o) => {
    if (o.geometry && !t.has(o.geometry) && (i += estimateBytesUsed(o.geometry), t.add(o.geometry)), o.material) {
      const e = o.material;
      for (const s in e) {
        const n = e[s];
        n && n.isTexture && !t.has(n) && (i += ft(n), t.add(n));
      }
    }
  }), i;
}
var Gt = Object.freeze(Object.defineProperty({
  __proto__: null,
  estimateBytesUsed: Dt,
  getTextureByteLength: ft
}, Symbol.toStringTag, { value: "Module" }));

// node_modules/@giro3d/giro3d/node_modules/3d-tiles-renderer/build/CameraTransitionManager-raJsCcaV.js
var Se = class extends Fe {
  constructor(t = DefaultLoadingManager) {
    super(), this.manager = t, this.adjustmentTransform = new Matrix4();
  }
  parse(t) {
    const e = super.parse(t), i = e.glbBytes.slice().buffer;
    return new Promise((s, o) => {
      const r2 = this.manager, n = this.fetchOptions, a = r2.getHandler("path.gltf") || new GLTFLoader(r2);
      n.credentials === "include" && n.mode === "cors" && a.setCrossOrigin("use-credentials"), "credentials" in n && a.setWithCredentials(n.credentials === "include"), n.headers && a.setRequestHeader(n.headers);
      let h2 = this.workingPath;
      !/[\\/]$/.test(h2) && h2.length && (h2 += "/");
      const l = this.adjustmentTransform;
      a.parse(i, h2, (c2) => {
        const { batchTable: p3, featureTable: f2 } = e, { scene: u2 } = c2, d3 = f2.getData("RTC_CENTER", 1, "FLOAT", "VEC3");
        d3 && (u2.position.x += d3[0], u2.position.y += d3[1], u2.position.z += d3[2]), c2.scene.updateMatrix(), c2.scene.matrix.multiply(l), c2.scene.matrix.decompose(c2.scene.position, c2.scene.quaternion, c2.scene.scale), c2.batchTable = p3, c2.featureTable = f2, u2.batchTable = p3, u2.featureTable = f2, s(c2);
      }, o);
    });
  }
};
function li(m2) {
  const t = m2 >> 11, e = m2 >> 5 & 63, i = m2 & 31, s = Math.round(t / 31 * 255), o = Math.round(e / 63 * 255), r2 = Math.round(i / 31 * 255);
  return [s, o, r2];
}
var xt2 = new Vector2();
function hi(m2, t, e = new Vector3()) {
  xt2.set(m2, t).divideScalar(256).multiplyScalar(2).subScalar(1), e.set(xt2.x, xt2.y, 1 - Math.abs(xt2.x) - Math.abs(xt2.y));
  const i = MathUtils.clamp(-e.z, 0, 1);
  return e.x >= 0 ? e.setX(e.x - i) : e.setX(e.x + i), e.y >= 0 ? e.setY(e.y - i) : e.setY(e.y + i), e.normalize(), e;
}
var ie2 = {
  RGB: "color",
  POSITION: "position"
};
var Ee = class extends G3 {
  constructor(t = DefaultLoadingManager) {
    super(), this.manager = t;
  }
  parse(t) {
    return super.parse(t).then(async (e) => {
      const { featureTable: i, batchTable: s } = e, o = new PointsMaterial(), r2 = i.header.extensions, n = new Vector3();
      let a;
      if (r2 && r2["3DTILES_draco_point_compression"]) {
        const { byteOffset: c2, byteLength: p3, properties: f2 } = r2["3DTILES_draco_point_compression"], u2 = this.manager.getHandler("draco.drc");
        if (u2 == null)
          throw new Error("PNTSLoader: dracoLoader not available.");
        const d3 = {};
        for (const _4 in f2)
          if (_4 in ie2 && _4 in f2) {
            const F3 = ie2[_4];
            d3[F3] = f2[_4];
          }
        const x5 = {
          attributeIDs: d3,
          attributeTypes: {
            position: "Float32Array",
            color: "Uint8Array"
          },
          useUniqueIDs: true
        }, E2 = i.getBuffer(c2, p3);
        a = await u2.decodeGeometry(E2, x5), a.attributes.color && (o.vertexColors = true);
      } else {
        const c2 = i.getData("POINTS_LENGTH"), p3 = i.getData("POSITION", c2, "FLOAT", "VEC3"), f2 = i.getData("NORMAL", c2, "FLOAT", "VEC3"), u2 = i.getData("NORMAL", c2, "UNSIGNED_BYTE", "VEC2"), d3 = i.getData("RGB", c2, "UNSIGNED_BYTE", "VEC3"), x5 = i.getData("RGBA", c2, "UNSIGNED_BYTE", "VEC4"), E2 = i.getData("RGB565", c2, "UNSIGNED_SHORT", "SCALAR"), _4 = i.getData("CONSTANT_RGBA", c2, "UNSIGNED_BYTE", "VEC4"), F3 = i.getData("POSITION_QUANTIZED", c2, "UNSIGNED_SHORT", "VEC3"), g3 = i.getData("QUANTIZED_VOLUME_SCALE", c2, "FLOAT", "VEC3"), S2 = i.getData("QUANTIZED_VOLUME_OFFSET", c2, "FLOAT", "VEC3");
        if (a = new BufferGeometry(), F3) {
          const C4 = new Float32Array(c2 * 3);
          for (let T2 = 0; T2 < c2; T2++)
            for (let R3 = 0; R3 < 3; R3++) {
              const V2 = 3 * T2 + R3;
              C4[V2] = F3[V2] / 65535 * g3[R3];
            }
          n.x = S2[0], n.y = S2[1], n.z = S2[2], a.setAttribute("position", new BufferAttribute(C4, 3, false));
        } else
          a.setAttribute("position", new BufferAttribute(p3, 3, false));
        if (f2 !== null)
          a.setAttribute("normal", new BufferAttribute(f2, 3, false));
        else if (u2 !== null) {
          const C4 = new Float32Array(c2 * 3), T2 = new Vector3();
          for (let R3 = 0; R3 < c2; R3++) {
            const V2 = u2[R3 * 2], yt2 = u2[R3 * 2 + 1], j5 = hi(V2, yt2, T2);
            C4[R3 * 3] = j5.x, C4[R3 * 3 + 1] = j5.y, C4[R3 * 3 + 2] = j5.z;
          }
          a.setAttribute("normal", new BufferAttribute(C4, 3, false));
        }
        if (x5 !== null)
          a.setAttribute("color", new BufferAttribute(x5, 4, true)), o.vertexColors = true, o.transparent = true, o.depthWrite = false;
        else if (d3 !== null)
          a.setAttribute("color", new BufferAttribute(d3, 3, true)), o.vertexColors = true;
        else if (E2 !== null) {
          const C4 = new Uint8Array(c2 * 3);
          for (let T2 = 0; T2 < c2; T2++) {
            const R3 = li(E2[T2]);
            for (let V2 = 0; V2 < 3; V2++) {
              const yt2 = 3 * T2 + V2;
              C4[yt2] = R3[V2];
            }
          }
          a.setAttribute("color", new BufferAttribute(C4, 3, true)), o.vertexColors = true;
        } else if (_4 !== null) {
          const C4 = new Color(_4[0], _4[1], _4[2]);
          o.color = C4;
          const T2 = _4[3] / 255;
          T2 < 1 && (o.opacity = T2, o.transparent = true, o.depthWrite = false);
        }
      }
      const h2 = new Points(a, o);
      h2.position.copy(n), e.scene = h2, e.scene.featureTable = i, e.scene.batchTable = s;
      const l = i.getData("RTC_CENTER", 1, "FLOAT", "VEC3");
      return l && (e.scene.position.x += l[0], e.scene.position.y += l[1], e.scene.position.z += l[2]), e;
    });
  }
};
var Dt2 = new Vector3();
var ct2 = new Vector3();
var lt2 = new Vector3();
var kt = new Vector3();
var wt = new Quaternion();
var Ct2 = new Vector3();
var ht2 = new Matrix4();
var se2 = new Matrix4();
var oe = new Vector3();
var ne2 = new Matrix4();
var Bt2 = new Quaternion();
var Ht = {};
function re2(m2, t, e, i) {
  if (m2 = m2 / e * 2 - 1, t = t / e * 2 - 1, i.x = m2, i.y = t, i.z = 1 - Math.abs(m2) - Math.abs(t), i.z < 0) {
    const s = i.x;
    i.x = (1 - Math.abs(i.y)) * (s >= 0 ? 1 : -1), i.y = (1 - Math.abs(s)) * (i.y >= 0 ? 1 : -1);
  }
  return i.normalize(), i;
}
var Oe = class extends x2 {
  constructor(t = DefaultLoadingManager) {
    super(), this.manager = t, this.adjustmentTransform = new Matrix4(), this.ellipsoid = Nt.clone();
  }
  resolveExternalURL(t) {
    return this.manager.resolveURL(super.resolveExternalURL(t));
  }
  parse(t) {
    return super.parse(t).then((e) => {
      const { featureTable: i, batchTable: s } = e, o = e.glbBytes.slice().buffer;
      return new Promise((r2, n) => {
        const a = this.fetchOptions, h2 = this.manager, l = h2.getHandler("path.gltf") || new GLTFLoader(h2);
        a.credentials === "include" && a.mode === "cors" && l.setCrossOrigin("use-credentials"), "credentials" in a && l.setWithCredentials(a.credentials === "include"), a.headers && l.setRequestHeader(a.headers);
        let c2 = e.gltfWorkingPath ?? this.workingPath;
        /[\\/]$/.test(c2) || (c2 += "/");
        const p3 = this.adjustmentTransform;
        l.parse(o, c2, (f2) => {
          const u2 = i.getData("INSTANCES_LENGTH");
          let d3 = i.getData("POSITION", u2, "FLOAT", "VEC3");
          const x5 = i.getData("POSITION_QUANTIZED", u2, "UNSIGNED_SHORT", "VEC3"), E2 = i.getData("QUANTIZED_VOLUME_OFFSET", 1, "FLOAT", "VEC3"), _4 = i.getData("QUANTIZED_VOLUME_SCALE", 1, "FLOAT", "VEC3"), F3 = i.getData("NORMAL_UP", u2, "FLOAT", "VEC3"), g3 = i.getData("NORMAL_RIGHT", u2, "FLOAT", "VEC3"), S2 = i.getData("NORMAL_UP_OCT32P", u2, "UNSIGNED_SHORT", "VEC2"), C4 = i.getData("NORMAL_RIGHT_OCT32P", u2, "UNSIGNED_SHORT", "VEC2"), T2 = i.getData("SCALE_NON_UNIFORM", u2, "FLOAT", "VEC3"), R3 = i.getData("SCALE", u2, "FLOAT", "SCALAR"), V2 = i.getData("RTC_CENTER", 1, "FLOAT", "VEC3"), yt2 = i.getData("EAST_NORTH_UP");
          if (!d3 && x5) {
            d3 = new Float32Array(u2 * 3);
            for (let b4 = 0; b4 < u2; b4++)
              d3[b4 * 3 + 0] = E2[0] + x5[b4 * 3 + 0] / 65535 * _4[0], d3[b4 * 3 + 1] = E2[1] + x5[b4 * 3 + 1] / 65535 * _4[1], d3[b4 * 3 + 2] = E2[2] + x5[b4 * 3 + 2] / 65535 * _4[2];
          }
          const j5 = new Vector3();
          for (let b4 = 0; b4 < u2; b4++)
            j5.x += d3[b4 * 3 + 0] / u2, j5.y += d3[b4 * 3 + 1] / u2, j5.z += d3[b4 * 3 + 2] / u2;
          const Tt2 = [], Jt = [];
          f2.scene.updateMatrixWorld(), f2.scene.traverse((b4) => {
            if (b4.isMesh) {
              Jt.push(b4);
              const { geometry: at3, material: Nt3 } = b4, q4 = new InstancedMesh(at3, Nt3, u2);
              q4.position.copy(j5), V2 && (q4.position.x += V2[0], q4.position.y += V2[1], q4.position.z += V2[2]), Tt2.push(q4);
            }
          });
          for (let b4 = 0; b4 < u2; b4++) {
            kt.set(
              d3[b4 * 3 + 0] - j5.x,
              d3[b4 * 3 + 1] - j5.y,
              d3[b4 * 3 + 2] - j5.z
            ), wt.identity(), F3 && g3 ? (ct2.set(
              F3[b4 * 3 + 0],
              F3[b4 * 3 + 1],
              F3[b4 * 3 + 2]
            ), lt2.set(
              g3[b4 * 3 + 0],
              g3[b4 * 3 + 1],
              g3[b4 * 3 + 2]
            ), Dt2.crossVectors(lt2, ct2).normalize(), ht2.makeBasis(
              lt2,
              ct2,
              Dt2
            ), wt.setFromRotationMatrix(ht2)) : S2 && C4 && (re2(
              S2[b4 * 2 + 0],
              S2[b4 * 2 + 1],
              65535,
              ct2
            ), re2(
              C4[b4 * 2 + 0],
              C4[b4 * 2 + 1],
              65535,
              lt2
            ), Dt2.crossVectors(lt2, ct2).normalize(), ht2.makeBasis(
              lt2,
              ct2,
              Dt2
            ), wt.setFromRotationMatrix(ht2)), Ct2.set(1, 1, 1), T2 && Ct2.set(
              T2[b4 * 3 + 0],
              T2[b4 * 3 + 1],
              T2[b4 * 3 + 2]
            ), R3 && Ct2.multiplyScalar(R3[b4]);
            for (let at3 = 0, Nt3 = Tt2.length; at3 < Nt3; at3++) {
              const q4 = Tt2[at3];
              Bt2.copy(wt), yt2 && (q4.updateMatrixWorld(), oe.copy(kt).applyMatrix4(q4.matrixWorld), this.ellipsoid.getPositionToCartographic(oe, Ht), this.ellipsoid.getEastNorthUpFrame(Ht.lat, Ht.lon, ne2), Bt2.setFromRotationMatrix(ne2)), ht2.compose(kt, Bt2, Ct2).multiply(p3);
              const Le2 = Jt[at3];
              se2.multiplyMatrices(ht2, Le2.matrixWorld), q4.setMatrixAt(b4, se2);
            }
          }
          f2.scene.clear(), f2.scene.add(...Tt2), f2.batchTable = s, f2.featureTable = i, f2.scene.batchTable = s, f2.scene.featureTable = i, r2(f2);
        }, n);
      });
    });
  }
};
var pi = class extends M2 {
  constructor(t = DefaultLoadingManager) {
    super(), this.manager = t, this.adjustmentTransform = new Matrix4(), this.ellipsoid = Nt.clone();
  }
  parse(t) {
    const e = super.parse(t), { manager: i, ellipsoid: s, adjustmentTransform: o } = this, r2 = [];
    for (const n in e.tiles) {
      const { type: a, buffer: h2 } = e.tiles[n];
      switch (a) {
        case "b3dm": {
          const l = h2.slice(), c2 = new Se(i);
          c2.workingPath = this.workingPath, c2.fetchOptions = this.fetchOptions, c2.adjustmentTransform.copy(o);
          const p3 = c2.parse(l.buffer);
          r2.push(p3);
          break;
        }
        case "pnts": {
          const l = h2.slice(), c2 = new Ee(i);
          c2.workingPath = this.workingPath, c2.fetchOptions = this.fetchOptions;
          const p3 = c2.parse(l.buffer);
          r2.push(p3);
          break;
        }
        case "i3dm": {
          const l = h2.slice(), c2 = new Oe(i);
          c2.workingPath = this.workingPath, c2.fetchOptions = this.fetchOptions, c2.ellipsoid.copy(s), c2.adjustmentTransform.copy(o);
          const p3 = c2.parse(l.buffer);
          r2.push(p3);
          break;
        }
      }
    }
    return Promise.all(r2).then((n) => {
      const a = new Group();
      return n.forEach((h2) => {
        a.add(h2.scene);
      }), {
        tiles: n,
        scene: a
      };
    });
  }
};
var bt2 = new Matrix4();
var di = class extends Group {
  constructor(t) {
    super(), this.isTilesGroup = true, this.name = "TilesRenderer.TilesGroup", this.tilesRenderer = t, this.matrixWorldInverse = new Matrix4();
  }
  raycast(t, e) {
    return this.tilesRenderer.optimizeRaycast ? (this.tilesRenderer.raycast(t, e), false) : true;
  }
  updateMatrixWorld(t) {
    if (this.matrixAutoUpdate && this.updateMatrix(), this.matrixWorldNeedsUpdate || t) {
      this.parent === null ? bt2.copy(this.matrix) : bt2.multiplyMatrices(this.parent.matrixWorld, this.matrix), this.matrixWorldNeedsUpdate = false;
      const e = bt2.elements, i = this.matrixWorld.elements;
      let s = false;
      for (let o = 0; o < 16; o++) {
        const r2 = e[o], n = i[o];
        if (Math.abs(r2 - n) > Number.EPSILON) {
          s = true;
          break;
        }
      }
      if (s) {
        this.matrixWorld.copy(bt2), this.matrixWorldInverse.copy(bt2).invert();
        const o = this.children;
        for (let r2 = 0, n = o.length; r2 < n; r2++)
          o[r2].updateMatrixWorld();
      }
    }
  }
  updateWorldMatrix(t, e) {
    this.parent && t && this.parent.updateWorldMatrix(t, false), this.updateMatrixWorld(true);
  }
};
var Ie = new Ray();
var jt2 = new Vector3();
var St = [];
function Re(m2, t) {
  return m2.distance - t.distance;
}
function ze(m2, t, e, i) {
  const { scene: s } = m2.engineData;
  e.invokeOnePlugin((r2) => r2.raycastTile && r2.raycastTile(m2, s, t, i)) || t.intersectObject(s, true, i);
}
function ui(m2, t, e) {
  ze(m2, t, e, St), St.sort(Re);
  const i = St[0] || null;
  return St.length = 0, i;
}
function Ae(m2) {
  return "traversal" in m2;
}
function Fe2(m2, t, e, i = null) {
  const { group: s, activeTiles: o } = m2;
  i === null && (i = Ie, i.copy(e.ray).applyMatrix4(s.matrixWorldInverse));
  const r2 = [], n = t.children;
  for (let l = 0, c2 = n.length; l < c2; l++) {
    const p3 = n[l];
    if (!Ae(p3) || !p3.traversal.used)
      continue;
    p3.engineData.boundingVolume.intersectRay(i, jt2) !== null && (jt2.applyMatrix4(s.matrixWorld), r2.push({
      distance: jt2.distanceToSquared(e.ray.origin),
      tile: p3
    }));
  }
  r2.sort(Re);
  let a = null, h2 = 1 / 0;
  if (o.has(t)) {
    const l = ui(t, e, m2);
    l && (a = l, h2 = l.distance * l.distance);
  }
  for (let l = 0, c2 = r2.length; l < c2; l++) {
    const p3 = r2[l], f2 = p3.distance, u2 = p3.tile;
    if (f2 > h2)
      break;
    const d3 = Fe2(m2, u2, e, i);
    if (d3) {
      const x5 = d3.distance * d3.distance;
      x5 < h2 && (a = d3, h2 = x5);
    }
  }
  return a;
}
function Ue(m2, t, e, i, s = null) {
  if (!Ae(t))
    return;
  const { group: o, activeTiles: r2 } = m2, { boundingVolume: n } = t.engineData;
  if (s === null && (s = Ie, s.copy(e.ray).applyMatrix4(o.matrixWorldInverse)), !t.traversal.used || !n.intersectsRay(s))
    return;
  r2.has(t) && ze(t, e, m2, i);
  const a = t.children;
  for (let h2 = 0, l = a.length; h2 < l; h2++)
    Ue(m2, a[h2], e, i, s);
}
var Q3 = new Vector3();
var $3 = new Vector3();
var Y3 = new Vector3();
var ae2 = new Vector3();
var ce = new Vector3();
var mi = class {
  constructor() {
    this.sphere = null, this.obb = null, this.region = null, this.regionObb = null;
  }
  intersectsRay(t) {
    const e = this.sphere, i = this.obb || this.regionObb;
    return !(e && !t.intersectsSphere(e) || i && !i.intersectsRay(t));
  }
  intersectRay(t, e = null) {
    const i = this.sphere, s = this.obb || this.regionObb;
    let o = -1 / 0, r2 = -1 / 0;
    i && t.intersectSphere(i, ae2) && (o = i.containsPoint(t.origin) ? 0 : t.origin.distanceToSquared(ae2)), s && s.intersectRay(t, ce) && (r2 = s.containsPoint(t.origin) ? 0 : t.origin.distanceToSquared(ce));
    const n = Math.max(o, r2);
    return n === -1 / 0 ? null : (t.at(Math.sqrt(n), e), e);
  }
  distanceToPoint(t) {
    const e = this.sphere, i = this.obb || this.regionObb;
    let s = -1 / 0, o = -1 / 0;
    return e && (s = Math.max(e.distanceToPoint(t), 0)), i && (o = i.distanceToPoint(t)), s > o ? s : o;
  }
  intersectsFrustum(t) {
    const e = this.obb || this.regionObb, i = this.sphere;
    return i && !t.intersectsSphere(i) || e && !e.intersectsFrustum(t) ? false : !!(i || e);
  }
  intersectsSphere(t) {
    const e = this.obb || this.regionObb, i = this.sphere;
    return i && !i.intersectsSphere(t) || e && !e.intersectsSphere(t) ? false : !!(i || e);
  }
  intersectsOBB(t) {
    const e = this.obb || this.regionObb, i = this.sphere;
    return i && !t.intersectsSphere(i) || e && !e.intersectsOBB(t) ? false : !!(i || e);
  }
  getOBB(t, e) {
    const i = this.obb || this.regionObb;
    i ? (t.copy(i.box), e.copy(i.transform)) : (this.getAABB(t), e.identity());
  }
  getAABB(t) {
    if (this.sphere)
      this.sphere.getBoundingBox(t);
    else {
      const e = this.obb || this.regionObb;
      t.copy(e.box).applyMatrix4(e.transform);
    }
  }
  getSphere(t) {
    if (this.sphere)
      t.copy(this.sphere);
    else if (this.region)
      this.region.getBoundingSphere(t);
    else {
      const e = this.obb || this.regionObb;
      e.box.getBoundingSphere(t), t.applyMatrix4(e.transform);
    }
  }
  setObbData(t, e) {
    const i = new jt();
    Q3.set(t[3], t[4], t[5]), $3.set(t[6], t[7], t[8]), Y3.set(t[9], t[10], t[11]);
    const s = Q3.length(), o = $3.length(), r2 = Y3.length();
    Q3.normalize(), $3.normalize(), Y3.normalize(), s === 0 && Q3.crossVectors($3, Y3), o === 0 && $3.crossVectors(Q3, Y3), r2 === 0 && Y3.crossVectors(Q3, $3), i.transform.set(
      Q3.x,
      $3.x,
      Y3.x,
      t[0],
      Q3.y,
      $3.y,
      Y3.y,
      t[1],
      Q3.z,
      $3.z,
      Y3.z,
      t[2],
      0,
      0,
      0,
      1
    ).premultiply(e), i.box.min.set(-s, -o, -r2), i.box.max.set(s, o, r2), i.update(), this.obb = i;
  }
  setSphereData(t, e, i, s, o) {
    const r2 = new Sphere();
    r2.center.set(t, e, i), r2.radius = s, r2.applyMatrix4(o), this.sphere = r2;
  }
  setRegionData(t, e, i, s, o, r2, n) {
    const a = new $t(
      ...t.radius,
      i,
      o,
      e,
      s,
      r2,
      n
    ), h2 = new jt();
    a.getBoundingBox(h2.box, h2.transform), h2.update(), this.region = a, this.regionObb = h2;
  }
};
var fi = new Matrix3();
function gi(m2, t, e, i) {
  const s = fi.set(
    m2.normal.x,
    m2.normal.y,
    m2.normal.z,
    t.normal.x,
    t.normal.y,
    t.normal.z,
    e.normal.x,
    e.normal.y,
    e.normal.z
  );
  return i.set(-m2.constant, -t.constant, -e.constant), i.applyMatrix3(s.invert()), i;
}
var yi = class extends Frustum {
  constructor() {
    super(), this.points = Array(8).fill().map(() => new Vector3());
  }
  setFromProjectionMatrix(t, e) {
    return super.setFromProjectionMatrix(t, e), this.calculateFrustumPoints(), this;
  }
  calculateFrustumPoints() {
    const { planes: t, points: e } = this;
    [
      [t[0], t[3], t[4]],
      // Near top left
      [t[1], t[3], t[4]],
      // Near top right
      [t[0], t[2], t[4]],
      // Near bottom left
      [t[1], t[2], t[4]],
      // Near bottom right
      [t[0], t[3], t[5]],
      // Far top left
      [t[1], t[3], t[5]],
      // Far top right
      [t[0], t[2], t[5]],
      // Far bottom left
      [t[1], t[2], t[5]]
      // Far bottom right
    ].forEach((s, o) => {
      gi(s[0], s[1], s[2], e[o]);
    });
  }
};
var le = new Matrix4();
var he = new Euler();
var Ve = Symbol("INITIAL_FRUSTUM_CULLED");
var Et = new Matrix4();
var Mt2 = new Vector3();
var Gt2 = new Vector2();
var xi = new Vector3(1, 0, 0);
var bi = new Vector3(0, 1, 0);
function pe2(m2, t) {
  m2.traverse((e) => {
    e.frustumCulled = e[Ve] && t;
  });
}
var Vi = class extends Pe {
  get autoDisableRendererCulling() {
    return this._autoDisableRendererCulling;
  }
  set autoDisableRendererCulling(t) {
    this._autoDisableRendererCulling !== t && (super._autoDisableRendererCulling = t, this.forEachLoadedModel((e) => {
      pe2(e, !t);
    }));
  }
  get optimizeRaycast() {
    return this._optimizeRaycast;
  }
  set optimizeRaycast(t) {
    console.warn('TilesRenderer: The "optimizeRaycast" option has been deprecated.'), this._optimizeRaycast = t;
  }
  constructor(...t) {
    super(...t), this.group = new di(this), this.ellipsoid = Nt.clone(), this.cameras = [], this.cameraMap = /* @__PURE__ */ new Map(), this.cameraInfo = [], this._optimizeRaycast = true, this._upRotationMatrix = new Matrix4(), this._bytesUsed = /* @__PURE__ */ new WeakMap(), this._autoDisableRendererCulling = true, this.manager = new LoadingManager(), this._listeners = {};
  }
  addEventListener(t, e) {
    t === "load-tile-set" && (console.warn('TilesRenderer: "load-tile-set" event has been deprecated. Use "load-tileset" instead.'), t = "load-tileset"), EventDispatcher.prototype.addEventListener.call(this, t, e);
  }
  hasEventListener(t, e) {
    return t === "load-tile-set" && (console.warn('TilesRenderer: "load-tile-set" event has been deprecated. Use "load-tileset" instead.'), t = "load-tileset"), EventDispatcher.prototype.hasEventListener.call(this, t, e);
  }
  removeEventListener(t, e) {
    t === "load-tile-set" && (console.warn('TilesRenderer: "load-tile-set" event has been deprecated. Use "load-tileset" instead.'), t = "load-tileset"), EventDispatcher.prototype.removeEventListener.call(this, t, e);
  }
  dispatchEvent(t) {
    "tileset" in t && Object.defineProperty(t, "tileSet", {
      get() {
        return console.warn('TilesRenderer: "event.tileSet" has been deprecated. Use "event.tileset" instead.'), t.tileset;
      },
      enumerable: false,
      configurable: true
    }), EventDispatcher.prototype.dispatchEvent.call(this, t);
  }
  /* Public API */
  getBoundingBox(t) {
    if (!this.root)
      return false;
    const e = this.root.engineData.boundingVolume;
    return e ? (e.getAABB(t), true) : false;
  }
  getOrientedBoundingBox(t, e) {
    if (!this.root)
      return false;
    const i = this.root.engineData.boundingVolume;
    return i ? (i.getOBB(t, e), true) : false;
  }
  getBoundingSphere(t) {
    if (!this.root)
      return false;
    const e = this.root.engineData.boundingVolume;
    return e ? (e.getSphere(t), true) : false;
  }
  forEachLoadedModel(t) {
    this.traverse((e) => {
      const i = e.engineData && e.engineData.scene;
      i && t(i, e);
    }, null, false);
  }
  raycast(t, e) {
    if (this.root)
      if (t.firstHitOnly) {
        const i = Fe2(this, this.root, t);
        i && e.push(i);
      } else
        Ue(this, this.root, t, e);
  }
  hasCamera(t) {
    return this.cameraMap.has(t);
  }
  setCamera(t) {
    const e = this.cameras, i = this.cameraMap;
    return i.has(t) ? false : (i.set(t, new Vector2()), e.push(t), this.dispatchEvent({ type: "add-camera", camera: t }), true);
  }
  setResolution(t, e, i) {
    const s = this.cameraMap;
    if (!s.has(t))
      return false;
    const o = e.isVector2 ? e.x : e, r2 = e.isVector2 ? e.y : i, n = s.get(t);
    return (n.width !== o || n.height !== r2) && (n.set(o, r2), this.dispatchEvent({ type: "camera-resolution-change" })), true;
  }
  setResolutionFromRenderer(t, e) {
    return e.getSize(Gt2), this.setResolution(t, Gt2.x, Gt2.y);
  }
  deleteCamera(t) {
    const e = this.cameras, i = this.cameraMap;
    if (i.has(t)) {
      const s = e.indexOf(t);
      return e.splice(s, 1), i.delete(t), this.dispatchEvent({ type: "delete-camera", camera: t }), true;
    }
    return false;
  }
  /* Overriden */
  loadRootTileset(...t) {
    return super.loadRootTileset(...t).then((e) => {
      const { asset: i, extensions: s = {} } = e;
      switch ((i && i.gltfUpAxis || "y").toLowerCase()) {
        case "x":
          this._upRotationMatrix.makeRotationAxis(bi, -Math.PI / 2);
          break;
        case "y":
          this._upRotationMatrix.makeRotationAxis(xi, Math.PI / 2);
          break;
      }
      if ("3DTILES_ellipsoid" in s) {
        const r2 = s["3DTILES_ellipsoid"], { ellipsoid: n } = this;
        n.name = r2.body, r2.radii ? n.radius.set(...r2.radii) : n.radius.set(1, 1, 1);
      }
      return e;
    });
  }
  prepareForTraversal() {
    const t = this.group, e = this.cameras, i = this.cameraMap, s = this.cameraInfo;
    for (; s.length > e.length; )
      s.pop();
    for (; s.length < e.length; )
      s.push({
        frustum: new yi(),
        isOrthographic: false,
        sseDenominator: -1,
        // used if isOrthographic:false
        position: new Vector3(),
        invScale: -1,
        pixelSize: 0
        // used if isOrthographic:true
      });
    Mt2.setFromMatrixScale(t.matrixWorldInverse), Math.abs(Math.max(Mt2.x - Mt2.y, Mt2.x - Mt2.z)) > 1e-6 && console.warn("ThreeTilesRenderer : Non uniform scale used for tile which may cause issues when calculating screen space error.");
    for (let o = 0, r2 = s.length; o < r2; o++) {
      const n = e[o], a = s[o], h2 = a.frustum, l = a.position, c2 = i.get(n);
      (c2.width === 0 || c2.height === 0) && console.warn("TilesRenderer: resolution for camera error calculation is not set.");
      const p3 = n.projectionMatrix.elements;
      if (a.isOrthographic = p3[15] === 1, a.isOrthographic) {
        const f2 = 2 / p3[0], u2 = 2 / p3[5];
        a.pixelSize = Math.max(u2 / c2.height, f2 / c2.width);
      } else
        a.sseDenominator = 2 / p3[5] / c2.height;
      Et.copy(t.matrixWorld), Et.premultiply(n.matrixWorldInverse), Et.premultiply(n.projectionMatrix), h2.setFromProjectionMatrix(Et), l.set(0, 0, 0), l.applyMatrix4(n.matrixWorld), l.applyMatrix4(t.matrixWorldInverse);
    }
  }
  update() {
    if (super.update(), this.cameras.length === 0 && this.root) {
      let t = false;
      this.invokeAllPlugins((e) => t = t || !!(e !== this && e.calculateTileViewError)), t === false && console.warn("TilesRenderer: no cameras defined. Cannot update 3d tiles.");
    }
  }
  preprocessNode(t, e, i = null) {
    super.preprocessNode(t, e, i);
    const s = new Matrix4();
    if (t.transform) {
      const n = t.transform;
      for (let a = 0; a < 16; a++)
        s.elements[a] = n[a];
    }
    i && s.premultiply(i.engineData.transform);
    const o = new Matrix4().copy(s).invert(), r2 = new mi();
    "sphere" in t.boundingVolume && r2.setSphereData(...t.boundingVolume.sphere, s), "box" in t.boundingVolume && r2.setObbData(t.boundingVolume.box, s), "region" in t.boundingVolume && r2.setRegionData(this.ellipsoid, ...t.boundingVolume.region), t.engineData.transform = s, t.engineData.transformInverse = o, t.engineData.boundingVolume = r2, t.engineData.geometry = null, t.engineData.materials = null, t.engineData.textures = null;
  }
  async parseTile(t, e, i, s, o) {
    const r2 = e.engineData, n = c(s), a = this.fetchOptions, h2 = this.manager;
    let l = null;
    const c2 = r2.transform, p3 = this._upRotationMatrix, f2 = (d(t) || i).toLowerCase();
    switch (f2) {
      case "b3dm": {
        const g3 = new Se(h2);
        g3.workingPath = n, g3.fetchOptions = a, g3.adjustmentTransform.copy(p3), l = g3.parse(t);
        break;
      }
      case "pnts": {
        const g3 = new Ee(h2);
        g3.workingPath = n, g3.fetchOptions = a, l = g3.parse(t);
        break;
      }
      case "i3dm": {
        const g3 = new Oe(h2);
        g3.workingPath = n, g3.fetchOptions = a, g3.adjustmentTransform.copy(p3), g3.ellipsoid.copy(this.ellipsoid), l = g3.parse(t);
        break;
      }
      case "cmpt": {
        const g3 = new pi(h2);
        g3.workingPath = n, g3.fetchOptions = a, g3.adjustmentTransform.copy(p3), g3.ellipsoid.copy(this.ellipsoid), l = g3.parse(t).then((S2) => S2.scene);
        break;
      }
      case "gltf":
      case "glb": {
        const g3 = h2.getHandler("path.gltf") || h2.getHandler("path.glb") || new GLTFLoader(h2);
        g3.setWithCredentials(a.credentials === "include"), g3.setRequestHeader(a.headers || {}), a.credentials === "include" && a.mode === "cors" && g3.setCrossOrigin("use-credentials");
        let S2 = g3.resourcePath || g3.path || n;
        !/[\\/]$/.test(S2) && S2.length && (S2 += "/"), l = g3.parseAsync(t, S2).then((C4) => {
          C4.scene = C4.scene || new Group();
          const { scene: T2 } = C4;
          return T2.updateMatrix(), T2.matrix.multiply(p3).decompose(T2.position, T2.quaternion, T2.scale), C4;
        });
        break;
      }
      default: {
        l = this.invokeOnePlugin((g3) => g3.parseToMesh && g3.parseToMesh(t, e, i, s, o));
        break;
      }
    }
    const u2 = await l;
    if (u2 === null)
      throw new Error(`TilesRenderer: Content type "${f2}" not supported.`);
    let d3, x5;
    u2.isObject3D ? (d3 = u2, x5 = null) : (d3 = u2.scene, x5 = u2), d3.updateMatrix(), d3.matrix.premultiply(c2), d3.matrix.decompose(d3.position, d3.quaternion, d3.scale), await this.invokeAllPlugins((g3) => g3.processTileModel && g3.processTileModel(d3, e)), d3.traverse((g3) => {
      g3[Ve] = g3.frustumCulled;
    }), pe2(d3, !this.autoDisableRendererCulling);
    const E2 = [], _4 = [], F3 = [];
    if (d3.traverse((g3) => {
      if (g3.geometry && _4.push(g3.geometry), g3.material) {
        const S2 = g3.material;
        E2.push(g3.material);
        for (const C4 in S2) {
          const T2 = S2[C4];
          T2 && T2.isTexture && F3.push(T2);
        }
      }
    }), o.aborted) {
      for (let g3 = 0, S2 = F3.length; g3 < S2; g3++) {
        const C4 = F3[g3];
        C4.image instanceof ImageBitmap && C4.image.close(), C4.dispose();
      }
      return;
    }
    r2.materials = E2, r2.geometry = _4, r2.textures = F3, r2.scene = d3, r2.metadata = x5;
  }
  disposeTile(t) {
    super.disposeTile(t);
    const e = t.engineData;
    if (e.scene) {
      const i = e.materials, s = e.geometry, o = e.textures, r2 = e.scene.parent;
      e.scene.traverse((n) => {
        n.userData.meshFeatures && n.userData.meshFeatures.dispose(), n.userData.structuralMetadata && n.userData.structuralMetadata.dispose();
      });
      for (let n = 0, a = s.length; n < a; n++)
        s[n].dispose();
      for (let n = 0, a = i.length; n < a; n++)
        i[n].dispose();
      for (let n = 0, a = o.length; n < a; n++) {
        const h2 = o[n];
        h2.image instanceof ImageBitmap && h2.image.close(), h2.dispose();
      }
      r2 && r2.remove(e.scene), e.scene = null, e.materials = null, e.textures = null, e.geometry = null, e.metadata = null;
    }
  }
  setTileVisible(t, e) {
    const i = t.engineData.scene, s = this.group;
    e ? i && (s.add(i), i.updateMatrixWorld(true)) : i && s.remove(i), super.setTileVisible(t, e);
  }
  calculateBytesUsed(t, e) {
    const i = this._bytesUsed;
    return !i.has(t) && e && i.set(t, Dt(e)), i.get(t) ?? null;
  }
  calculateTileViewError(t, e) {
    const i = t.engineData, s = this.cameras, o = this.cameraInfo, r2 = i.boundingVolume;
    let n = false, a = 0, h2 = 1 / 0, l = 0, c2 = 1 / 0;
    for (let p3 = 0, f2 = s.length; p3 < f2; p3++) {
      const u2 = o[p3];
      let d3, x5;
      if (u2.isOrthographic) {
        const _4 = u2.pixelSize;
        d3 = t.geometricError / _4, x5 = 1 / 0;
      } else {
        const _4 = u2.sseDenominator;
        x5 = r2.distanceToPoint(u2.position), d3 = x5 === 0 ? 1 / 0 : t.geometricError / (x5 * _4);
      }
      const E2 = o[p3].frustum;
      r2.intersectsFrustum(E2) && (n = true, a = Math.max(a, d3), h2 = Math.min(h2, x5)), l = Math.max(l, d3), c2 = Math.min(c2, x5);
    }
    n ? (e.inView = true, e.error = a, e.distanceFromCamera = h2) : (e.inView = false, e.error = l, e.distanceFromCamera = c2);
  }
  // adjust the rotation of the group such that Y is altitude, X is North, and Z is East
  setLatLonToYUp(t, e) {
    console.warn("TilesRenderer: setLatLonToYUp is deprecated. Use the ReorientationPlugin, instead.");
    const { ellipsoid: i, group: s } = this;
    he.set(Math.PI / 2, Math.PI / 2, 0), le.makeRotationFromEuler(he), i.getEastNorthUpFrame(t, e, 0, s.matrix).multiply(le).invert().decompose(
      s.position,
      s.quaternion,
      s.scale
    ), s.updateMatrixWorld(true);
  }
  dispose() {
    super.dispose(), this.group.removeFromParent();
  }
};
var de = new Vector2();
var ue = new Vector2();
var Ot = new Matrix4();
var tt = new Matrix4();
var It = new Matrix4();
var W3 = new Vector3();
var M4 = new Vector3();
var Rt2 = new Vector3();
var zt = new Vector3();
var z4 = new Vector3();
var B3 = new Vector3();
var $t2 = new Vector3();
var At2 = new Vector3();
var G5 = new Quaternion();
var me2 = new Plane();
var O3 = new Vector3();
var Ft = new Vector3();
var Yt = new Vector3();
var vi = new Quaternion();
var D2 = new Ray();
var Ut2 = new Vector2();
var L2 = new Vector2();
var fe2 = new Vector2();
var _t2 = new Vector2();
var Xt = new Vector2();
var ge2 = new Vector2();
var Me = new Matrix4();
var pt2 = new Matrix4();
var N3 = new Vector3();
var P3 = new Vector3();
var K2 = new Vector3();
var k2 = new Vector3();
var Di = new Vector3();
var dt = new Vector3();
var J4 = new Quaternion();
var _e = new Vector3();
var ot = new Vector3();
var w3 = new Ray();
var Pe2 = new Mt();
var Vt = new Vector2();
var U2 = new Vector3();
var nt2 = new Vector3();
var rt2 = new OrthographicCamera();
var Ci = new Vector3();
var Si = new Vector3();
var Ei = new Vector3();
var Te2 = new Quaternion();
var Oi = new Quaternion();

// node_modules/@giro3d/giro3d/node_modules/3d-tiles-renderer/build/index.core-plugins.js
function p2(u2) {
  return u2.implicitTilingData.root.implicitTiling.subdivisionScheme === "OCTREE";
}
function m(u2) {
  return p2(u2) ? 8 : 4;
}
function y3(u2, i) {
  if (!u2)
    return [0, 0, 0];
  const e = u2.implicitTilingData.x, t = u2.implicitTilingData.y, r2 = u2.implicitTilingData.z, n = 2 * e + i % 2, s = 2 * t + Math.floor(i / 2) % 2, l = p2(u2) ? 2 * r2 + Math.floor(i / 4) % 2 : 0;
  return [n, s, l];
}
var b3 = class {
  constructor(i, e) {
    this.parent = i, this.children = [], this.geometricError = 0, this.boundingVolume = null;
    const [t, r2, n] = y3(i, e);
    this.implicitTilingData = {
      level: i.implicitTilingData.level + 1,
      root: i.implicitTilingData.root,
      subtreeIdx: e,
      x: t,
      y: r2,
      z: n
    };
  }
  static clone(i) {
    return {
      parent: i.parent,
      children: [],
      geometricError: i.geometricError,
      boundingVolume: i.boundingVolume,
      implicitTilingData: {
        ...i.implicitTilingData
      }
    };
  }
};
var A2 = class extends y {
  constructor(i) {
    super(), this.tile = i, this.rootTile = i.implicitTilingData.root, this.workingPath = null;
  }
  /**
   * A helper object for storing the two parts of the subtree binary
   *
   * @typedef {object} Subtree
   * @property {number} version
   * @property {JSON} subtreeJson
   * @property {ArrayBuffer} subtreeByte
   * @private
   */
  /**
   *
   * @param buffer
   * @return {Subtree}
   */
  parseBuffer(i) {
    const e = new DataView(i);
    let t = 0;
    const r2 = d(e);
    console.assert(r2 === "subt", 'SUBTREELoader: The magic bytes equal "subt".'), t += 4;
    const n = e.getUint32(t, true);
    console.assert(n === 1, 'SUBTREELoader: The version listed in the header is "1".'), t += 4;
    const s = e.getUint32(t, true);
    t += 8;
    const l = e.getUint32(t, true);
    t += 8;
    const o = JSON.parse(f(new Uint8Array(i, t, s)));
    t += s;
    const a = i.slice(t, t + l);
    return {
      version: n,
      subtreeJson: o,
      subtreeByte: a
    };
  }
  async parse(i) {
    const e = this.parseBuffer(i), t = e.subtreeJson;
    t.contentAvailabilityHeaders = [].concat(t.contentAvailability);
    const r2 = this.preprocessBuffers(t.buffers), n = this.preprocessBufferViews(
      t.bufferViews,
      r2
    );
    this.markActiveBufferViews(t, n);
    const s = await this.requestActiveBuffers(
      r2,
      e.subtreeByte
    ), l = this.parseActiveBufferViews(n, s);
    this.parseAvailability(e, t, l), this.expandSubtree(this.tile, e);
  }
  /**
   * Determine which buffer views need to be loaded into memory. This includes:
   *
   * <ul>
   * <li>The tile availability bitstream (if a bitstream is defined)</li>
   * <li>The content availability bitstream(s) (if a bitstream is defined)</li>
   * <li>The child subtree availability bitstream (if a bitstream is defined)</li>
   * </ul>
   *
   * <p>
   * This function modifies the buffer view headers' isActive flags in place.
   * </p>
   *
   * @param {JSON} subtreeJson The JSON chunk from the subtree
   * @param {BufferViewHeader[]} bufferViewHeaders The preprocessed buffer view headers
   * @private
   */
  markActiveBufferViews(i, e) {
    let t;
    const r2 = i.tileAvailability;
    isNaN(r2.bitstream) ? isNaN(r2.bufferView) || (t = e[r2.bufferView]) : t = e[r2.bitstream], t && (t.isActive = true, t.bufferHeader.isActive = true);
    const n = i.contentAvailabilityHeaders;
    for (let l = 0; l < n.length; l++)
      t = void 0, isNaN(n[l].bitstream) ? isNaN(n[l].bufferView) || (t = e[n[l].bufferView]) : t = e[n[l].bitstream], t && (t.isActive = true, t.bufferHeader.isActive = true);
    t = void 0;
    const s = i.childSubtreeAvailability;
    isNaN(s.bitstream) ? isNaN(s.bufferView) || (t = e[s.bufferView]) : t = e[s.bitstream], t && (t.isActive = true, t.bufferHeader.isActive = true);
  }
  /**
   * Go through the list of buffers and gather all the active ones into
   * a dictionary.
   * <p>
   * The results are put into a dictionary object. The keys are indices of
   * buffers, and the values are Uint8Arrays of the contents. Only buffers
   * marked with the isActive flag are fetched.
   * </p>
   * <p>
   * The internal buffer (the subtree's binary chunk) is also stored in this
   * dictionary if it is marked active.
   * </p>
   * @param {BufferHeader[]} bufferHeaders The preprocessed buffer headers
   * @param {ArrayBuffer} internalBuffer The binary chunk of the subtree file
   * @returns {object} buffersU8 A dictionary of buffer index to a Uint8Array of its contents.
   * @private
   */
  async requestActiveBuffers(i, e) {
    const t = [];
    for (let s = 0; s < i.length; s++) {
      const l = i[s];
      if (!l.isActive)
        t.push(Promise.resolve());
      else if (l.isExternal) {
        const o = this.parseImplicitURIBuffer(
          this.tile,
          this.rootTile.implicitTiling.subtrees.uri,
          l.uri
        ), a = fetch(o, this.fetchOptions).then((c2) => {
          if (!c2.ok)
            throw new Error(`SUBTREELoader: Failed to load external buffer from ${l.uri} with error code ${c2.status}.`);
          return c2.arrayBuffer();
        }).then((c2) => new Uint8Array(c2));
        t.push(a);
      } else
        t.push(Promise.resolve(new Uint8Array(e)));
    }
    const r2 = await Promise.all(t), n = {};
    for (let s = 0; s < r2.length; s++) {
      const l = r2[s];
      l && (n[s] = l);
    }
    return n;
  }
  /**
   * Go through the list of buffer views, and if they are marked as active,
   * extract a subarray from one of the active buffers.
   *
   * @param {BufferViewHeader[]} bufferViewHeaders
   * @param {object} buffersU8 A dictionary of buffer index to a Uint8Array of its contents.
   * @returns {object} A dictionary of buffer view index to a Uint8Array of its contents.
   * @private
   */
  parseActiveBufferViews(i, e) {
    const t = {};
    for (let r2 = 0; r2 < i.length; r2++) {
      const n = i[r2];
      if (!n.isActive)
        continue;
      const s = n.byteOffset, l = s + n.byteLength, o = e[n.buffer];
      t[r2] = o.slice(s, l);
    }
    return t;
  }
  /**
   * A buffer header is the JSON header from the subtree JSON chunk plus
   * a couple extra boolean flags for easy reference.
   *
   * Buffers are assumed inactive until explicitly marked active. This is used
   * to avoid fetching unneeded buffers.
   *
   * @typedef {object} BufferHeader
   * @property {boolean} isActive Whether this buffer is currently used.
   * @property {string} [uri] The URI of the buffer (external buffers only)
   * @property {number} byteLength The byte length of the buffer, including any padding contained within.
   * @private
   */
  /**
   * Iterate over the list of buffers from the subtree JSON and add the isActive field for easier parsing later.
   * This modifies the objects in place.
   * @param {Object[]} [bufferHeaders=[]] The JSON from subtreeJson.buffers.
   * @returns {BufferHeader[]} The same array of headers with additional fields.
   * @private
   */
  preprocessBuffers(i = []) {
    for (let e = 0; e < i.length; e++) {
      const t = i[e];
      t.isActive = false, t.isExternal = !!t.uri;
    }
    return i;
  }
  /**
   * A buffer view header is the JSON header from the subtree JSON chunk plus
   * the isActive flag and a reference to the header for the underlying buffer.
   *
   * @typedef {object} BufferViewHeader
   * @property {BufferHeader} bufferHeader A reference to the header for the underlying buffer
   * @property {boolean} isActive Whether this bufferView is currently used.
   * @property {number} buffer The index of the underlying buffer.
   * @property {number} byteOffset The start byte of the bufferView within the buffer.
   * @property {number} byteLength The length of the bufferView. No padding is included in this length.
   * @private
   */
  /**
   * Iterate the list of buffer views from the subtree JSON and add the
   * isActive flag. Also save a reference to the bufferHeader.
   *
   * @param {Object[]} [bufferViewHeaders=[]] The JSON from subtree.bufferViews.
   * @param {BufferHeader[]} bufferHeaders The preprocessed buffer headers.
   * @returns {BufferViewHeader[]} The same array of bufferView headers with additional fields.
   * @private
   */
  preprocessBufferViews(i = [], e) {
    for (let t = 0; t < i.length; t++) {
      const r2 = i[t];
      r2.bufferHeader = e[r2.buffer], r2.isActive = false, r2.isExternal = r2.bufferHeader.isExternal;
    }
    return i;
  }
  /**
   * Parse the three availability bitstreams and store them in the subtree.
   *
   * @param {Subtree} subtree The subtree to modify.
   * @param {Object} subtreeJson The subtree JSON.
   * @param {Object} bufferViewsU8 A dictionary of buffer view index to a Uint8Array of its contents.
   * @private
   */
  parseAvailability(i, e, t) {
    const r2 = m(this.rootTile), n = this.rootTile.implicitTiling.subtreeLevels, s = (Math.pow(r2, n) - 1) / (r2 - 1), l = Math.pow(r2, n);
    i._tileAvailability = this.parseAvailabilityBitstream(
      e.tileAvailability,
      t,
      s
    ), i._contentAvailabilityBitstreams = [];
    for (let o = 0; o < e.contentAvailabilityHeaders.length; o++) {
      const a = this.parseAvailabilityBitstream(
        e.contentAvailabilityHeaders[o],
        t,
        // content availability has the same length as tile availability.
        s
      );
      i._contentAvailabilityBitstreams.push(a);
    }
    i._childSubtreeAvailability = this.parseAvailabilityBitstream(
      e.childSubtreeAvailability,
      t,
      l
    );
  }
  /**
   * Given the JSON describing an availability bitstream, turn it into an
   * in-memory representation using an object. This handles bitstreams from a bufferView.
   *
   * @param {Object} availabilityJson A JSON object representing the availability.
   * @param {Object} bufferViewsU8 A dictionary of buffer view index to its Uint8Array contents.
   * @param {number} lengthBits The length of the availability bitstream in bits.
   * @returns {object}
   * @private
   */
  parseAvailabilityBitstream(i, e, t) {
    if (!isNaN(i.constant))
      return {
        constant: !!i.constant,
        lengthBits: t
      };
    let r2;
    return isNaN(i.bitstream) ? isNaN(i.bufferView) || (r2 = e[i.bufferView]) : r2 = e[i.bitstream], {
      bitstream: r2,
      lengthBits: t
    };
  }
  /**
   * Expand a single subtree tile. This transcodes the subtree into
   * a tree of {@link SubtreeTile}. The root of this tree is stored in
   * the placeholder tile's children array. This method also creates
   * tiles for the child subtrees to be lazily expanded as needed.
   *
   * @param {Object | SubtreeTile} subtreeRoot The first node of the subtree.
   * @param {Subtree} subtree The parsed subtree.
   * @private
   */
  expandSubtree(i, e) {
    const t = b3.clone(i);
    for (let s = 0; e && s < e._contentAvailabilityBitstreams.length; s++)
      if (e && this.getBit(e._contentAvailabilityBitstreams[s], 0)) {
        t.content = { uri: this.parseImplicitURI(i, this.rootTile.content.uri) };
        break;
      }
    i.children.push(t);
    const r2 = this.transcodeSubtreeTiles(
      t,
      e
    ), n = this.listChildSubtrees(e, r2);
    for (let s = 0; s < n.length; s++) {
      const l = n[s], o = l.tile, a = this.deriveChildTile(
        null,
        o,
        null,
        l.childMortonIndex
      );
      a.content = { uri: this.parseImplicitURI(a, this.rootTile.implicitTiling.subtrees.uri) }, o.children.push(a);
    }
  }
  /**
   * Transcode the implicitly defined tiles within this subtree and generate
   * explicit {@link SubtreeTile} objects. This function only transcodes tiles,
   * child subtrees are handled separately.
   *
   * @param {Object | SubtreeTile} subtreeRoot The root of the current subtree.
   * @param {Subtree} subtree The subtree to get availability information.
   * @returns {Array} The bottom row of transcoded tiles. This is helpful for processing child subtrees.
   * @private
   */
  transcodeSubtreeTiles(i, e) {
    let t = [i], r2 = [];
    for (let n = 1; n < this.rootTile.implicitTiling.subtreeLevels; n++) {
      const s = m(this.rootTile), l = (Math.pow(s, n) - 1) / (s - 1), o = s * t.length;
      for (let a = 0; a < o; a++) {
        const c2 = l + a, h2 = a >> Math.log2(s), f2 = t[h2];
        if (!this.getBit(e._tileAvailability, c2)) {
          r2.push(void 0);
          continue;
        }
        const g3 = this.deriveChildTile(
          e,
          f2,
          c2,
          a
        );
        f2.children.push(g3), r2.push(g3);
      }
      t = r2, r2 = [];
    }
    return t;
  }
  /**
   * Given a parent tile and information about which child to create, derive
   * the properties of the child tile implicitly.
   * <p>
   * This creates a real tile for rendering.
   * </p>
   *
   * @param {Subtree} subtree The subtree the child tile belongs to.
   * @param {Object | SubtreeTile} parentTile The parent of the new child tile.
   * @param {number} childBitIndex The index of the child tile within the tile's availability information.
   * @param {number} childMortonIndex The morton index of the child tile relative to its parent.
   * @returns {SubtreeTile} The new child tile.
   * @private
   */
  deriveChildTile(i, e, t, r2) {
    const n = new b3(e, r2);
    n.boundingVolume = this.getTileBoundingVolume(n), n.geometricError = this.getGeometricError(n);
    for (let s = 0; i && s < i._contentAvailabilityBitstreams.length; s++)
      if (i && this.getBit(i._contentAvailabilityBitstreams[s], t)) {
        n.content = { uri: this.parseImplicitURI(n, this.rootTile.content.uri) };
        break;
      }
    return n;
  }
  /**
   * Get a bit from the bitstream as a Boolean. If the bitstream
   * is a constant, the constant value is returned instead.
   *
   * @param {ParsedBitstream} object
   * @param {number} index The integer index of the bit.
   * @returns {boolean} The value of the bit.
   * @private
   */
  getBit(i, e) {
    if (e < 0 || e >= i.lengthBits)
      throw new Error("Bit index out of bounds.");
    if (i.constant !== void 0)
      return i.constant;
    const t = e >> 3, r2 = e % 8;
    return (new Uint8Array(i.bitstream)[t] >> r2 & 1) === 1;
  }
  /**
   * //TODO Adapt for Sphere
   * To maintain numerical stability during this subdivision process,
   * the actual bounding volumes should not be computed progressively by subdividing a non-root tile volume.
   * Instead, the exact bounding volumes are computed directly for a given level.
   * @param {Object | SubtreeTile} tile
   * @return {Object} object containing the bounding volume.
   */
  getTileBoundingVolume(i) {
    const e = {};
    if (this.rootTile.boundingVolume.region) {
      const t = [...this.rootTile.boundingVolume.region], r2 = t[0], n = t[2], s = t[1], l = t[3], o = (n - r2) / Math.pow(2, i.implicitTilingData.level), a = (l - s) / Math.pow(2, i.implicitTilingData.level);
      t[0] = r2 + o * i.implicitTilingData.x, t[2] = r2 + o * (i.implicitTilingData.x + 1), t[1] = s + a * i.implicitTilingData.y, t[3] = s + a * (i.implicitTilingData.y + 1);
      for (let c2 = 0; c2 < 4; c2++) {
        const h2 = t[c2];
        h2 < -Math.PI ? t[c2] += 2 * Math.PI : h2 > Math.PI && (t[c2] -= 2 * Math.PI);
      }
      if (p2(i)) {
        const c2 = t[4], f2 = (t[5] - c2) / Math.pow(2, i.implicitTilingData.level);
        t[4] = c2 + f2 * i.implicitTilingData.z, t[5] = c2 + f2 * (i.implicitTilingData.z + 1);
      }
      e.region = t;
    }
    if (this.rootTile.boundingVolume.box) {
      const t = [...this.rootTile.boundingVolume.box], r2 = 2 ** i.implicitTilingData.level - 1, n = Math.pow(2, -i.implicitTilingData.level), s = p2(i) ? 3 : 2;
      for (let l = 0; l < s; l++) {
        t[3 + l * 3 + 0] *= n, t[3 + l * 3 + 1] *= n, t[3 + l * 3 + 2] *= n;
        const o = t[3 + l * 3 + 0], a = t[3 + l * 3 + 1], c2 = t[3 + l * 3 + 2], h2 = l === 0 ? i.implicitTilingData.x : l === 1 ? i.implicitTilingData.y : i.implicitTilingData.z;
        t[0] += 2 * o * (-0.5 * r2 + h2), t[1] += 2 * a * (-0.5 * r2 + h2), t[2] += 2 * c2 * (-0.5 * r2 + h2);
      }
      e.box = t;
    }
    return e;
  }
  /**
   * Each child’s geometricError is half of its parent’s geometricError.
   * @param {Object | SubtreeTile} tile
   * @return {number}
   */
  getGeometricError(i) {
    return this.rootTile.geometricError / Math.pow(2, i.implicitTilingData.level);
  }
  /**
   * Determine what child subtrees exist and return a list of information.
   *
   * @param {Object} subtree The subtree for looking up availability.
   * @param {Array} bottomRow The bottom row of tiles in a transcoded subtree.
   * @returns {[]} A list of identifiers for the child subtrees.
   * @private
   */
  listChildSubtrees(i, e) {
    const t = [], r2 = m(this.rootTile);
    for (let n = 0; n < e.length; n++) {
      const s = e[n];
      if (s !== void 0)
        for (let l = 0; l < r2; l++) {
          const o = n * r2 + l;
          this.getBit(i._childSubtreeAvailability, o) && t.push({
            tile: s,
            childMortonIndex: o
          });
        }
    }
    return t;
  }
  /**
   * Replaces placeholder tokens in a URI template with the corresponding tile properties.
   *
   * The URI template should contain the tokens:
   * - `{level}` for the tile's subdivision level.
   * - `{x}` for the tile's x-coordinate.
   * - `{y}` for the tile's y-coordinate.
   * - `{z}` for the tile's z-coordinate.
   *
   * @param {Object} tile - The tile object containing properties __level, __x, __y, and __z.
   * @param {string} uri - The URI template string with placeholders.
   * @returns {string} The URI with placeholders replaced by the tile's properties.
   */
  parseImplicitURI(i, e) {
    return e = e.replace("{level}", i.implicitTilingData.level), e = e.replace("{x}", i.implicitTilingData.x), e = e.replace("{y}", i.implicitTilingData.y), e = e.replace("{z}", i.implicitTilingData.z), e;
  }
  /**
   * Generates the full external buffer URI for a tile by combining an implicit URI with a buffer URI.
   *
   * First, it parses the implicit URI using the tile properties and the provided template. Then, it creates a new URL
   * relative to the tile's base path, removes the last path segment, and appends the buffer URI.
   *
   * @param {Object} tile - The tile object that contains properties:
   *   - __level: the subdivision level,
   *   - __x, __y, __z: the tile coordinates,
   * @param {string} uri - The URI template string with placeholders for the tile (e.g., `{level}`, `{x}`, `{y}`, `{z}`).
   * @param {string} bufUri - The buffer file name to append (e.g., "0_1.bin").
   * @returns {string} The full external buffer URI.
   */
  parseImplicitURIBuffer(i, e, t) {
    const r2 = this.parseImplicitURI(i, e), n = new URL(r2, this.workingPath + "/");
    return n.pathname = n.pathname.substring(0, n.pathname.lastIndexOf("/")), new URL(n.pathname + "/" + t, this.workingPath + "/").toString();
  }
};
var w4 = class {
  constructor() {
    this.name = "IMPLICIT_TILING_PLUGIN";
  }
  init(i) {
    this.tiles = i;
  }
  preprocessNode(i, e, t) {
    var r2;
    i.implicitTiling ? (i.internal.hasUnrenderableContent = true, i.internal.hasRenderableContent = false, i.implicitTilingData = {
      // Keep this tile as an Implicit Root Tile
      root: i,
      // Idx of the tile in its subtree
      subtreeIdx: 0,
      // Coords of the tile
      x: 0,
      y: 0,
      z: 0,
      level: 0
    }) : /.subtree$/i.test((r2 = i.content) == null ? void 0 : r2.uri) && (i.internal.hasUnrenderableContent = true, i.internal.hasRenderableContent = false);
  }
  parseTile(i, e, t) {
    if (/^subtree$/i.test(t)) {
      const r2 = new A2(e);
      return r2.workingPath = e.internal.basePath, r2.fetchOptions = this.tiles.fetchOptions, r2.parse(i);
    }
  }
  preprocessURL(i, e) {
    if (e && e.implicitTiling) {
      const t = e.implicitTiling.subtrees.uri.replace("{level}", e.implicitTilingData.level).replace("{x}", e.implicitTilingData.x).replace("{y}", e.implicitTilingData.y).replace("{z}", e.implicitTilingData.z);
      return new URL(t, e.internal.basePath + "/").toString();
    }
    return i;
  }
  disposeTile(i) {
    var e;
    /.subtree$/i.test((e = i.content) == null ? void 0 : e.uri) && (i.children.forEach((t) => {
      this.tiles.processNodeQueue.remove(t);
    }), i.children.length = 0);
  }
};

// node_modules/@giro3d/giro3d/node_modules/3d-tiles-renderer/build/WMSCapabilitiesLoader-BJRXfwtc.js
var fe3 = new Vector2();
var ye2 = Symbol("TILE_X");
var xe = Symbol("TILE_Y");
var ae3 = Symbol("TILE_LEVEL");
var pt3 = new Vector3();
var Ge = new Vector3();
var ft2 = new Vector3();
var ss = new Vector3();
var ie3 = new Vector2();
var mt = new Sphere();
var re3 = class {
  get isMercator() {
    return this.scheme === "EPSG:3857";
  }
  constructor(e = "EPSG:4326") {
    this.scheme = e, this.tileCountX = 1, this.tileCountY = 1, this.setScheme(e);
  }
  setScheme(e) {
    switch (this.scheme = e, e) {
      case "CRS:84":
      case "EPSG:4326":
        this.tileCountX = 2, this.tileCountY = 1;
        break;
      case "EPSG:3857":
        this.tileCountX = 1, this.tileCountY = 1;
        break;
      case "none":
        this.tileCountX = 1, this.tileCountY = 1;
        break;
      default:
        throw new Error(`ProjectionScheme: Unknown projection scheme "${e}"`);
    }
  }
  convertNormalizedToLatitude(e) {
    if (this.scheme === "none")
      return e;
    if (this.isMercator) {
      const t = MathUtils.mapLinear(e, 0, 1, -1, 1);
      return 2 * Math.atan(Math.exp(t * Math.PI)) - Math.PI / 2;
    } else
      return MathUtils.mapLinear(e, 0, 1, -Math.PI / 2, Math.PI / 2);
  }
  convertNormalizedToLongitude(e) {
    return this.scheme === "none" ? e : MathUtils.mapLinear(e, 0, 1, -Math.PI, Math.PI);
  }
  convertLatitudeToNormalized(e) {
    if (this.scheme === "none")
      return e;
    if (this.isMercator) {
      const t = Math.log(Math.tan(Math.PI / 4 + e / 2));
      return 1 / 2 + 1 * t / (2 * Math.PI);
    } else
      return MathUtils.mapLinear(e, -Math.PI / 2, Math.PI / 2, 0, 1);
  }
  convertLongitudeToNormalized(e) {
    return this.scheme === "none" ? e : (e + Math.PI) / (2 * Math.PI);
  }
  getLongitudeDerivativeAtNormalized(e) {
    return this.scheme === "none" ? 1 : 2 * Math.PI;
  }
  getLatitudeDerivativeAtNormalized(e) {
    if (this.scheme === "none")
      return 1;
    {
      let s = e - 1e-5;
      return s < 0 && (s = e + 1e-5), this.isMercator ? Math.abs(this.convertNormalizedToLatitude(e) - this.convertNormalizedToLatitude(s)) / 1e-5 : Math.PI;
    }
  }
  getBounds() {
    return this.scheme === "none" ? [0, 0, 1, 1] : [
      this.convertNormalizedToLongitude(0),
      this.convertNormalizedToLatitude(0),
      this.convertNormalizedToLongitude(1),
      this.convertNormalizedToLatitude(1)
    ];
  }
  toNormalizedPoint(e, t) {
    const s = [e, t];
    return s[0] = this.convertLongitudeToNormalized(s[0]), s[1] = this.convertLatitudeToNormalized(s[1]), s;
  }
  toNormalizedRange(e) {
    return [
      ...this.toNormalizedPoint(e[0], e[1]),
      ...this.toNormalizedPoint(e[2], e[3])
    ];
  }
  toCartographicPoint(e, t) {
    const s = [e, t];
    return s[0] = this.convertNormalizedToLongitude(s[0]), s[1] = this.convertNormalizedToLatitude(s[1]), s;
  }
  toCartographicRange(e) {
    return [
      ...this.toCartographicPoint(e[0], e[1]),
      ...this.toCartographicPoint(e[2], e[3])
    ];
  }
  clampToBounds(e, t = false) {
    const s = [...e];
    let n;
    t ? n = [0, 0, 1, 1] : n = this.getBounds();
    const [i, r2, o, l] = n;
    return s[0] = MathUtils.clamp(s[0], i, o), s[1] = MathUtils.clamp(s[1], r2, l), s[2] = MathUtils.clamp(s[2], i, o), s[3] = MathUtils.clamp(s[3], r2, l), s;
  }
};
var is = new Vector3();
var ze2 = new Triangle();
var U3 = new Vector3();
var oe2 = new Vector3();
var w5 = new Vector4();
var rs = new Vector4();
var os = new Vector4();
var as = new Vector4();
var vi2 = new Vector3();
var gt2 = new Vector3();
var yt = new Vector3();
var Ei2 = new Vector3();
var wi = new Vector3();
var Y4 = new Vector3();
var be2 = new Vector3();
var j4 = new Vector2();
var le2 = new Vector2();
var us = new Vector2();
var ds = Symbol("TILE_X");
var ps = Symbol("TILE_Y");
var Pe3 = Symbol("TILE_LEVEL");
var me3 = Symbol("TILE_AVAILABLE");
var fs = new Vector3();
var Tt = new Matrix4();
var ms = new Vector3();
function q3(a, e, t) {
  return a && e in a ? a[e] : t;
}
function Tn(a) {
  return a !== "BOOLEAN" && a !== "STRING" && a !== "ENUM";
}
function Ni(a) {
  return /^FLOAT/.test(a);
}
function Ve2(a) {
  return /^VEC/.test(a);
}
function Fe3(a) {
  return /^MAT/.test(a);
}
function bn(a, e, t, s = null) {
  return Fe3(t) || Ve2(t) ? s.fromArray(a, e) : a[e];
}
function Nt2(a) {
  const { type: e, componentType: t } = a;
  switch (e) {
    case "SCALAR":
      return t === "INT64" ? 0n : 0;
    case "VEC2":
      return new Vector2();
    case "VEC3":
      return new Vector3();
    case "VEC4":
      return new Vector4();
    case "MAT2":
      return new Matrix2();
    case "MAT3":
      return new Matrix3();
    case "MAT4":
      return new Matrix4();
    case "BOOLEAN":
      return false;
    case "STRING":
      return "";
    case "ENUM":
      return 0;
  }
}
function gs(a, e) {
  if (e == null)
    return false;
  switch (a) {
    case "SCALAR":
      return typeof e == "number" || typeof e == "bigint";
    case "VEC2":
      return e.isVector2;
    case "VEC3":
      return e.isVector3;
    case "VEC4":
      return e.isVector4;
    case "MAT2":
      return e.isMatrix2;
    case "MAT3":
      return e.isMatrix3;
    case "MAT4":
      return e.isMatrix4;
    case "BOOLEAN":
      return typeof e == "boolean";
    case "STRING":
      return typeof e == "string";
    case "ENUM":
      return typeof e == "number" || typeof e == "bigint";
  }
  throw new Error("ClassProperty: invalid type.");
}
function De(a, e = null) {
  switch (a) {
    case "INT8":
      return Int8Array;
    case "INT16":
      return Int16Array;
    case "INT32":
      return Int32Array;
    case "INT64":
      return BigInt64Array;
    case "UINT8":
      return Uint8Array;
    case "UINT16":
      return Uint16Array;
    case "UINT32":
      return Uint32Array;
    case "UINT64":
      return BigUint64Array;
    case "FLOAT32":
      return Float32Array;
    case "FLOAT64":
      return Float64Array;
  }
  switch (e) {
    case "BOOLEAN":
      return Uint8Array;
    case "STRING":
      return Uint8Array;
  }
  throw new Error("ClassProperty: invalid type.");
}
function Vi2(a, e = null) {
  if (a.array) {
    e = e && Array.isArray(e) ? e : [], e.length = a.count;
    for (let s = 0, n = e.length; s < n; s++)
      e[s] = st2(a, e[s]);
  } else
    e = st2(a, e);
  return e;
}
function st2(a, e = null) {
  const t = a.default, s = a.type;
  if (e = e || Nt2(a), t === null) {
    switch (s) {
      case "SCALAR":
        return 0;
      case "VEC2":
        return e.set(0, 0);
      case "VEC3":
        return e.set(0, 0, 0);
      case "VEC4":
        return e.set(0, 0, 0, 0);
      case "MAT2":
        return e.identity();
      case "MAT3":
        return e.identity();
      case "MAT4":
        return e.identity();
      case "BOOLEAN":
        return false;
      case "STRING":
        return "";
      case "ENUM":
        return "";
    }
    throw new Error("ClassProperty: invalid type.");
  } else if (Fe3(s))
    e.fromArray(t);
  else if (Ve2(s))
    e.fromArray(t);
  else
    return t;
}
function Fi(a, e) {
  if (a.noData === null)
    return e;
  const t = a.noData, s = a.type;
  if (Array.isArray(e))
    for (let r2 = 0, o = e.length; r2 < o; r2++)
      e[r2] = n(e[r2]);
  else
    e = n(e);
  return e;
  function n(r2) {
    return i(r2) && (r2 = st2(a, r2)), r2;
  }
  function i(r2) {
    if (Fe3(s)) {
      const o = r2.elements;
      for (let l = 0, c2 = t.length; l < c2; l++)
        if (t[l] !== o[l])
          return false;
      return true;
    } else if (Ve2(s)) {
      for (let o = 0, l = t.length; o < l; o++)
        if (t[o] !== r2.getComponent(o))
          return false;
      return true;
    } else
      return t === r2;
  }
}
function ki(a, e) {
  switch (a) {
    case "INT8":
      return Math.max(e / 127, -1);
    case "INT16":
      return Math.max(e, 32767, -1);
    case "INT32":
      return Math.max(e / 2147483647, -1);
    case "INT64":
      return Math.max(Number(e) / 9223372036854776e3, -1);
    case "UINT8":
      return e / 255;
    case "UINT16":
      return e / 65535;
    case "UINT32":
      return e / 4294967295;
    case "UINT64":
      return Number(e) / 18446744073709552e3;
  }
}
function Gi(a, e) {
  const {
    type: t,
    componentType: s,
    scale: n,
    offset: i,
    normalized: r2
  } = a;
  if (Array.isArray(e))
    for (let h2 = 0, d3 = e.length; h2 < d3; h2++)
      e[h2] = o(e[h2]);
  else
    e = o(e);
  return e;
  function o(h2) {
    return Fe3(t) ? h2 = c2(h2) : Ve2(t) ? h2 = l(h2) : h2 = u2(h2), h2;
  }
  function l(h2) {
    return h2.x = u2(h2.x), h2.y = u2(h2.y), "z" in h2 && (h2.z = u2(h2.z)), "w" in h2 && (h2.w = u2(h2.w)), h2;
  }
  function c2(h2) {
    const d3 = h2.elements;
    for (let m2 = 0, f2 = d3.length; m2 < f2; m2++)
      d3[m2] = u2(d3[m2]);
    return h2;
  }
  function u2(h2) {
    return r2 && (h2 = ki(s, h2)), (r2 || Ni(s)) && (h2 = h2 * n + i), h2;
  }
}
function Yt2(a, e, t = null) {
  if (a.array) {
    Array.isArray(e) || (e = new Array(a.count || 0)), e.length = t !== null ? t : a.count;
    for (let s = 0, n = e.length; s < n; s++)
      gs(a.type, e[s]) || (e[s] = Nt2(a));
  } else
    gs(a.type, e) || (e = Nt2(a));
  return e;
}
function nt3(a, e) {
  for (const t in e)
    t in a || delete e[t];
  for (const t in a) {
    const s = a[t];
    e[t] = Yt2(s, e[t]);
  }
}
function zi(a) {
  switch (a) {
    case "ENUM":
      return 1;
    case "SCALAR":
      return 1;
    case "VEC2":
      return 2;
    case "VEC3":
      return 3;
    case "VEC4":
      return 4;
    case "MAT2":
      return 4;
    case "MAT3":
      return 9;
    case "MAT4":
      return 16;
    case "BOOLEAN":
      return -1;
    case "STRING":
      return -1;
    default:
      return -1;
  }
}
var ut2 = class {
  constructor(e, t, s = null) {
    this.name = t.name || null, this.description = t.description || null, this.type = t.type, this.componentType = t.componentType || null, this.enumType = t.enumType || null, this.array = t.array || false, this.count = t.count || 0, this.normalized = t.normalized || false, this.offset = t.offset || 0, this.scale = q3(t, "scale", 1), this.max = q3(t, "max", 1 / 0), this.min = q3(t, "min", -1 / 0), this.required = t.required || false, this.noData = q3(t, "noData", null), this.default = q3(t, "default", null), this.semantic = q3(t, "semantic", null), this.enumSet = null, this.accessorProperty = s, s && (this.offset = q3(s, "offset", this.offset), this.scale = q3(s, "scale", this.scale), this.max = q3(s, "max", this.max), this.min = q3(s, "min", this.min)), t.type === "ENUM" && (this.enumSet = e[this.enumType], this.componentType === null && (this.componentType = q3(this.enumSet, "valueType", "UINT16")));
  }
  // shape the given target to match the data type of the property
  // enums are set to their integer value
  shapeToProperty(e, t = null) {
    return Yt2(this, e, t);
  }
  // resolve the given object to the default value for the property for a single element
  // enums are set to a default string
  resolveDefaultElement(e) {
    return st2(this, e);
  }
  // resolve the target to the default value for the property for every element if it's an array
  // enums are set to a default string
  resolveDefault(e) {
    return Vi2(this, e);
  }
  // converts any instances of no data to the default value
  resolveNoData(e) {
    return Fi(this, e);
  }
  // converts enums integers in the given target to strings
  resolveEnumsToStrings(e) {
    const t = this.enumSet;
    if (this.type === "ENUM")
      if (Array.isArray(e))
        for (let n = 0, i = e.length; n < i; n++)
          e[n] = s(e[n]);
      else
        e = s(e);
    return e;
    function s(n) {
      const i = t.values.find((r2) => r2.value === n);
      return i === null ? "" : i.name;
    }
  }
  // apply scales
  adjustValueScaleOffset(e) {
    return Tn(this.type) ? Gi(this, e) : e;
  }
};
var $t3 = class {
  constructor(e, t = {}, s = {}, n = null) {
    this.definition = e, this.class = t[e.class], this.className = e.class, this.enums = s, this.data = n, this.name = "name" in e ? e.name : null, this.properties = null;
  }
  getPropertyNames() {
    return Object.keys(this.class.properties);
  }
  includesData(e) {
    return !!this.definition.properties[e];
  }
  dispose() {
  }
  _initProperties(e = ut2) {
    const t = {};
    for (const s in this.class.properties)
      t[s] = new e(this.enums, this.class.properties[s], this.definition.properties[s]);
    this.properties = t;
  }
};
var Hi = class extends ut2 {
  constructor(e, t, s = null) {
    super(e, t, s), this.attribute = (s == null ? void 0 : s.attribute) ?? null;
  }
};
var qi = class extends $t3 {
  constructor(...e) {
    super(...e), this.isPropertyAttributeAccessor = true, this._initProperties(Hi);
  }
  getData(e, t, s = {}) {
    const n = this.properties;
    nt3(n, s);
    for (const i in n)
      s[i] = this.getPropertyValue(i, e, t, s[i]);
    return s;
  }
  getPropertyValue(e, t, s, n = null) {
    if (t >= this.count)
      throw new Error("PropertyAttributeAccessor: Requested index is outside the range of the buffer.");
    const i = this.properties[e], r2 = i.type;
    if (i) {
      if (!this.definition.properties[e])
        return i.resolveDefault(n);
    } else throw new Error("PropertyAttributeAccessor: Requested class property does not exist.");
    n = i.shapeToProperty(n);
    const o = s.getAttribute(i.attribute.toLowerCase());
    if (Fe3(r2)) {
      const l = n.elements;
      for (let c2 = 0, u2 = l.length; c2 < u2; c2 < u2)
        l[c2] = o.getComponent(t, c2);
    } else if (Ve2(r2))
      n.fromBufferAttribute(o, t);
    else if (r2 === "SCALAR" || r2 === "ENUM")
      n = o.getX(t);
    else
      throw new Error("StructuredMetadata.PropertyAttributeAccessor: BOOLEAN and STRING types are not supported by property attributes.");
    return n = i.adjustValueScaleOffset(n), n = i.resolveEnumsToStrings(n), n = i.resolveNoData(n), n;
  }
};
var Wi2 = class extends ut2 {
  constructor(e, t, s = null) {
    super(e, t, s), this.values = (s == null ? void 0 : s.values) ?? null, this.valueLength = zi(this.type), this.arrayOffsets = q3(s, "arrayOffsets", null), this.stringOffsets = q3(s, "stringOffsets", null), this.arrayOffsetType = q3(s, "arrayOffsetType", "UINT32"), this.stringOffsetType = q3(s, "stringOffsetType", "UINT32");
  }
  // returns the necessary array length based on the array offsets if present
  getArrayLengthFromId(e, t) {
    let s = this.count;
    if (this.arrayOffsets !== null) {
      const { arrayOffsets: n, arrayOffsetType: i } = this, r2 = De(i), o = new r2(e[n]);
      s = o[t + 1] - o[t];
    }
    return s;
  }
  // returns the index offset into the data buffer for the given id based on the
  // the array offsets if present
  getIndexOffsetFromId(e, t) {
    let s = t;
    if (this.arrayOffsets) {
      const { arrayOffsets: n, arrayOffsetType: i } = this, r2 = De(i);
      s = new r2(e[n])[s];
    } else this.array && (s *= this.count);
    return s;
  }
};
var ji = class extends $t3 {
  constructor(...e) {
    super(...e), this.isPropertyTableAccessor = true, this.count = this.definition.count, this._initProperties(Wi2);
  }
  getData(e, t = {}) {
    const s = this.properties;
    nt3(s, t);
    for (const n in s)
      t[n] = this.getPropertyValue(n, e, t[n]);
    return t;
  }
  // reads an individual element
  _readValueAtIndex(e, t, s, n = null) {
    const i = this.properties[e], { componentType: r2, type: o } = i, l = this.data, c2 = l[i.values], u2 = De(r2, o), h2 = new u2(c2), d3 = i.getIndexOffsetFromId(l, t);
    if (Tn(o) || o === "ENUM")
      return bn(h2, (d3 + s) * i.valueLength, o, n);
    if (o === "STRING") {
      let m2 = d3 + s, f2 = 0;
      if (i.stringOffsets !== null) {
        const { stringOffsets: g3, stringOffsetType: y4 } = i, x5 = De(y4), M5 = new x5(l[g3]);
        f2 = M5[m2 + 1] - M5[m2], m2 = M5[m2];
      }
      const p3 = new Uint8Array(h2.buffer, m2, f2);
      n = new TextDecoder().decode(p3);
    } else if (o === "BOOLEAN") {
      const m2 = d3 + s, f2 = Math.floor(m2 / 8), p3 = m2 % 8;
      n = (h2[f2] >> p3 & 1) === 1;
    }
    return n;
  }
  // Reads the data for the given table index
  getPropertyValue(e, t, s = null) {
    if (t >= this.count)
      throw new Error("PropertyTableAccessor: Requested index is outside the range of the table.");
    const n = this.properties[e];
    if (n) {
      if (!this.definition.properties[e])
        return n.resolveDefault(s);
    } else throw new Error("PropertyTableAccessor: Requested property does not exist.");
    const i = n.array, r2 = this.data, o = n.getArrayLengthFromId(r2, t);
    if (s = n.shapeToProperty(s, o), i)
      for (let l = 0, c2 = s.length; l < c2; l++)
        s[l] = this._readValueAtIndex(e, t, l, s[l]);
    else
      s = this._readValueAtIndex(e, t, 0, s);
    return s = n.adjustValueScaleOffset(s), s = n.resolveEnumsToStrings(s), s = n.resolveNoData(s), s;
  }
};
var Le = new Box2();
var ys = class {
  constructor() {
    this._renderer = new WebGLRenderer(), this._target = new WebGLRenderTarget(1, 1), this._texTarget = new WebGLRenderTarget(), this._quad = new FullScreenQuad(new ShaderMaterial({
      blending: CustomBlending,
      blendDst: ZeroFactor,
      blendSrc: OneFactor,
      uniforms: {
        map: { value: null },
        pixel: { value: new Vector2() }
      },
      vertexShader: (
        /* glsl */
        `
				void main() {

					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

				}
			`
      ),
      fragmentShader: (
        /* glsl */
        `
				uniform sampler2D map;
				uniform ivec2 pixel;

				void main() {

					gl_FragColor = texelFetch( map, pixel, 0 );

				}
			`
      )
    }));
  }
  // increases the width of the target render target to support more data
  increaseSizeTo(e) {
    this._target.setSize(Math.max(this._target.width, e), 1);
  }
  // read data from the rendered texture asynchronously
  readDataAsync(e) {
    const { _renderer: t, _target: s } = this;
    return t.readRenderTargetPixelsAsync(s, 0, 0, e.length / 4, 1, e);
  }
  // read data from the rendered texture
  readData(e) {
    const { _renderer: t, _target: s } = this;
    t.readRenderTargetPixels(s, 0, 0, e.length / 4, 1, e);
  }
  // render a single pixel from the source at the destination point on the render target
  // takes the texture, pixel to read from, and pixel to render in to
  renderPixelToTarget(e, t, s) {
    const { _renderer: n, _target: i } = this;
    Le.min.copy(t), Le.max.copy(t), Le.max.x += 1, Le.max.y += 1, n.initRenderTarget(i), n.copyTextureToTexture(e, i.texture, Le, s, 0);
  }
};
var he2 = new class {
  constructor() {
    let a = null;
    Object.getOwnPropertyNames(ys.prototype).forEach((e) => {
      e !== "constructor" && (this[e] = (...t) => (a = a || new ys(), a[e](...t)));
    });
  }
}();
var xs = new Vector2();
var Ts = new Vector2();
var bs = new Vector2();
function Xi(a, e) {
  return e === 0 ? a.getAttribute("uv") : a.getAttribute(`uv${e}`);
}
function _n(a, e, t = new Array(3)) {
  let s = 3 * e, n = 3 * e + 1, i = 3 * e + 2;
  return a.index && (s = a.index.getX(s), n = a.index.getX(n), i = a.index.getX(i)), t[0] = s, t[1] = n, t[2] = i, t;
}
function Sn(a, e, t, s, n) {
  const [i, r2, o] = s, l = Xi(a, e);
  xs.fromBufferAttribute(l, i), Ts.fromBufferAttribute(l, r2), bs.fromBufferAttribute(l, o), n.set(0, 0, 0).addScaledVector(xs, t.x).addScaledVector(Ts, t.y).addScaledVector(bs, t.z);
}
function Mn(a, e, t, s) {
  const n = a.x - Math.floor(a.x), i = a.y - Math.floor(a.y), r2 = Math.floor(n * e % e), o = Math.floor(i * t % t);
  return s.set(r2, o), s;
}
var _s = new Vector2();
var Ss = new Vector2();
var Ms = new Vector2();
var Yi = class extends ut2 {
  constructor(e, t, s = null) {
    super(e, t, s), this.channels = q3(s, "channels", [0]), this.index = q3(s, "index", null), this.texCoord = q3(s, "texCoord", null), this.valueLength = parseInt(this.type.replace(/[^0-9]/g, "")) || 1;
  }
  // takes the buffer to read from and the value index to read
  readDataFromBuffer(e, t, s = null) {
    const n = this.type;
    if (n === "BOOLEAN" || n === "STRING")
      throw new Error("PropertyTextureAccessor: BOOLEAN and STRING types not supported.");
    return bn(e, t * this.valueLength, n, s);
  }
};
var $i = class extends $t3 {
  constructor(...e) {
    super(...e), this.isPropertyTextureAccessor = true, this._asyncRead = false, this._initProperties(Yi);
  }
  // Reads the full set of property data
  getData(e, t, s, n = {}) {
    const i = this.properties;
    nt3(i, n);
    const r2 = Object.keys(i), o = r2.map((l) => n[l]);
    return this.getPropertyValuesAtTexel(r2, e, t, s, o), r2.forEach((l, c2) => n[l] = o[c2]), n;
  }
  // Reads the full set of property data asynchronously
  async getDataAsync(e, t, s, n = {}) {
    const i = this.properties;
    nt3(i, n);
    const r2 = Object.keys(i), o = r2.map((l) => n[l]);
    return await this.getPropertyValuesAtTexelAsync(r2, e, t, s, o), r2.forEach((l, c2) => n[l] = o[c2]), n;
  }
  // Reads values asynchronously
  getPropertyValuesAtTexelAsync(...e) {
    this._asyncRead = true;
    const t = this.getPropertyValuesAtTexel(...e);
    return this._asyncRead = false, t;
  }
  // Reads values from the textures synchronously
  getPropertyValuesAtTexel(e, t, s, n, i = []) {
    for (; i.length < e.length; ) i.push(null);
    i.length = e.length, he2.increaseSizeTo(i.length);
    const r2 = this.data, o = this.definition.properties, l = this.properties, c2 = _n(n, t);
    for (let d3 = 0, m2 = e.length; d3 < m2; d3++) {
      const f2 = e[d3];
      if (!o[f2])
        continue;
      const p3 = l[f2], g3 = r2[p3.index];
      Sn(n, p3.texCoord, s, c2, _s), Mn(_s, g3.image.width, g3.image.height, Ss), Ms.set(d3, 0), he2.renderPixelToTarget(g3, Ss, Ms);
    }
    const u2 = new Uint8Array(e.length * 4);
    if (this._asyncRead)
      return he2.readDataAsync(u2).then(() => (h2.call(this), i));
    return he2.readData(u2), h2.call(this), i;
    function h2() {
      for (let d3 = 0, m2 = e.length; d3 < m2; d3++) {
        const f2 = e[d3], p3 = l[f2], g3 = p3.type;
        if (i[d3] = Yt2(p3, i[d3]), p3) {
          if (!o[f2]) {
            i[d3] = p3.resolveDefault(i);
            continue;
          }
        } else throw new Error("PropertyTextureAccessor: Requested property does not exist.");
        const y4 = p3.valueLength * (p3.count || 1), x5 = p3.channels.map((_4) => u2[4 * d3 + _4]), M5 = p3.componentType, T2 = De(M5, g3), S2 = new T2(y4);
        if (new Uint8Array(S2.buffer).set(x5), p3.array) {
          const _4 = i[d3];
          for (let C4 = 0, L3 = _4.length; C4 < L3; C4++)
            _4[C4] = p3.readDataFromBuffer(S2, C4, _4[C4]);
        } else
          i[d3] = p3.readDataFromBuffer(S2, 0, i[d3]);
        i[d3] = p3.adjustValueScaleOffset(i[d3]), i[d3] = p3.resolveEnumsToStrings(i[d3]), i[d3] = p3.resolveNoData(i[d3]);
      }
    }
  }
  // dispose all of the texture data used
  dispose() {
    this.data.forEach((e) => {
      e && (e.dispose(), e.image instanceof ImageBitmap && e.image.close());
    });
  }
};
var Cs = class {
  constructor(e, t, s, n = null, i = null) {
    const {
      schema: r2,
      propertyTables: o = [],
      propertyTextures: l = [],
      propertyAttributes: c2 = []
    } = e, { enums: u2, classes: h2 } = r2, d3 = o.map((p3) => new ji(p3, h2, u2, s));
    let m2 = [], f2 = [];
    n && (n.propertyTextures && (m2 = n.propertyTextures.map((p3) => new $i(l[p3], h2, u2, t))), n.propertyAttributes && (f2 = n.propertyAttributes.map((p3) => new qi(c2[p3], h2, u2)))), this.schema = r2, this.tableAccessors = d3, this.textureAccessors = m2, this.attributeAccessors = f2, this.object = i, this.textures = t, this.nodeMetadata = n;
  }
  // Property Tables
  getPropertyTableData(e, t, s = null) {
    if (!Array.isArray(e) || !Array.isArray(t))
      s = s || {}, s = this.tableAccessors[e].getData(t, s);
    else {
      s = s || [];
      const n = Math.min(e.length, t.length);
      s.length = n;
      for (let i = 0; i < n; i++) {
        const r2 = this.tableAccessors[e[i]];
        s[i] = r2.getData(t[i], s[i]);
      }
    }
    return s;
  }
  getPropertyTableInfo(e = null) {
    if (e === null && (e = this.tableAccessors.map((t, s) => s)), Array.isArray(e))
      return e.map((t) => {
        const s = this.tableAccessors[t];
        return {
          name: s.name,
          className: s.definition.class
        };
      });
    {
      const t = this.tableAccessors[e];
      return {
        name: t.name,
        className: t.definition.class
      };
    }
  }
  // Property Textures
  getPropertyTextureData(e, t, s = []) {
    const n = this.textureAccessors;
    s.length = n.length;
    for (let i = 0; i < n.length; i++) {
      const r2 = n[i];
      s[i] = r2.getData(e, t, this.object.geometry, s[i]);
    }
    return s;
  }
  async getPropertyTextureDataAsync(e, t, s = []) {
    const n = this.textureAccessors;
    s.length = n.length;
    const i = [];
    for (let r2 = 0; r2 < n.length; r2++) {
      const l = n[r2].getDataAsync(e, t, this.object.geometry, s[r2]).then((c2) => {
        s[r2] = c2;
      });
      i.push(l);
    }
    return await Promise.all(i), s;
  }
  getPropertyTextureInfo() {
    return this.textureAccessors;
  }
  // Property Attributes
  getPropertyAttributeData(e, t = []) {
    const s = this.attributeAccessors;
    t.length = s.length;
    for (let n = 0; n < s.length; n++) {
      const i = s[n];
      t[n] = i.getData(e, this.object.geometry, t[n]);
    }
    return t;
  }
  getPropertyAttributeInfo() {
    return this.attributeAccessors.map((e) => ({
      name: e.name,
      className: e.definition.class
    }));
  }
  dispose() {
    this.textureAccessors.forEach((e) => e.dispose()), this.tableAccessors.forEach((e) => e.dispose()), this.attributeAccessors.forEach((e) => e.dispose());
  }
};
var ve2 = "EXT_structural_metadata";
function Qi(a, e = []) {
  var n;
  const t = ((n = a.json.textures) == null ? void 0 : n.length) || 0, s = new Array(t).fill(null);
  return e.forEach(({ properties: i }) => {
    for (const r2 in i) {
      const { index: o } = i[r2];
      s[o] === null && (s[o] = a.loadTexture(o));
    }
  }), Promise.all(s);
}
function Zi(a, e = []) {
  var n;
  const t = ((n = a.json.bufferViews) == null ? void 0 : n.length) || 0, s = new Array(t).fill(null);
  return e.forEach(({ properties: i }) => {
    for (const r2 in i) {
      const { values: o, arrayOffsets: l, stringOffsets: c2 } = i[r2];
      s[o] === null && (s[o] = a.loadBufferView(o)), s[l] === null && (s[l] = a.loadBufferView(l)), s[c2] === null && (s[c2] = a.loadBufferView(c2));
    }
  }), Promise.all(s);
}
var Ji = class {
  constructor(e) {
    this.parser = e, this.name = ve2;
  }
  async afterRoot({ scene: e, parser: t }) {
    const s = t.json.extensionsUsed;
    if (!s || !s.includes(ve2))
      return;
    let n = null, i = t.json.extensions[ve2];
    if (i.schemaUri) {
      const { manager: c2, path: u2, requestHeader: h2, crossOrigin: d3 } = t.options, m2 = new URL(i.schemaUri, u2).toString(), f2 = new FileLoader(c2);
      f2.setCrossOrigin(d3), f2.setResponseType("json"), f2.setRequestHeader(h2), n = f2.loadAsync(m2).then((p3) => {
        i = { ...i, schema: p3 };
      });
    }
    const [r2, o] = await Promise.all([
      Qi(t, i.propertyTextures),
      Zi(t, i.propertyTables),
      n
    ]), l = new Cs(i, r2, o);
    e.userData.structuralMetadata = l, e.traverse((c2) => {
      var u2;
      if (t.associations.has(c2)) {
        const { meshes: h2, primitives: d3 } = t.associations.get(c2), m2 = (u2 = t.json.meshes[h2]) == null ? void 0 : u2.primitives[d3];
        if (m2 && m2.extensions && m2.extensions[ve2]) {
          const f2 = m2.extensions[ve2];
          c2.userData.structuralMetadata = new Cs(i, r2, o, f2, c2);
        } else
          c2.userData.structuralMetadata = l;
      }
    });
  }
};
var As = new Vector2();
var Is = new Vector2();
var Ls = new Vector2();
function Ki(a) {
  return a.x > a.y && a.x > a.z ? 0 : a.y > a.z ? 1 : 2;
}
var er = class {
  constructor(e, t, s) {
    this.geometry = e, this.textures = t, this.data = s, this._asyncRead = false, this.featureIds = s.featureIds.map((n) => {
      const { texture: i, ...r2 } = n, o = {
        label: null,
        propertyTable: null,
        nullFeatureId: null,
        ...r2
      };
      return i && (o.texture = {
        texCoord: 0,
        channels: [0],
        ...i
      }), o;
    });
  }
  // returns list of textures
  getTextures() {
    return this.textures;
  }
  // returns a set of info for each feature
  getFeatureInfo() {
    return this.featureIds;
  }
  // performs texture data read back asynchronously
  getFeaturesAsync(...e) {
    this._asyncRead = true;
    const t = this.getFeatures(...e);
    return this._asyncRead = false, t;
  }
  // returns all features for the given point on the given triangle
  getFeatures(e, t) {
    const { geometry: s, textures: n, featureIds: i } = this, r2 = new Array(i.length).fill(null), o = i.length;
    he2.increaseSizeTo(o);
    const l = _n(s, e), c2 = l[Ki(t)];
    for (let d3 = 0, m2 = i.length; d3 < m2; d3++) {
      const f2 = i[d3], p3 = "nullFeatureId" in f2 ? f2.nullFeatureId : null;
      if ("texture" in f2) {
        const g3 = n[f2.texture.index];
        Sn(s, f2.texture.texCoord, t, l, As), Mn(As, g3.image.width, g3.image.height, Is), Ls.set(d3, 0), he2.renderPixelToTarget(n[f2.texture.index], Is, Ls);
      } else if ("attribute" in f2) {
        const y4 = s.getAttribute(`_feature_id_${f2.attribute}`).getX(c2);
        y4 !== p3 && (r2[d3] = y4);
      } else {
        const g3 = c2;
        g3 !== p3 && (r2[d3] = g3);
      }
    }
    const u2 = new Uint8Array(o * 4);
    if (this._asyncRead)
      return he2.readDataAsync(u2).then(() => (h2(), r2));
    return he2.readData(u2), h2(), r2;
    function h2() {
      const d3 = new Uint32Array(1);
      for (let m2 = 0, f2 = i.length; m2 < f2; m2++) {
        const p3 = i[m2], g3 = "nullFeatureId" in p3 ? p3.nullFeatureId : null;
        if ("texture" in p3) {
          const { channels: y4 } = p3.texture, x5 = y4.map((T2) => u2[4 * m2 + T2]);
          new Uint8Array(d3.buffer).set(x5);
          const M5 = d3[0];
          M5 !== g3 && (r2[m2] = M5);
        }
      }
    }
  }
  // dispose all of the texture data used
  dispose() {
    this.textures.forEach((e) => {
      e && (e.dispose(), e.image instanceof ImageBitmap && e.image.close());
    });
  }
};
var it2 = "EXT_mesh_features";
function vs(a, e, t) {
  a.traverse((s) => {
    var n;
    if (e.associations.has(s)) {
      const { meshes: i, primitives: r2 } = e.associations.get(s), o = (n = e.json.meshes[i]) == null ? void 0 : n.primitives[r2];
      o && o.extensions && o.extensions[it2] && t(s, o.extensions[it2]);
    }
  });
}
var tr = class {
  constructor(e) {
    this.parser = e, this.name = it2;
  }
  async afterRoot({ scene: e, parser: t }) {
    var o;
    const s = t.json.extensionsUsed;
    if (!s || !s.includes(it2))
      return;
    const n = ((o = t.json.textures) == null ? void 0 : o.length) || 0, i = new Array(n).fill(null);
    vs(e, t, (l, { featureIds: c2 }) => {
      c2.forEach((u2) => {
        if (u2.texture && i[u2.texture.index] === null) {
          const h2 = u2.texture.index;
          i[h2] = t.loadTexture(h2);
        }
      });
    });
    const r2 = await Promise.all(i);
    vs(e, t, (l, c2) => {
      l.userData.meshFeatures = new er(l.geometry, r2, c2);
    });
  }
};
var sr = class {
  constructor() {
    this.name = "CESIUM_RTC";
  }
  afterRoot(e) {
    if (e.parser.json.extensions && e.parser.json.extensions.CESIUM_RTC) {
      const { center: t } = e.parser.json.extensions.CESIUM_RTC;
      t && (e.scene.position.x += t[0], e.scene.position.y += t[1], e.scene.position.z += t[2]);
    }
  }
};
var ao = class {
  constructor(e) {
    e = {
      metadata: true,
      rtc: true,
      plugins: [],
      dracoLoader: null,
      ktxLoader: null,
      meshoptDecoder: null,
      autoDispose: true,
      ...e
    }, this.tiles = null, this.metadata = e.metadata, this.rtc = e.rtc, this.plugins = e.plugins, this.dracoLoader = e.dracoLoader, this.ktxLoader = e.ktxLoader, this.meshoptDecoder = e.meshoptDecoder, this._gltfRegex = /\.(gltf|glb)$/g, this._dracoRegex = /\.drc$/g, this._loader = null;
  }
  init(e) {
    const t = new GLTFLoader(e.manager);
    this.dracoLoader && (t.setDRACOLoader(this.dracoLoader), e.manager.addHandler(this._dracoRegex, this.dracoLoader)), this.ktxLoader && t.setKTX2Loader(this.ktxLoader), this.meshoptDecoder && t.setMeshoptDecoder(this.meshoptDecoder), this.rtc && t.register(() => new sr()), this.metadata && (t.register(() => new Ji()), t.register(() => new tr())), this.plugins.forEach((s) => t.register(s)), e.manager.addHandler(this._gltfRegex, t), this.tiles = e, this._loader = t;
  }
  dispose() {
    this.tiles.manager.removeHandler(this._gltfRegex), this.tiles.manager.removeHandler(this._dracoRegex), this.autoDispose && (this.ktxLoader.dispose(), this.dracoLoader.dispose());
  }
};
var qe = new Sphere();
var co = class {
  set delay(e) {
    this.deferCallbacks.delay = e;
  }
  get delay() {
    return this.deferCallbacks.delay;
  }
  set bytesTarget(e) {
    this.lruCache.minBytesSize = e;
  }
  get bytesTarget() {
    return this.lruCache.minBytesSize;
  }
  get estimatedGpuBytes() {
    return this.lruCache.cachedBytes;
  }
  constructor(e = {}) {
    const {
      delay: t = 0,
      bytesTarget: s = 0
    } = e;
    this.name = "UNLOAD_TILES_PLUGIN", this.tiles = null, this.lruCache = new z(), this.deferCallbacks = new nr(), this.delay = t, this.bytesTarget = s;
  }
  init(e) {
    this.tiles = e;
    const { lruCache: t, deferCallbacks: s } = this;
    s.callback = (i) => {
      t.markUnused(i), t.scheduleUnload(false);
    };
    const n = (i) => {
      const r2 = i.engineData.scene;
      e.visibleTiles.has(i) || e.invokeOnePlugin((l) => l.unloadTileFromGPU && l.unloadTileFromGPU(r2, i));
    };
    this._onUpdateBefore = () => {
      t.unloadPriorityCallback = e.lruCache.unloadPriorityCallback, t.computeMemoryUsageCallback = e.lruCache.computeMemoryUsageCallback, t.minSize = 1 / 0, t.maxSize = 1 / 0, t.maxBytesSize = 1 / 0, t.unloadPercent = 1, t.autoMarkUnused = false;
    }, this._onVisibilityChangeCallback = ({ tile: i, visible: r2 }) => {
      r2 ? (t.add(i, n), e.markTileUsed(i), s.cancel(i)) : s.run(i);
    }, e.forEachLoadedModel((i, r2) => {
      const o = e.visibleTiles.has(r2);
      this._onVisibilityChangeCallback({ scene: i, visible: o });
    }), e.addEventListener("tile-visibility-change", this._onVisibilityChangeCallback), e.addEventListener("update-before", this._onUpdateBefore);
  }
  unloadTileFromGPU(e, t) {
    e && e.traverse((s) => {
      if (s.material) {
        const n = s.material;
        n.dispose();
        for (const i in n) {
          const r2 = n[i];
          r2 && r2.isTexture && r2.dispose();
        }
      }
      s.geometry && s.geometry.dispose();
    });
  }
  dispose() {
    this.tiles.removeEventListener("tile-visibility-change", this._onVisibilityChangeCallback), this.tiles.removeEventListener("update-before", this._onUpdateBefore), this.deferCallbacks.cancelAll();
  }
};
var nr = class {
  constructor(e = () => {
  }) {
    this.map = /* @__PURE__ */ new Map(), this.callback = e, this.delay = 0;
  }
  run(e) {
    const { map: t, delay: s } = this;
    if (t.has(e))
      throw new Error("DeferCallbackManager: Callback already initialized.");
    s === 0 ? this.callback(e) : t.set(e, setTimeout(() => this.callback(e), s));
  }
  cancel(e) {
    const { map: t } = this;
    t.has(e) && (clearTimeout(t.get(e)), t.delete(e));
  }
  cancelAll() {
    this.map.forEach((e, t) => {
      this.cancel(t);
    });
  }
};
var { clamp: bt3 } = MathUtils;
var _t3 = Symbol("FADE_PARAMS");
var Es = Symbol("HAS_POPPED_IN");
var ws = new Vector3();
var Ps = new Vector3();
var Rs = new Quaternion();
var Ds = new Quaternion();
var Bs = new Vector3();
var St2 = new Matrix4();
var Os = new Vector3();
var Us = new Vector3();
var ee2 = new Mesh();
var Mt3 = new FullScreenQuad(new MeshBasicMaterial());
var Vt2 = new DataTexture(new Uint8Array([255, 255, 255, 255]), 1, 1);
Vt2.needsUpdate = true;
var Ct3 = new Sphere();
var je = new Vector3();
var Ee2 = new Matrix4();
var Ns = new Matrix4();
var At3 = new Raycaster();
var mr = new MeshBasicMaterial({ side: DoubleSide });
var Vs = new Box3();
var vt2 = Symbol("OVERLAY_PARAMS");
var Et2 = new Vector3();
var Xe = new Vector3();
var _e2 = new Matrix4();
var Ye = new Vector3();
var wt2 = new Vector3();
var Pt = new Vector3();
var se3 = new Vector3();
var Cr = new Box3();
var Gs = Symbol("SPLIT_TILE_DATA");
var $e = Symbol("SPLIT_HASH");
var te2 = new Vector3();
var zs = ["x", "y", "z"];
var Ar = class extends LineSegments {
  constructor(e, t = 16776960, s = 40) {
    const n = new BufferGeometry(), i = [];
    for (let r2 = 0; r2 < 3; r2++) {
      const o = zs[r2], l = zs[(r2 + 1) % 3];
      te2.set(0, 0, 0);
      for (let c2 = 0; c2 < s; c2++) {
        let u2;
        u2 = 2 * Math.PI * c2 / (s - 1), te2[o] = Math.sin(u2), te2[l] = Math.cos(u2), i.push(te2.x, te2.y, te2.z), u2 = 2 * Math.PI * (c2 + 1) / (s - 1), te2[o] = Math.sin(u2), te2[l] = Math.cos(u2), i.push(te2.x, te2.y, te2.z);
      }
    }
    n.setAttribute("position", new BufferAttribute(new Float32Array(i), 3)), n.computeBoundingSphere(), super(n, new LineBasicMaterial({ color: t, toneMapped: false })), this.sphere = e, this.type = "SphereHelper";
  }
  updateMatrixWorld(e) {
    const t = this.sphere;
    this.position.copy(t.center), this.scale.setScalar(t.radius), super.updateMatrixWorld(e);
  }
};
var Rt3 = new Vector3();
var Qe = new Vector3();
var ne3 = new Vector3();
function Ir(a, { computeNormals: e = false } = {}) {
  const {
    latStart: t = -Math.PI / 2,
    latEnd: s = Math.PI / 2,
    lonStart: n = 0,
    lonEnd: i = 2 * Math.PI,
    heightStart: r2 = 0,
    heightEnd: o = 0
  } = a, l = new BoxGeometry(1, 1, 1, 32, 32), { normal: c2, position: u2 } = l.attributes, h2 = u2.clone();
  for (let d3 = 0, m2 = u2.count; d3 < m2; d3++) {
    ne3.fromBufferAttribute(u2, d3);
    const f2 = MathUtils.mapLinear(ne3.x, -0.5, 0.5, t, s), p3 = MathUtils.mapLinear(ne3.y, -0.5, 0.5, n, i);
    let g3 = r2;
    a.getCartographicToNormal(f2, p3, Rt3), ne3.z < 0 && (g3 = o), a.getCartographicToPosition(f2, p3, g3, ne3), u2.setXYZ(d3, ...ne3);
  }
  e && l.computeVertexNormals();
  for (let d3 = 0, m2 = h2.count; d3 < m2; d3++) {
    ne3.fromBufferAttribute(h2, d3);
    const f2 = MathUtils.mapLinear(ne3.x, -0.5, 0.5, t, s), p3 = MathUtils.mapLinear(ne3.y, -0.5, 0.5, n, i);
    Rt3.fromBufferAttribute(c2, d3), a.getCartographicToNormal(f2, p3, Qe), Math.abs(Rt3.dot(Qe)) > 0.1 && (ne3.z > 0 && Qe.multiplyScalar(-1), c2.setXYZ(d3, ...Qe));
  }
  return l;
}
var Lr = class extends LineSegments {
  constructor(e = new $t(), t = 16776960) {
    super(), this.ellipsoidRegion = e, this.material.color.set(t), this.update();
  }
  update() {
    const e = Ir(this.ellipsoidRegion);
    this.geometry.dispose(), this.geometry = new EdgesGeometry(e, 80);
  }
  dispose() {
    this.geometry.dispose(), this.material.dispose();
  }
};
var Hs = Symbol("ORIGINAL_MATERIAL");
var Dt3 = Symbol("HAS_RANDOM_COLOR");
var Bt3 = Symbol("HAS_RANDOM_NODE_COLOR");
var Ot2 = Symbol("LOAD_TIME");
var ge3 = Symbol("PARENT_BOUND_REF_COUNT");
var qs = new Sphere();
var Ze = () => {
};
var Ut3 = {};
function Je(a) {
  if (!Ut3[a]) {
    const e = Math.random(), t = 0.5 + Math.random() * 0.5, s = 0.375 + Math.random() * 0.25;
    Ut3[a] = new Color().setHSL(e, t, s);
  }
  return Ut3[a];
}
var Re2 = 0;
var vn = 1;
var En = 2;
var wn = 3;
var Pn = 4;
var Rn = 5;
var Dn = 6;
var Ke = 7;
var et2 = 8;
var Bn = 9;
var Ft2 = 10;
var vr = Object.freeze({
  NONE: Re2,
  SCREEN_ERROR: vn,
  GEOMETRIC_ERROR: En,
  DISTANCE: wn,
  DEPTH: Pn,
  RELATIVE_DEPTH: Rn,
  IS_LEAF: Dn,
  RANDOM_COLOR: Ke,
  RANDOM_NODE_COLOR: et2,
  CUSTOM_COLOR: Bn,
  LOAD_ORDER: Ft2
});
var Io = class {
  static get ColorModes() {
    return vr;
  }
  get unlit() {
    return this._unlit;
  }
  set unlit(e) {
    e !== this._unlit && (this._unlit = e, this.materialsNeedUpdate = true);
  }
  get colorMode() {
    return this._colorMode;
  }
  set colorMode(e) {
    e !== this._colorMode && (this._colorMode = e, this.materialsNeedUpdate = true);
  }
  get enabled() {
    return this._enabled;
  }
  set enabled(e) {
    e !== this._enabled && this.tiles !== null && (this._enabled = e, e ? this.init(this.tiles) : this.dispose());
  }
  get displayParentBounds() {
    return this._displayParentBounds;
  }
  set displayParentBounds(e) {
    this._displayParentBounds !== e && (this._displayParentBounds = e, e ? this.tiles.traverse((t) => {
      t.traversal.visible && this._onTileVisibilityChange(t, true);
    }) : this.tiles.traverse((t) => {
      t[ge3] = null, this._onTileVisibilityChange(t, t.traversal.visible);
    }));
  }
  constructor(e) {
    e = {
      displayParentBounds: false,
      displayBoxBounds: false,
      displaySphereBounds: false,
      displayRegionBounds: false,
      colorMode: Re2,
      maxDebugDepth: -1,
      maxDebugDistance: -1,
      maxDebugError: -1,
      customColorCallback: null,
      unlit: false,
      enabled: true,
      ...e
    }, this.name = "DEBUG_TILES_PLUGIN", this.tiles = null, this._colorMode = null, this._unlit = null, this.materialsNeedUpdate = false, this.extremeDebugDepth = -1, this.extremeDebugError = -1, this.boxGroup = null, this.sphereGroup = null, this.regionGroup = null, this._enabled = e.enabled, this._displayParentBounds = e.displayParentBounds, this.displayBoxBounds = e.displayBoxBounds, this.displaySphereBounds = e.displaySphereBounds, this.displayRegionBounds = e.displayRegionBounds, this.colorMode = e.colorMode, this.maxDebugDepth = e.maxDebugDepth, this.maxDebugDistance = e.maxDebugDistance, this.maxDebugError = e.maxDebugError, this.customColorCallback = e.customColorCallback, this.unlit = e.unlit, this.getDebugColor = (t, s) => {
      s.setRGB(t, t, t);
    };
  }
  // initialize the groups for displaying helpers, register events, and initialize existing tiles
  init(e) {
    if (this.tiles = e, !this.enabled)
      return;
    const t = e.group;
    this.boxGroup = new Group(), this.boxGroup.name = "DebugTilesRenderer.boxGroup", t.add(this.boxGroup), this.boxGroup.updateMatrixWorld(), this.sphereGroup = new Group(), this.sphereGroup.name = "DebugTilesRenderer.sphereGroup", t.add(this.sphereGroup), this.sphereGroup.updateMatrixWorld(), this.regionGroup = new Group(), this.regionGroup.name = "DebugTilesRenderer.regionGroup", t.add(this.regionGroup), this.regionGroup.updateMatrixWorld(), this._onLoadTilesetCB = () => {
      this._initExtremes();
    }, this._onLoadModelCB = ({ scene: s, tile: n }) => {
      this._onLoadModel(s, n);
    }, this._onDisposeModelCB = ({ tile: s }) => {
      this._onDisposeModel(s);
    }, this._onUpdateAfterCB = () => {
      this.update();
    }, this._onTileVisibilityChangeCB = ({ scene: s, tile: n, visible: i }) => {
      this._onTileVisibilityChange(n, i);
    }, e.addEventListener("load-tileset", this._onLoadTilesetCB), e.addEventListener("load-model", this._onLoadModelCB), e.addEventListener("dispose-model", this._onDisposeModelCB), e.addEventListener("update-after", this._onUpdateAfterCB), e.addEventListener("tile-visibility-change", this._onTileVisibilityChangeCB), this._initExtremes(), e.traverse((s) => {
      s.engineData.scene && this._onLoadModel(s.engineData.scene, s);
    }), e.visibleTiles.forEach((s) => {
      this._onTileVisibilityChange(s, true);
    });
  }
  getTileFromObject3D(e) {
    let t = null;
    return this.tiles.activeTiles.forEach((n) => {
      if (t)
        return true;
      const i = n.engineData.scene;
      i && i.traverse((r2) => {
        r2 === e && (t = n);
      });
    }), t;
  }
  _initExtremes() {
    if (!(this.tiles && this.tiles.root))
      return;
    let e = -1, t = -1;
    this.tiles.traverse(null, (s, n, i) => {
      e = Math.max(e, i), t = Math.max(t, s.geometricError);
    }, false), this.extremeDebugDepth = e, this.extremeDebugError = t;
  }
  update() {
    const { tiles: e, colorMode: t } = this;
    if (!e.root)
      return;
    this.materialsNeedUpdate && (e.forEachLoadedModel((c2) => {
      this._updateMaterial(c2);
    }), this.materialsNeedUpdate = false), this.boxGroup.visible = this.displayBoxBounds, this.sphereGroup.visible = this.displaySphereBounds, this.regionGroup.visible = this.displayRegionBounds;
    let s = -1;
    this.maxDebugDepth === -1 ? s = this.extremeDebugDepth : s = this.maxDebugDepth;
    let n = -1;
    this.maxDebugError === -1 ? n = this.extremeDebugError : n = this.maxDebugError;
    let i = -1;
    this.maxDebugDistance === -1 ? (e.getBoundingSphere(qs), i = qs.radius) : i = this.maxDebugDistance;
    const { errorTarget: r2, visibleTiles: o } = e;
    let l;
    t === Ft2 && (l = Array.from(o).sort((c2, u2) => c2[Ot2] - u2[Ot2])), o.forEach((c2) => {
      const u2 = c2.engineData.scene;
      let h2, d3, m2;
      t === Ke && (h2 = Math.random(), d3 = 0.5 + Math.random() * 0.5, m2 = 0.375 + Math.random() * 0.25), u2.traverse((f2) => {
        if (t === et2 && (h2 = Math.random(), d3 = 0.5 + Math.random() * 0.5, m2 = 0.375 + Math.random() * 0.25), f2.material)
          switch (t !== Ke && delete f2.material[Dt3], t !== et2 && delete f2.material[Bt3], t) {
            case Pn: {
              const p3 = c2.internal.depth / s;
              this.getDebugColor(p3, f2.material.color);
              break;
            }
            case Rn: {
              const p3 = c2.internal.depthFromRenderedParent / s;
              this.getDebugColor(p3, f2.material.color);
              break;
            }
            case vn: {
              const p3 = c2.traversal.error / r2;
              p3 > 1 ? f2.material.color.setRGB(1, 0, 0) : this.getDebugColor(p3, f2.material.color);
              break;
            }
            case En: {
              const p3 = Math.min(c2.geometricError / n, 1);
              this.getDebugColor(p3, f2.material.color);
              break;
            }
            case wn: {
              const p3 = Math.min(c2.traversal.distanceFromCamera / i, 1);
              this.getDebugColor(p3, f2.material.color);
              break;
            }
            case Dn: {
              !c2.children || c2.children.length === 0 ? this.getDebugColor(1, f2.material.color) : this.getDebugColor(0, f2.material.color);
              break;
            }
            case et2: {
              f2.material[Bt3] || (f2.material.color.setHSL(h2, d3, m2), f2.material[Bt3] = true);
              break;
            }
            case Ke: {
              f2.material[Dt3] || (f2.material.color.setHSL(h2, d3, m2), f2.material[Dt3] = true);
              break;
            }
            case Bn: {
              this.customColorCallback ? this.customColorCallback(c2, f2) : console.warn("DebugTilesRenderer: customColorCallback not defined");
              break;
            }
            case Ft2: {
              const p3 = l.indexOf(c2);
              this.getDebugColor(p3 / (l.length - 1), f2.material.color);
              break;
            }
          }
      });
    });
  }
  _onTileVisibilityChange(e, t) {
    this.displayParentBounds ? u(e, (s) => {
      s[ge3] == null && (s[ge3] = 0), t ? s[ge3]++ : s[ge3] > 0 && s[ge3]--;
      const n = s === e && t || this.displayParentBounds && s[ge3] > 0;
      this._updateBoundHelper(s, n);
    }) : this._updateBoundHelper(e, t);
  }
  _createBoundHelper(e) {
    const t = this.tiles, s = e.engineData, { sphere: n, obb: i, region: r2 } = s.boundingVolume;
    if (i) {
      const o = new Group();
      o.name = "DebugTilesRenderer.boxHelperGroup", o.matrix.copy(i.transform), o.matrixAutoUpdate = false, s.boxHelperGroup = o;
      const l = new Box3Helper(i.box, Je(e.internal.depth));
      l.raycast = Ze, o.add(l);
      const c2 = new Mesh(new BoxGeometry(), new MeshBasicMaterial({
        color: Je(e.internal.depth),
        transparent: true,
        depthWrite: false,
        opacity: 0.05,
        side: DoubleSide
      }));
      i.box.getSize(c2.scale), c2.raycast = Ze, o.add(c2), t.visibleTiles.has(e) && this.displayBoxBounds && (this.boxGroup.add(o), o.updateMatrixWorld(true));
    }
    if (n) {
      const o = new Ar(n, Je(e.internal.depth));
      o.raycast = Ze, s.sphereHelper = o, t.visibleTiles.has(e) && this.displaySphereBounds && (this.sphereGroup.add(o), o.updateMatrixWorld(true));
    }
    if (r2) {
      const o = new Lr(r2, Je(e.internal.depth));
      o.raycast = Ze;
      const l = new Sphere();
      r2.getBoundingSphere(l), o.position.copy(l.center), l.center.multiplyScalar(-1), o.geometry.translate(...l.center), s.regionHelper = o, t.visibleTiles.has(e) && this.displayRegionBounds && (this.regionGroup.add(o), o.updateMatrixWorld(true));
    }
  }
  _updateHelperMaterials(e, t) {
    t.traverse((s) => {
      const { material: n } = s;
      if (!n)
        return;
      e.traversal.visible || !this.displayParentBounds ? n.opacity = s.isMesh ? 0.05 : 1 : n.opacity = s.isMesh ? 0.01 : 0.2;
      const i = n.transparent;
      n.transparent = n.opacity < 1, n.transparent !== i && (n.needsUpdate = true);
    });
  }
  _updateBoundHelper(e, t) {
    const s = e.engineData;
    if (!s)
      return;
    const n = this.sphereGroup, i = this.boxGroup, r2 = this.regionGroup;
    t && s.boxHelperGroup == null && s.sphereHelper == null && s.regionHelper == null && this._createBoundHelper(e);
    const o = s.boxHelperGroup, l = s.sphereHelper, c2 = s.regionHelper;
    t ? (o && (i.add(o), o.updateMatrixWorld(true), this._updateHelperMaterials(e, o)), l && (n.add(l), l.updateMatrixWorld(true), this._updateHelperMaterials(e, l)), c2 && (r2.add(c2), c2.updateMatrixWorld(true), this._updateHelperMaterials(e, c2))) : (o && i.remove(o), l && n.remove(l), c2 && r2.remove(c2));
  }
  _updateMaterial(e) {
    const { colorMode: t, unlit: s } = this;
    e.traverse((n) => {
      if (!n.material)
        return;
      const i = n.material, r2 = n[Hs];
      if (i !== r2 && i.dispose(), t !== Re2 || s) {
        if (n.isPoints) {
          const o = new PointsMaterial();
          o.size = r2.size, o.sizeAttenuation = r2.sizeAttenuation, n.material = o;
        } else s ? n.material = new MeshBasicMaterial() : (n.material = new MeshStandardMaterial(), n.material.flatShading = true);
        t === Re2 && (n.material.map = r2.map, n.material.color.set(r2.color));
      } else
        n.material = r2;
    });
  }
  _onLoadModel(e, t) {
    t[Ot2] = performance.now(), e.traverse((s) => {
      const n = s.material;
      n && (s[Hs] = n);
    }), this._updateMaterial(e);
  }
  _onDisposeModel(e) {
    const t = e.engineData;
    t != null && t.boxHelperGroup && (t.boxHelperGroup.children[0].geometry.dispose(), delete t.boxHelperGroup), t != null && t.sphereHelper && (t.sphereHelper.geometry.dispose(), delete t.sphereHelper), t != null && t.regionHelper && (t.regionHelper.geometry.dispose(), delete t.regionHelper);
  }
  dispose() {
    var t, s, n;
    const e = this.tiles;
    e.removeEventListener("load-tileset", this._onLoadTilesetCB), e.removeEventListener("load-model", this._onLoadModelCB), e.removeEventListener("dispose-model", this._onDisposeModelCB), e.removeEventListener("update-after", this._onUpdateAfterCB), e.removeEventListener("tile-visibility-change", this._onTileVisibilityChangeCB), this.colorMode = Re2, this.unlit = false, e.forEachLoadedModel((i) => {
      this._updateMaterial(i);
    }), e.traverse((i) => {
      this._onDisposeModel(i);
    }, null, false), (t = this.boxGroup) == null || t.removeFromParent(), (s = this.sphereGroup) == null || s.removeFromParent(), (n = this.regionGroup) == null || n.removeFromParent();
  }
};
var rt4 = N * Math.PI * 2;
var Ws = new re3("EPSG:3857");
var Xs = N * Math.PI * 2;
var Ys = new re3("EPSG:3857");

// node_modules/three/examples/jsm/libs/meshopt_decoder.module.js
var MeshoptDecoder = function() {
  "use strict";
  var wasm_base = "b9H79Tebbbe8Fv9Gbb9Gvuuuuueu9Giuuub9Geueu9Giuuueuikqbeeedddillviebeoweuec:q;iekr;:P8Yqdbk;3sezu8Jjjjjbcj;eb9Rgv8Kjjjjbc9:hodnadcefal0mbcuhoaiRbbc:Ge9hmbavaialfgrad9Radz1jjjbhwcj;abad9UhoaicefhldnadTmbaoc;WFbGgocjdaocjd6EhDcbhqinaqae9pmeaDaeaq9RaqaDfae6Egkcsfgocl4cifcd4hxdndndndnaoc9WGgmTmbcbhPcehsawcjdfhzalhHinaraH9Rax6midnaraHaxfgl9RcK6mbczhoinawcj;::faOaAfgORbbalRbrglcl4gAaAcsSgAE86bbaocufaOaAfgoRbbalcsGglalcsSglE86bbaoalfhlxekaOal8Pbb83bbaOcwfalcwf8Pbb83bbalczfhlkdnaiam9pmbaiczfhoaral9RcL0mekkaiam6mialTmidnakTmbawaPfRbbhOcbhoazhiinaiawcj;cbfaofRbbgAce4cbaAceG9R7aOfgO86bbaiadfhiaocefgoak9hmbkkazcefhzaPcefgPad6hsalhHaPad9hmexvkkcbhlasceGmdxikalaxad2fhCdnakTmbcbhHcehsawcjdfhminaral9Rax6mialTmdalaxfhlawaHfRbbhOcbhoamhiinaiawcj;cbfaofRbbgAce4cbaAceG9R7aOfgO86bbaiadfhiaocefgoak9hmbkamcefhmaHcefgHad6hsaHad9hmbkaChlxikcbhocehsinaral9Rax6mdalTmealaxfhlaocefgoad6hsadao9hmbkaChlxdkcbhlasceGTmekc9:hoxikabaqad2fawcjdfakad2z1jjjb8Aawawcjdfakcufad2fadz1jjjb8Aakaqfhqalmbkc9:hoxekcbc99aral9Radcaadca0ESEhokavcj;ebf8Kjjjjbaok;yzeHu8Jjjjjbc;ae9Rgv8Kjjjjbc9:hodnaeci9UgrcHfal0mbcuhoaiRbbgwc;WeGc;Ge9hmbawcsGgDce0mbavc;abfcFecjez:jjjjb8AavcUf9cu83ibavc8Wf9cu83ibavcyf9cu83ibavcaf9cu83ibavcKf9cu83ibavczf9cu83ibav9cu83iwav9cu83ibaialfc9WfhqaicefgwarfhodnaeTmbcmcsaDceSEhkcbhxcbhmcbhDcbhicbhlindnaoaq9nmbc9:hoxikdndnawRbbgrc;Ve0mbavc;abfalarcl4cu7fcsGcitfgPydlhsaPydbhzdnarcsGgPak9pmbavaiarcu7fcsGcdtfydbaxaPEhraPThPdndnadcd9hmbabaDcetfgHaz87ebaHcdfas87ebaHclfar87ebxekabaDcdtfgHazBdbaHclfasBdbaHcwfarBdbkaxaPfhxavc;abfalcitfgHarBdbaHasBdlavaicdtfarBdbavc;;abfalcitfgramBdbarasBdlavaicdtfamBdbavc;abfalcefcsGglcitfgrazBdbaramBdlaicefhialcefhlxekdnarcpe0mbaxcefgOavaiaqarcsGfRbbgPcl49RcsGcdtfydbaPcz6gHEhravaiaP9RcsGcdtfydbaOaHfgsaPcsGgOEhPaOThOdndnadcd9hmbabaDcetfgzax87ebazcdfar87ebazclfaP87ebxekabaDcdtfgzaxBdbazclfarBdbazcwfaPBdbkavaicdtfaxBdbavc;abfalcitfgzarBdbazaxBdlavaicefgicsGcdtfarBdbavc;abfalcefcsGcitfgzaPBdbazarBdlavaiaHfcsGgicdtfaPBdbavc;abfalcdfcsGglcitfgraxBdbaraPBdlalcefhlaiaOfhiasaOfhxxekaxcbaoRbbgzEgAarc;:;abfalcitfgrasBdbaraABdlavaicdtfaABdbavc;abfalcefcsGcitfgraOBdbarasBdlavaicefgicsGcdtfasBdbavc;abfalcdfcsGcitfgraABdbaraOBdlavaiazcz6aXcsSVfgicsGcdtfaOBdbaiaCTaCcsSVfhialcifhlkawcefhwalcsGhlaicsGhiaDcifgDae6mbkkcbc99aoaqSEhokavc;aef8Kjjjjbaok:llevu8Jjjjjbcz9Rhvc9:hodnaecvfal0mbcuhoaiRbbc;:eGc;qe9hmbav9cb83iwaicefhraialfc98fhwdnaeTmbdnadcdSmbcbhDindnaraw6mbc9:skarcefhoar8SbbglcFeGhidndnalcu9mmbaohrxekarcvfhraicFbGhicrhldninao8SbbgdcFbGaltaiVhiadcu9kmeaocefhoalcrfglc8J9hmbxdkkaocefhrkabaDcdtfaicd4cbaice4ceG9R7avcwfaiceGcdtVgoydbfglBdbaoalBdbaDcefgDae9hmbxdkkcbhDindnaraw6mbc9:skarcefhoar8SbbglcFeGhidndnalcu9mmbaohrxekarcvfhraicFbGhicrhldninao8SbbgdcFbGaltaiVhiadcu9kmeaocefhoalcrfglc8J9hmbxdkkaocefhrkabaDcetfaicd4cbaice4ceG9R7avcwfaiceGcdtVgoydbfgl87ebaoalBdbaDcefgDae9hmbkkcbc99arawSEhokaok:Lvoeue99dud99eud99dndnadcl9hmbaeTmeindndnabcdfgd8Sbb:Yab8Sbbgi:Ygl:l:tabcefgv8Sbbgo:Ygr:l:tgwJbb;:9cawawNJbbbbawawJbbbb9GgDEgq:mgkaqaicb9iEalMgwawNakaqaocb9iEarMgqaqNMM:r:vglNJbbbZJbbb:;aDEMgr:lJbbb9p9DTmbar:Ohixekcjjjj94hikadai86bbdndnaqalNJbbbZJbbb:;aqJbbbb9GEMgq:lJbbb9p9DTmbaq:Ohdxekcjjjj94hdkavad86bbdndnawalNJbbbZJbbb:;awJbbbb9GEMgw:lJbbb9p9DTmbaw:Ohdxekcjjjj94hdkabad86bbabclfhbaecufgembxdkkaeTmbindndnabclfgd8Ueb:Yab8Uebgi:Ygl:l:tabcdfgv8Uebgo:Ygr:l:tgwJb;:FSawawNJbbbbawawJbbbb9GgDEgq:mgkaqaicb9iEalMgwawNakaqaocb9iEarMgqaqNMM:r:vglNJbbbZJbbb:;aDEMgr:lJbbb9p9DTmbar:Ohixekcjjjj94hikadai87ebdndnaqalNJbbbZJbbb:;aqJbbbb9GEMgq:lJbbb9p9DTmbaq:Ohdxekcjjjj94hdkavad87ebdndnawalNJbbbZJbbb:;awJbbbb9GEMgw:lJbbb9p9DTmbaw:Ohdxekcjjjj94hdkabad87ebabcwfhbaecufgembkkk;siliui99iue99dnaeTmbcbhiabhlindndnJ;Zl81Zalcof8UebgvciV:Y:vgoal8Ueb:YNgrJb;:FSNJbbbZJbbb:;arJbbbb9GEMgw:lJbbb9p9DTmbaw:OhDxekcjjjj94hDkalclf8Uebhqalcdf8UebhkabavcefciGaiVcetfaD87ebdndnaoak:YNgwJb;:FSNJbbbZJbbb:;awJbbbb9GEMgx:lJbbb9p9DTmbax:Ohkxekcjjjj94hkkabavcdfciGaiVcetfak87ebdndnaoaq:YNgoJb;:FSNJbbbZJbbb:;aoJbbbb9GEMgx:lJbbb9p9DTmbax:Ohqxekcjjjj94hqkabavcufciGaiVcetfaq87ebdndnJbbjZararN:tawawN:taoaoN:tgrJbbbbarJbbbb9GE:rJb;:FSNJbbbZMgr:lJbbb9p9DTmbar:Ohqxekcjjjj94hqkabavciGaiVcetfaq87ebalcwfhlaiclfhiaecufgembkkk9mbdnadcd4ae2geTmbinababydbgdcwtcw91:Yadce91cjjj;8ifcjjj98G::NUdbabclfhbaecufgembkkk9teiucbcbydj1jjbgeabcifc98GfgbBdj1jjbdndnabZbcztgd9nmbcuhiabad9RcFFifcz4nbcuSmekaehikaik;LeeeudndnaeabVciGTmbabhixekdndnadcz9pmbabhixekabhiinaiaeydbBdbaiclfaeclfydbBdbaicwfaecwfydbBdbaicxfaecxfydbBdbaiczfhiaeczfheadc9Wfgdcs0mbkkadcl6mbinaiaeydbBdbaeclfheaiclfhiadc98fgdci0mbkkdnadTmbinaiaeRbb86bbaicefhiaecefheadcufgdmbkkabk;aeedudndnabciGTmbabhixekaecFeGc:b:c:ew2hldndnadcz9pmbabhixekabhiinaialBdbaicxfalBdbaicwfalBdbaiclfalBdbaiczfhiadc9Wfgdcs0mbkkadcl6mbinaialBdbaiclfhiadc98fgdci0mbkkdnadTmbinaiae86bbaicefhiadcufgdmbkkabkkkebcjwklz9Kbb";
  var wasm_simd = "b9H79TebbbeKl9Gbb9Gvuuuuueu9Giuuub9Geueuikqbbebeedddilve9Weeeviebeoweuec:q;Aekr;;t9tqlbzik9:evu8Jjjjjbcz9Rhbcbheincbhdcbhiinabcwfadfaicjuaead4ceGglE86bbaialfhiadcefgdcw9hmbkaec:q:yjjbfai86bbaecitc:q1jjbfab8Piw83ibaecefgecjd9hmbkk;h8JlHud97euo978Jjjjjbcj;kb9Rgv8Kjjjjbc9:hodnadcefal0mbcuhoaiRbbc:Ge9hmbavaialfgrad9Rad;8qbbcj;abad9UhoaicefhldnadTmbaoc;WFbGgocjdaocjd6EhwcbhDinaDae9pmeawaeaD9RaDawfae6Egqcsfgoc9WGgkci2hxakcethmaocl4cifcd4hPabaDad2fhscbhzdnincehHalhOcbhAdninaraO9RaP6miavcj;cbfaAak2fhCaOaPfhlcbhidnakc;ab6mbaral9Rc;Gb6mbcbhoinaCaofhidndndndndnaOaoco4fRbbgXciGPlbedibkaipxbbbbbbbbbbbbbbbbpklbxikaialpbblalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLgQcdp:meaQpmbzeHdOiAlCvXoQrLpxiiiiiiiiiiiiiiiip9ogLpxiiiiiiiiiiiiiiiip8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibaKc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spklbalclfaYpQbfaKc:q:yjjbfRbbfhlxdkaialpbbwalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLpxssssssssssssssssp9ogLpxssssssssssssssssp8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibaKc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spklbalcwfaYpQbfaKc:q:yjjbfRbbfhlxekaialpbbbpklbalczfhlkdndndndndnaXcd4ciGPlbedibkaipxbbbbbbbbbbbbbbbbpklzxikaialpbblalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLgQcdp:meaQpmbzeHdOiAlCvXoQrLpxiiiiiiiiiiiiiiiip9ogLpxiiiiiiiiiiiiiiiip8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibaKc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spklzalclfaYpQbfaKc:q:yjjbfRbbfhlxdkaialpbbwalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLpxssssssssssssssssp9ogLpxssssssssssssssssp8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibaKc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spklzalcwfaYpQbfaKc:q:yjjbfRbbfhlxekaialpbbbpklzalczfhlkdndndndndnaXcl4ciGPlbedibkaipxbbbbbbbbbbbbbbbbpklaxikaialpbblalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLgQcdp:meaQpmbzeHdOiAlCvXoQrLpxiiiiiiiiiiiiiiiip9ogLpxiiiiiiiiiiiiiiiip8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibaKc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spklaalclfaYpQbfaKc:q:yjjbfRbbfhlxdkaialpbbwalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLpxssssssssssssssssp9ogLpxssssssssssssssssp8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibaKc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spklaalcwfaYpQbfaKc:q:yjjbfRbbfhlxekaialpbbbpklaalczfhlkdndndndndnaXco4Plbedibkaipxbbbbbbbbbbbbbbbbpkl8WxikaialpbblalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLgQcdp:meaQpmbzeHdOiAlCvXoQrLpxiiiiiiiiiiiiiiiip9ogLpxiiiiiiiiiiiiiiiip8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgXcitc:q1jjbfpbibaXc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgXcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spkl8WalclfaYpQbfaXc:q:yjjbfRbbfhlxdkaialpbbwalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLpxssssssssssssssssp9ogLpxssssssssssssssssp8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgXcitc:q1jjbfpbibaXc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgXcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spkl8WalcwfaYpQbfaXc:q:yjjbfRbbfhlxekaialpbbbpkl8Walczfhlkaoc;abfhiaocjefak0meaihoaral9Rc;Fb0mbkkdndnaiak9pmbaici4hoinaral9RcK6mdaCaifhXdndndndndnaOaico4fRbbaocoG4ciGPlbedibkaXpxbbbbbbbbbbbbbbbbpklbxikaXalpbblalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLgQcdp:meaQpmbzeHdOiAlCvXoQrLpxiiiiiiiiiiiiiiiip9ogLpxiiiiiiiiiiiiiiiip8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibaKc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spklbalclfaYpQbfaKc:q:yjjbfRbbfhlxdkaXalpbbwalpbbbgQclp:meaQpmbzeHdOiAlCvXoQrLpxssssssssssssssssp9ogLpxssssssssssssssssp8JgQp5b9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibaKc:q:yjjbfpbbbgYaYpmbbbbbbbbbbbbbbbbaQp5e9cjF;8;4;W;G;ab9:9cU1:NgKcitc:q1jjbfpbibp9UpmbedilvorzHOACXQLpPaLaQp9spklbalcwfaYpQbfaKc:q:yjjbfRbbfhlxekaXalpbbbpklbalczfhlkaocdfhoaiczfgiak6mbkkalTmbaAci6hHalhOaAcefgohAaoclSmdxekkcbhlaHceGmdkdnakTmbavcjdfazfhiavazfpbdbhYcbhXinaiavcj;;8qbbavavcjdfaqcufad2fad;8qbbaqaDfhDc9:hoalmexikkc9:hoxekcbc99aral9Radcaadca0ESEhokavcj;kbf8Kjjjjbaokwbz:bjjjbk;uzeHu8Jjjjjbc;ae9Rgv8Kjjjjbc9:hodnaeci9UgrcHfal0mbcuhoaiRbbgwc;WeGc;Ge9hmbawcsGgDce0mbavc;abfcFecje;8kbavcUf9cu83ibavc8Wf9cu83ibavcyf9cu83ibavcaf9cu83ibavcKf9cu83ibavczf9cu83ibav9cu83iwav9cu83ibaialfc9WfhqaicefgwarfhodnaeTmbcmcsaDceSEhkcbhxcbhmcbhDcbhicbhlindnaoaq9nmbc9:hoxikdndnawRbbgrc;Ve0mbavc;abfalarcl4cu7fcsGcitfgPydlhsaPydbhzdnarcsGgPak9pmbavaiarcu7fcsGcdtfydbaxaPEhraPThPdndnadcd9hmbabaDcetfgHaz87ebaHcdfas87ebaHclfar87ebxekabaDcdtfgHazBdbaHclfasBdbaHcwfarBdbkaxaPfhxavc;abfalcitfgHarBdbaHasBdlavaicdtfarBdbavc;;abfalcitfgramBdbarasBdlavaicdtfamBdbavc;abfalcefcsGglcitfgrazBdbaramBdlaicefhialcefhlxekdnarcpe0mbaxcefgOavaiaqarcsGfRbbgPcl49RcsGcdtfydbaPcz6gHEhravaiaP9RcsGcdtfydbaOaHfgsaPcsGgOEhPaOThOdndnadcd9hmbabaDcetfgzax87ebazcdfar87ebazclfaP87ebxekabaDcdtfgzaxBdbazclfarBdbazcwfaPBdbkavaicdtfaxBdbavc;abfalcitfgzarBdbazaxBdlavaicefgicsGcdtfarBdbavc;abfalcefcsGcitfgzaPBdbazarBdlavaiaHfcsGgicdtfaPBdbavc;abfalcdfcsGglcitfgraxBdbaraPBdlalcefhlaiaOfhiasaOfhxxekaxcbaoRbbgzEgAarc;:;abfalcitfgrasBdbaraABdlavaicdtfaABdbavc;abfalcefcsGcitfgraOBdbarasBdlavaicefgicsGcdtfasBdbavc;abfalcdfcsGcitfgraABdbaraOBdlavaiazcz6aXcsSVfgicsGcdtfaOBdbaiaCTaCcsSVfhialcifhlkawcefhwalcsGhlaicsGhiaDcifgDae6mbkkcbc99aoaqSEhokavc;aef8Kjjjjbaok:llevu8Jjjjjbcz9Rhvc9:hodnaecvfal0mbcuhoaiRbbc;:eGc;qe9hmbav9cb83iwaicefhraialfc98fhwdnaeTmbdnadcdSmbcbhDindnaraw6mbc9:skarcefhoar8SbbglcFeGhidndnalcu9mmbaohrxekarcvfhraicFbGhicrhldninao8SbbgdcFbGaltaiVhiadcu9kmeaocefhoalcrfglc8J9hmbxdkkaocefhrkabaDcdtfaicd4cbaice4ceG9R7avcwfaiceGcdtVgoydbfglBdbaoalBdbaDcefgDae9hmbxdkkcbhDindnaraw6mbc9:skarcefhoar8SbbglcFeGhidndnalcu9mmbaohrxekarcvfhraicFbGhicrhldninao8SbbgdcFbGaltaiVhiadcu9kmeaocefhoalcrfglc8J9hmbxdkkaocefhrkabaDcetfaicd4cbaice4ceG9R7avcwfaiceGcdtVgoydbfgl87ebaoalBdbaDcefgDae9hmbkkcbc99arawSEhokaok:EPliuo97eue978Jjjjjbca9Rhidndnadcl9hmbdnaec98GglTmbcbhvabhdinadadpbbbgocKp:RecKp:Sep;6egraocwp:RecKp:Sep;6earp;Geaoczp:RecKp:Sep;6egwp;Gep;Kep;LegDpxbbbbbbbbbbbbbbbbp:2egqarpxbbbjbbbjbbbjbbbjgkp9op9rp;Kegrpxbb;:9cbb;:9cbb;:9cbb;:9cararp;MeaDaDp;Meawaqawakp9op9rp;Kegrarp;Mep;Kep;Kep;Jep;Negwp;Mepxbbn0bbn0bbn0bbn0gqp;KepxFbbbFbbbFbbbFbbbp9oaopxbbbFbbbFbbbFbbbFp9op9qarawp;Meaqp;Kecwp:RepxbFbbbFbbbFbbbFbbp9op9qaDawp;Meaqp;Keczp:RepxbbFbbbFbbbFbbbFbp9op9qpkbbadczfhdavclfgval6mbkkalae9pmeaiaeciGgvcdtgdVcbczad9R;8kbaiabalcdtfglad;8qbbdnavTmbaiaipblbgocKp:RecKp:Sep;6egraocwp:RecKp:Sep;6earp;Geaoczp:RecKp:Sep;6egwp;Gep;Kep;LegDpxbbbbbbbbbbbbbbbbp:2egqarpxbbbjbbbjbbbjbbbjgkp9op9rp;Kegrpxbb;:9cbb;:9cbb;:9cbb;:9cararp;MeaDaDp;Meawaqawakp9op9rp;Kegrarp;Mep;Kep;Kep;Jep;Negwp;Mepxbbn0bbn0bbn0bbn0gqp;KepxFbbbFbbbFbbbFbbbp9oaopxbbbFbbbFbbbFbbbFp9op9qarawp;Meaqp;Kecwp:RepxbFbbbFbbbFbbbFbbp9op9qaDawp;Meaqp;Keczp:RepxbbFbbbFbbbFbbbFbp9op9qpklbkalaiad;8qbbskdnaec98GgxTmbcbhvabhdinadczfglalpbbbgopxbbbbbbFFbbbbbbFFgkp9oadpbbbgDaopmlvorxmPsCXQL358E8FpxFubbFubbFubbFubbp9op;6eaDaopmbediwDqkzHOAKY8AEgoczp:Sep;6egrp;Geaoczp:Reczp:Sep;6egwp;Gep;Kep;Legopxb;:FSb;:FSb;:FSb;:FSawaopxbbbbbbbbbbbbbbbbp:2egqawpxbbbjbbbjbbbjbbbjgmp9op9rp;Kegwawp;Meaoaop;Mearaqaramp9op9rp;Kegoaop;Mep;Kep;Kep;Jep;Negrp;Mepxbbn0bbn0bbn0bbn0gqp;Keczp:Reawarp;Meaqp;KepxFFbbFFbbFFbbFFbbp9op9qgwaoarp;Meaqp;KepxFFbbFFbbFFbbFFbbp9ogopmwDKYqk8AExm35Ps8E8Fp9qpkbbadaDakp9oawaopmbezHdiOAlvCXorQLp9qpkbbadcafhdavclfgvax6mbkkaxae9pmbaiaeciGgvcitgdfcbcaad9R;8kbaiabaxcitfglad;8qbbdnavTmbaiaipblzgopxbbbbbbFFbbbbbbFFgkp9oaipblbgDaopmlvorxmPsCXQL358E8FpxFubbFubbFubbFubbp9op;6eaDaopmbediwDqkzHOAKY8AEgoczp:Sep;6egrp;Geaoczp:Reczp:Sep;6egwp;Gep;Kep;Legopxb;:FSb;:FSb;:FSb;:FSawaopxbbbbbbbbbbbbbbbbp:2egqawpxbbbjbbbjbbbjbbbjgmp9op9rp;Kegwawp;Meaoaop;Mearaqaramp9op9rp;Kegoaop;Mep;Kep;Kep;Jep;Negrp;Mepxbbn0bbn0bbn0bbn0gqp;Keczp:Reawarp;Meaqp;KepxFFbbFFbbFFbbFFbbp9op9qgwaoarp;Meaqp;KepxFFbbFFbbFFbbFFbbp9ogopmwDKYqk8AExm35Ps8E8Fp9qpklzaiaDakp9oawaopmbezHdiOAlvCXorQLp9qpklbkalaiad;8qbbkk;4wllue97euv978Jjjjjbc8W9Rhidnaec98GglTmbcbhvabhoinaiaopbbbgraoczfgwpbbbgDpmlvorxmPsCXQL358E8Fgqczp:Segkclp:RepklbaopxbbjZbbjZbbjZbbjZpx;Zl81Z;Zl81Z;Zl81Z;Zl81Zakpxibbbibbbibbbibbbp9qp;6ep;NegkaraDpmbediwDqkzHOAKY8AEgrczp:Reczp:Sep;6ep;MegDaDp;Meakarczp:Sep;6ep;Megxaxp;Meakaqczp:Reczp:Sep;6ep;Megqaqp;Mep;Kep;Kep;Lepxbbbbbbbbbbbbbbbbp:4ep;Jepxb;:FSb;:FSb;:FSb;:FSgkp;Mepxbbn0bbn0bbn0bbn0grp;KepxFFbbFFbbFFbbFFbbgmp9oaxakp;Mearp;Keczp:Rep9qgxaqakp;Mearp;Keczp:ReaDakp;Mearp;Keamp9op9qgkpmbezHdiOAlvCXorQLgrp5baipblbpEb:T:j83ibaocwfarp5eaipblbpEe:T:j83ibawaxakpmwDKYqk8AExm35Ps8E8Fgkp5baipblbpEd:T:j83ibaocKfakp5eaipblbpEi:T:j83ibaocafhoavclfgval6mbkkdnalae9pmbaiaeciGgvcitgofcbcaao9R;8kbaiabalcitfgwao;8qbbdnavTmbaiaipblbgraipblzgDpmlvorxmPsCXQL358E8Fgqczp:Segkclp:RepklaaipxbbjZbbjZbbjZbbjZpx;Zl81Z;Zl81Z;Zl81Z;Zl81Zakpxibbbibbbibbbibbbp9qp;6ep;NegkaraDpmbediwDqkzHOAKY8AEgrczp:Reczp:Sep;6ep;MegDaDp;Meakarczp:Sep;6ep;Megxaxp;Meakaqczp:Reczp:Sep;6ep;Megqaqp;Mep;Kep;Kep;Lepxbbbbbbbbbbbbbbbbp:4ep;Jepxb;:FSb;:FSb;:FSb;:FSgkp;Mepxbbn0bbn0bbn0bbn0grp;KepxFFbbFFbbFFbbFFbbgmp9oaxakp;Mearp;Keczp:Rep9qgxaqakp;Mearp;Keczp:ReaDakp;Mearp;Keamp9op9qgkpmbezHdiOAlvCXorQLgrp5baipblapEb:T:j83ibaiarp5eaipblapEe:T:j83iwaiaxakpmwDKYqk8AExm35Ps8E8Fgkp5baipblapEd:T:j83izaiakp5eaipblapEi:T:j83iKkawaiao;8qbbkk:Pddiue978Jjjjjbc;ab9Rhidnadcd4ae2glc98GgvTmbcbhdabheinaeaepbbbgocwp:Recwp:Sep;6eaocep:SepxbbjZbbjZbbjZbbjZp:UepxbbjFbbjFbbjFbbjFp9op;Mepkbbaeczfheadclfgdav6mbkkdnaval9pmbaialciGgdcdtgeVcbc;abae9R;8kbaiabavcdtfgvae;8qbbdnadTmbaiaipblbgocwp:Recwp:Sep;6eaocep:SepxbbjZbbjZbbjZbbjZp:UepxbbjFbbjFbbjFbbjFp9op;Mepklbkavaiae;8qbbkk9teiucbcbydj1jjbgeabcifc98GfgbBdj1jjbdndnabZbcztgd9nmbcuhiabad9RcFFifcz4nbcuSmekaehikaikkkebcjwklz9Tbb";
  var detector = new Uint8Array([0, 97, 115, 109, 1, 0, 0, 0, 1, 4, 1, 96, 0, 0, 3, 3, 2, 0, 0, 5, 3, 1, 0, 1, 12, 1, 0, 10, 22, 2, 12, 0, 65, 0, 65, 0, 65, 0, 252, 10, 0, 0, 11, 7, 0, 65, 0, 253, 15, 26, 11]);
  var wasmpack = new Uint8Array([32, 0, 65, 2, 1, 106, 34, 33, 3, 128, 11, 4, 13, 64, 6, 253, 10, 7, 15, 116, 127, 5, 8, 12, 40, 16, 19, 54, 20, 9, 27, 255, 113, 17, 42, 67, 24, 23, 146, 148, 18, 14, 22, 45, 70, 69, 56, 114, 101, 21, 25, 63, 75, 136, 108, 28, 118, 29, 73, 115]);
  if (typeof WebAssembly !== "object") {
    return {
      supported: false
    };
  }
  var wasm = WebAssembly.validate(detector) ? wasm_simd : wasm_base;
  var instance;
  var ready = WebAssembly.instantiate(unpack(wasm), {}).then(function(result) {
    instance = result.instance;
    instance.exports.__wasm_call_ctors();
  });
  function unpack(data) {
    var result = new Uint8Array(data.length);
    for (var i = 0; i < data.length; ++i) {
      var ch = data.charCodeAt(i);
      result[i] = ch > 96 ? ch - 97 : ch > 64 ? ch - 39 : ch + 4;
    }
    var write = 0;
    for (var i = 0; i < data.length; ++i) {
      result[write++] = result[i] < 60 ? wasmpack[result[i]] : (result[i] - 60) * 64 + result[++i];
    }
    return result.buffer.slice(0, write);
  }
  function decode(fun, target, count, size, source, filter) {
    var sbrk = instance.exports.sbrk;
    var count4 = count + 3 & ~3;
    var tp = sbrk(count4 * size);
    var sp = sbrk(source.length);
    var heap = new Uint8Array(instance.exports.memory.buffer);
    heap.set(source, sp);
    var res = fun(tp, count, size, sp, source.length);
    if (res == 0 && filter) {
      filter(tp, count4, size);
    }
    target.set(heap.subarray(tp, tp + count * size));
    sbrk(tp - sbrk(0));
    if (res != 0) {
      throw new Error("Malformed buffer data: " + res);
    }
  }
  var filters = {
    NONE: "",
    OCTAHEDRAL: "meshopt_decodeFilterOct",
    QUATERNION: "meshopt_decodeFilterQuat",
    EXPONENTIAL: "meshopt_decodeFilterExp"
  };
  var decoders = {
    ATTRIBUTES: "meshopt_decodeVertexBuffer",
    TRIANGLES: "meshopt_decodeIndexBuffer",
    INDICES: "meshopt_decodeIndexSequence"
  };
  var workers = [];
  var requestId = 0;
  function createWorker(url) {
    var worker = {
      object: new Worker(url),
      pending: 0,
      requests: {}
    };
    worker.object.onmessage = function(event) {
      var data = event.data;
      worker.pending -= data.count;
      worker.requests[data.id][data.action](data.value);
      delete worker.requests[data.id];
    };
    return worker;
  }
  function initWorkers(count) {
    var source = "var instance; var ready = WebAssembly.instantiate(new Uint8Array([" + new Uint8Array(unpack(wasm)) + "]), {}).then(function(result) { instance = result.instance; instance.exports.__wasm_call_ctors(); });self.onmessage = workerProcess;" + decode.toString() + workerProcess.toString();
    var blob = new Blob([source], { type: "text/javascript" });
    var url = URL.createObjectURL(blob);
    for (var i = 0; i < count; ++i) {
      workers[i] = createWorker(url);
    }
    URL.revokeObjectURL(url);
  }
  function decodeWorker(count, size, source, mode, filter) {
    var worker = workers[0];
    for (var i = 1; i < workers.length; ++i) {
      if (workers[i].pending < worker.pending) {
        worker = workers[i];
      }
    }
    return new Promise(function(resolve, reject) {
      var data = new Uint8Array(source);
      var id = requestId++;
      worker.pending += count;
      worker.requests[id] = { resolve, reject };
      worker.object.postMessage({ id, count, size, source: data, mode, filter }, [data.buffer]);
    });
  }
  function workerProcess(event) {
    ready.then(function() {
      var data = event.data;
      try {
        var target = new Uint8Array(data.count * data.size);
        decode(instance.exports[data.mode], target, data.count, data.size, data.source, instance.exports[data.filter]);
        self.postMessage({ id: data.id, count: data.count, action: "resolve", value: target }, [target.buffer]);
      } catch (error) {
        self.postMessage({ id: data.id, count: data.count, action: "reject", value: error });
      }
    });
  }
  return {
    ready,
    supported: true,
    useWorkers: function(count) {
      initWorkers(count);
    },
    decodeVertexBuffer: function(target, count, size, source, filter) {
      decode(instance.exports.meshopt_decodeVertexBuffer, target, count, size, source, instance.exports[filters[filter]]);
    },
    decodeIndexBuffer: function(target, count, size, source) {
      decode(instance.exports.meshopt_decodeIndexBuffer, target, count, size, source);
    },
    decodeIndexSequence: function(target, count, size, source) {
      decode(instance.exports.meshopt_decodeIndexSequence, target, count, size, source);
    },
    decodeGltfBuffer: function(target, count, size, source, mode, filter) {
      decode(instance.exports[decoders[mode]], target, count, size, source, instance.exports[filters[filter]]);
    },
    decodeGltfBufferAsync: function(count, size, source, mode, filter) {
      if (workers.length > 0) {
        return decodeWorker(count, size, source, decoders[mode], filters[filter]);
      }
      return ready.then(function() {
        var target = new Uint8Array(count * size);
        decode(instance.exports[decoders[mode]], target, count, size, source, instance.exports[filters[filter]]);
        return target;
      });
    }
  };
}();

// node_modules/@giro3d/giro3d/entities/3dtiles/FetchPlugin.js
var FetchPlugin = class {
  constructor() {
    __publicField(this, "_opCounter", new OperationCounter_default());
  }
  get loading() {
    return this._opCounter.loading;
  }
  get progress() {
    return this._opCounter.progress;
  }
  fetchData(url, options) {
    this._opCounter.increment();
    return Fetcher_default.fetch(url, options).finally(() => this._opCounter.decrement());
  }
};

// node_modules/@giro3d/giro3d/entities/3dtiles/PointCloudParameters.js
var DEFAULT_TILES3D_POINTCLOUD_ATTRIBUTE_MAPPING = {
  classification: "classification",
  scalar: "intensity"
};

// node_modules/@giro3d/giro3d/entities/3dtiles/PointCloudPlugin.js
function isPNTSScene(obj) {
  return obj.isPoints && "batchTable" in obj;
}
var PointCloudPlugin = class {
  constructor(parameters) {
    this._parameters = parameters;
  }
  processBufferAttribute(geometry, batchTable, sourceAttribute, targetAttribute) {
    const count = batchTable.count;
    const array = batchTable.getPropertyArray(sourceAttribute);
    if (array == null) {
      return;
    }
    let bufferAttribute;
    const itemSize = array.length / count;
    if (array instanceof Uint8Array || array instanceof Uint8ClampedArray) {
      bufferAttribute = new Uint8BufferAttribute(array, itemSize, false);
      bufferAttribute.gpuType = IntType;
    } else if (array instanceof Uint16Array) {
      bufferAttribute = new Uint16BufferAttribute(array, itemSize, false);
      bufferAttribute.gpuType = IntType;
    } else if (array instanceof Uint32Array) {
      bufferAttribute = new Uint32BufferAttribute(array, itemSize, false);
      bufferAttribute.gpuType = IntType;
    } else if (array instanceof Int8Array) {
      bufferAttribute = new Int8BufferAttribute(array, itemSize, false);
      bufferAttribute.gpuType = IntType;
    } else if (array instanceof Int16Array) {
      bufferAttribute = new Int16BufferAttribute(array, itemSize, false);
      bufferAttribute.gpuType = IntType;
    } else if (array instanceof Int32Array) {
      bufferAttribute = new Int32BufferAttribute(array, itemSize, false);
      bufferAttribute.gpuType = IntType;
    } else if (array instanceof Float32Array) {
      bufferAttribute = new Float32BufferAttribute(array, itemSize, false);
      bufferAttribute.gpuType = FloatType;
    } else if (array instanceof Float64Array) {
      bufferAttribute = new Float32BufferAttribute(new Float32Array(array), itemSize, false);
      bufferAttribute.gpuType = FloatType;
    } else {
      throw new Error("invalid array type");
    }
    geometry.setAttribute(targetAttribute, bufferAttribute);
  }
  updateMaterial(material) {
    material.size = this._parameters.pointSize;
    material.elevationColorMap = this._parameters.pointCloudColorMap;
    material.attributesState = {
      scalars: [{
        colorMap: this._parameters.pointCloudColorMap
      }],
      classifications: [{
        classifications: this._parameters.classifications
      }]
    };
    material.mode = this._parameters.pointCloudMode;
    material.brightness = this._parameters.colorimetry.brightness;
    material.contrast = this._parameters.colorimetry.contrast;
    material.saturation = this._parameters.colorimetry.saturation;
    if (this._parameters.overlayColor != null) {
      material.overlayColor = new Vector4(this._parameters.overlayColor.r, this._parameters.overlayColor.g, this._parameters.overlayColor.b, 1);
    } else {
      material.overlayColor = new Vector4(0, 0, 0, 0);
    }
    material.updateUniforms();
  }
  processTileModel(scene, tile) {
    if (isPNTSScene(scene)) {
      const batchTable = scene.batchTable;
      const mapping = this._parameters.attributeMapping;
      this.processBufferAttribute(scene.geometry, batchTable, mapping["scalar"], "scalar");
      this.processBufferAttribute(scene.geometry, batchTable, mapping["classification"], "classification");
      const material = new PointCloudMaterial_default({
        mode: this._parameters.pointCloudMode
      });
      scene.material = material;
      material.setupFromGeometry(scene.geometry);
      this.updateMaterial(material);
      enablePointCloudPostProcessing(scene);
      if (scene.geometry.boundingBox == null) {
        scene.geometry.computeBoundingBox();
      }
      const layerNode = scene;
      layerNode.lod = tile.__depth;
      const pointCount = scene.geometry.getAttribute("position").count;
      const pixels = MathUtils.clamp(Math.round(Math.sqrt(pointCount)), 32, 512);
      layerNode.textureSize = new Vector2(pixels, pixels);
      layerNode.canProcessColorLayer = () => true;
      layerNode.disposed = false;
      layerNode.userData.boundingBox = scene.geometry.boundingBox;
      layerNode.getExtent = () => {
        return layerNode.userData.extent;
      };
    }
  }
};

// node_modules/@giro3d/giro3d/entities/Tiles3D.js
var tmpBox3 = new Box3();
var tmpVector = new Vector3();
function isLayerNode(obj) {
  if (obj == null) {
    return false;
  }
  if ("material" in obj && PointCloudMaterial_default.isPointCloudMaterial(obj.material)) {
    return true;
  }
  return false;
}
var perInstanceSharedResources = /* @__PURE__ */ new Map();
function getSharedResources(instance) {
  return perInstanceSharedResources.get(instance) ?? null;
}
function setSharedResources(instance, resources) {
  if (perInstanceSharedResources.has(instance)) {
    return;
  }
  perInstanceSharedResources.set(instance, resources);
  instance.addEventListener("dispose", (e) => perInstanceSharedResources.delete(e.target));
}
var Tiles3D = class extends Entity3D_default {
  constructor(options) {
    super(options);
    __publicField(this, "isPickable", true);
    __publicField(this, "hasLayers", true);
    __publicField(this, "isTiles3D", true);
    __publicField(this, "type", "Tiles3D");
    __publicField(this, "_debugOptions", {
      displayBoxBounds: false,
      displaySphereBounds: false,
      displayRegionBounds: false
    });
    // Settings that only applies to point cloud tiles
    __publicField(this, "_pointCloudParameters", {
      pointSize: 0,
      // Automatic size
      pointCloudMode: MODE.COLOR,
      colorimetry: defaultColorimetryOptions(),
      overlayColor: null,
      attributeMapping: DEFAULT_TILES3D_POINTCLOUD_ATTRIBUTE_MAPPING,
      pointCloudColorMap: new ColorMap_default({
        colors: [new Color("black"), new Color("white")],
        min: 0,
        max: 100
      }),
      classifications: ASPRS_CLASSIFICATIONS.map((c2) => c2.clone())
    });
    __publicField(this, "_objectOptions", {
      castShadow: false,
      receiveShadow: false
    });
    __publicField(this, "_colorLayer", null);
    this._tiles = new Vi(options == null ? void 0 : options.url);
    this._tiles.errorTarget = (options == null ? void 0 : options.errorTarget) ?? 8;
    this.object3d.add(this._tiles.group);
    this._listeners = {
      onModelLoaded: this.onModelLoaded.bind(this),
      onTileVisibilityChanged: this.onTileVisibilityChanged.bind(this),
      onColorMapUpdated: this.onColorMapUpdated.bind(this),
      onTileDisposed: this.onTileDisposed.bind(this)
    };
    this._tiles.addEventListener("load-model", this._listeners.onModelLoaded);
    this._tiles.addEventListener("tile-visibility-change", this._listeners.onTileVisibilityChanged);
    this._tiles.addEventListener("dispose-model", this._listeners.onTileDisposed);
    this._debugPlugin = new Io();
    this._tiles.registerPlugin(this._debugPlugin);
    this.updateDebugPluginState();
    this._tiles.registerPlugin(new w4());
    this._tiles.registerPlugin(new co({
      delay: 5e3,
      bytesTarget: Infinity
    }));
    this._fetchPlugin = new FetchPlugin();
    this._pointCloudPlugin = new PointCloudPlugin(this._pointCloudParameters);
    this._tiles.registerPlugin(this._pointCloudPlugin);
    this._tiles.registerPlugin(this._fetchPlugin);
    const dracoLoader = new DRACOLoader(this._tiles.manager).setDecoderPath((options == null ? void 0 : options.dracoDecoderPath) ?? `https://unpkg.com/three@0.${REVISION}.0/examples/jsm/libs/draco/gltf/`);
    const ktxLoader = new KTX2Loader(this._tiles.manager).setTranscoderPath((options == null ? void 0 : options.ktx2DecoderPath) ?? `https://unpkg.com/three@0.${REVISION}.0/examples/jsm/libs/basis/`);
    this._ktx2Loader = ktxLoader;
    this._tiles.registerPlugin(new ao({
      dracoLoader,
      ktxLoader,
      meshoptDecoder: MeshoptDecoder
    }));
    this._pointCloudParameters.pointCloudMode = (options == null ? void 0 : options.pointCloudMode) ?? this._pointCloudParameters.pointCloudMode;
    this._pointCloudParameters.pointSize = (options == null ? void 0 : options.pointSize) ?? this._pointCloudParameters.pointSize;
    this._pointCloudParameters.pointCloudColorMap = (options == null ? void 0 : options.colorMap) ?? this._pointCloudParameters.pointCloudColorMap;
    this._pointCloudParameters.classifications = (options == null ? void 0 : options.classifications) ?? this._pointCloudParameters.classifications;
    this._pointCloudParameters.attributeMapping = (options == null ? void 0 : options.pointCloudAttributeMapping) ?? this._pointCloudParameters.attributeMapping;
    this._pointCloudParameters.pointCloudColorMap.addEventListener("updated", this._listeners.onColorMapUpdated);
  }
  /**
   * Returns the underlying renderer.
   */
  get tiles() {
    return this._tiles;
  }
  onRenderingContextRestored() {
    this.forEachLayer((layer) => layer.onRenderingContextRestored());
    this.instance.notifyChange(this);
  }
  getBoundingBox() {
    const box = new Box3();
    this._tiles.getBoundingBox(box);
    return box;
  }
  getMemoryUsage(context) {
    this.traverse((obj) => {
      if ("geometry" in obj && isBufferGeometry(obj.geometry)) {
        getGeometryMemoryUsage(context, obj.geometry);
      }
    });
    if (this.layerCount > 0) {
      this.forEachLayer((layer) => {
        layer.getMemoryUsage(context);
      });
    }
  }
  get loading() {
    var _a;
    return this.tiles.loadProgress !== 1 || (((_a = this._colorLayer) == null ? void 0 : _a.loading) ?? false);
  }
  get progress() {
    let sum = this.tiles.loadProgress;
    let count = 1;
    if (this._colorLayer) {
      sum += this._colorLayer.progress;
      count = 2;
    }
    return sum / count;
  }
  updateObjectOption(key, value) {
    if (this._objectOptions[key] !== value) {
      this._objectOptions[key] = value;
      this.traverse((o) => this.updateObject(o));
      this.notifyChange(this);
    }
  }
  /**
   * Toggles the `.castShadow` property on objects generated by this entity.
   *
   * Note: shadow maps require normal attributes on objects.
   */
  get castShadow() {
    return this._objectOptions.castShadow;
  }
  set castShadow(v2) {
    this.updateObjectOption("castShadow", v2);
  }
  /**
   * Toggles the `.receiveShadow` property on objects generated by this entity.
   *
   * Note: shadow maps require normal attributes on objects.
   */
  get receiveShadow() {
    return this._objectOptions.receiveShadow;
  }
  set receiveShadow(v2) {
    this.updateObjectOption("receiveShadow", v2);
  }
  getLayers(predicate) {
    if (this._colorLayer) {
      if (typeof predicate != "function" || predicate(this._colorLayer)) {
        return [this._colorLayer];
      }
    }
    return [];
  }
  forEachLayer(callback) {
    if (this._colorLayer) {
      callback(this._colorLayer);
    }
  }
  removeColorLayer() {
    if (this._colorLayer) {
      this.dispatchEvent({
        type: "layer-removed",
        layer: this._colorLayer
      });
      this.traverse((obj) => {
        var _a;
        if (isLayerNode(obj)) {
          (_a = this._colorLayer) == null ? void 0 : _a.unregisterNode(obj);
        }
      });
      this._colorLayer = null;
    }
  }
  /**
   * Sets the color layer used to colorize tiles.
   * Note: this feature only works with point cloud tiles.
   */
  async setColorLayer(layer) {
    if (this._colorLayer) {
      this.removeColorLayer();
    }
    this._colorLayer = layer;
    await layer.initialize({
      instance: this.instance,
      composerProjection: this.instance.coordinateSystem
    });
    this.dispatchEvent({
      type: "layer-removed",
      layer
    });
  }
  get layerCount() {
    if (this._colorLayer) {
      return 1;
    }
    return 0;
  }
  updateOpacity() {
    this.traverseMaterials((material) => {
      this.setMaterialOpacity(material);
    });
  }
  preprocess(opts) {
    return new Promise((resolve, reject) => {
      const instance = opts.instance;
      const shared = getSharedResources(instance);
      if (shared) {
        this._tiles.lruCache = shared.lruCache;
        this._tiles.downloadQueue = shared.downloadQueue;
        this._tiles.parseQueue = shared.parseQueue;
      } else {
        const toShare = {
          lruCache: this._tiles.lruCache,
          downloadQueue: this._tiles.downloadQueue,
          parseQueue: this._tiles.parseQueue
        };
        setSharedResources(instance, toShare);
      }
      const handlers = {
        success: () => {
          this._tiles.removeEventListener("load-content", handlers.success);
          this._tiles.removeEventListener("load-error", handlers.error);
          this._tiles.update();
          this.notifyChange(this);
          resolve();
        },
        error: (error) => {
          this._tiles.removeEventListener("load-content", handlers.success);
          this._tiles.removeEventListener("load-error", handlers.error);
          reject(error);
        }
      };
      this._tiles.addEventListener("load-content", handlers.success);
      this._tiles.addEventListener("load-error", handlers.error);
      const camera = instance.view.camera;
      if (this._tiles.hasCamera(camera) === false) {
        this._tiles.setCamera(camera);
        this._tiles.setResolutionFromRenderer(camera, instance.renderer);
      }
      this._ktx2Loader.detectSupport(instance.renderer);
      this._tiles.update();
      this.notifyChange(this);
    });
  }
  preUpdate(context) {
    if (this.frozen || !this.visible) {
      return null;
    }
    const camera = context.view.camera;
    this._tiles.setResolutionFromRenderer(camera, this.instance.renderer);
    this._tiles.update();
    return null;
  }
  postUpdate(context) {
    if (this.frozen || !this.visible) {
      return;
    }
    this.traverse((obj) => {
      if (obj.visible) {
        this.updateCameraDistances(context, obj);
        if ("material" in obj && PointCloudMaterial_default.isPointCloudMaterial(obj.material)) {
          this._pointCloudPlugin.updateMaterial(obj.material);
          if (isLayerNode(obj)) {
            this.prepareLayerNode(obj);
            this.forEachLayer((layer) => layer.update(context, obj));
          }
        }
      }
    });
    this.forEachLayer((layer) => layer.postUpdate());
  }
  /**
   * Calculate and set the material opacity, taking into account this entity opacity and the
   * original opacity of the object.
   *
   * @param material - a material belonging to an object of this entity
   */
  setMaterialOpacity(material) {
    material.opacity = this.opacity * material.userData.originalOpacity;
    const currentTransparent = material.transparent;
    material.transparent = material.opacity < 1;
    material.needsUpdate = currentTransparent !== material.transparent;
  }
  onColorMapUpdated() {
    this.traversePointCloudMaterials((m2) => m2.updateUniforms());
  }
  get errorTarget() {
    return this._tiles.errorTarget;
  }
  set errorTarget(v2) {
    if (this._tiles.errorTarget !== v2) {
      this._tiles.errorTarget = v2;
      this.notifyChange(this);
    }
  }
  /**
   * Gets or sets the size of points. Only applies to point cloud tiles.
   */
  get pointSize() {
    return this._pointCloudParameters.pointSize;
  }
  set pointSize(v2) {
    if (this._pointCloudParameters.pointSize !== v2) {
      this._pointCloudParameters.pointSize = v2;
      this.traversePointCloudMaterials((m2) => {
        m2.size = v2;
      });
      this.notifyChange(this);
    }
  }
  /**
   * Gets or sets display mode of point clouds. Only applies to point cloud tiles.
   */
  get pointCloudMode() {
    return this._pointCloudParameters.pointCloudMode;
  }
  set pointCloudMode(v2) {
    if (this._pointCloudParameters.pointCloudMode !== v2) {
      this._pointCloudParameters.pointCloudMode = v2;
      this.traversePointCloudMaterials((m2) => m2.mode = v2);
      this.notifyChange(this);
    }
  }
  /**
   * Gets or sets the default color of point clouds. Only applies to point cloud tiles.
   */
  get pointCloudColor() {
    return this._pointCloudParameters.overlayColor;
  }
  set pointCloudColor(v2) {
    const color = v2 != null ? new Color(v2) : new Color();
    if (v2 == null || this._pointCloudParameters.overlayColor == null || !this._pointCloudParameters.overlayColor.equals(color)) {
      this._pointCloudParameters.overlayColor = color;
      this.notifyChange(this);
    }
  }
  /**
   * Gets or sets the point cloud brightness, contrast and saturation. Only applies to point cloud tiles.
   */
  get pointCloudColorimetryOptions() {
    return this._pointCloudParameters.colorimetry;
  }
  set pointCloudColorimetryOptions(v2) {
    if (this._pointCloudParameters.colorimetry !== v2) {
      this._pointCloudParameters.colorimetry = v2;
      this.traversePointCloudMaterials((m2) => {
        m2.brightness = v2.brightness;
        m2.contrast = v2.contrast;
        m2.saturation = v2.saturation;
      });
      this.notifyChange(this);
    }
  }
  /**
   * Gets the classifications for point clouds. Only applies to point cloud tiles.
   */
  get pointCloudClassifications() {
    return this._pointCloudParameters.classifications;
  }
  /**
   * Gets the colormap used for point clouds. Only applies to point cloud tiles.
   */
  get colorMap() {
    return this._pointCloudParameters.pointCloudColorMap;
  }
  traversePointCloudMaterials(callback) {
    this.traverseMaterials((m2) => {
      if (PointCloudMaterial_default.isPointCloudMaterial(m2)) {
        callback(m2);
      }
    });
  }
  setDebugParam(key, value) {
    let plugin = this._tiles.getPluginByName("DEBUG_TILES_PLUGIN");
    if (plugin == null) {
      plugin = new Io();
      this._tiles.registerPlugin(plugin);
    }
    if (plugin != null && plugin[key] !== value) {
      plugin[key] = value;
      this.notifyChange(this);
    }
    this.updateDebugPluginState();
  }
  updateDebugPluginState() {
    this._debugPlugin.enabled = this._debugOptions.displayBoxBounds || this._debugOptions.displayRegionBounds || this._debugOptions.displaySphereBounds;
  }
  /**
   * Toggles the display of box volumes.
   */
  get displayBoxBounds() {
    return this._debugOptions.displayBoxBounds;
  }
  set displayBoxBounds(v2) {
    if (this._debugOptions.displayBoxBounds !== v2) {
      this._debugOptions.displayBoxBounds = v2;
      this.setDebugParam("displayBoxBounds", v2);
    }
  }
  /**
   * Toggles the display of sphere volumes.
   */
  get displaySphereBounds() {
    return this._debugOptions.displaySphereBounds;
  }
  set displaySphereBounds(v2) {
    if (this._debugOptions.displaySphereBounds !== v2) {
      this._debugOptions.displaySphereBounds = v2;
      this.setDebugParam("displaySphereBounds", v2);
    }
  }
  /**
   * Toggles the display of region volumes.
   */
  get displayRegionBounds() {
    return this._debugOptions.displayRegionBounds;
  }
  set displayRegionBounds(v2) {
    if (this._debugOptions.displayRegionBounds !== v2) {
      this._debugOptions.displayRegionBounds = v2;
      this.setDebugParam("displayRegionBounds", v2);
    }
  }
  /**
   * Prepares the object so that it can receive a color layer.
   */
  prepareLayerNode(node) {
    if (node.visible && node.userData.extent == null) {
      const localBox = node.userData.boundingBox;
      const worldBox = localBox.clone().applyMatrix4(node.matrixWorld);
      const extent = Extent_default.fromBox3(this.instance.coordinateSystem, worldBox);
      node.userData.extent = extent;
    }
  }
  onTileDisposed(e) {
    const {
      scene
    } = e;
    if (this.layerCount !== 0 && isLayerNode(scene)) {
      this.forEachLayer((layer) => layer.unregisterNode(scene));
    }
    this.notifyChange(this);
  }
  onTileVisibilityChanged(e) {
    const {
      scene,
      visible
    } = e;
    if (this.layerCount !== 0 && isLayerNode(scene)) {
      if (visible && scene.userData.extent == null) {
        this.prepareLayerNode(scene);
      }
      if (!visible) {
        this.forEachLayer((layer) => layer.unregisterNode(scene));
      }
      scene.dispatchEvent({
        type: "visibility-changed"
      });
    }
    if (visible) {
      this.updateMaterial(scene);
    }
    this.notifyChange(this);
  }
  updateMaterial(scene) {
    this.traverseMaterials((m2) => this.setupMaterial(m2), scene);
    if (isPNTSScene(scene)) {
      this._pointCloudPlugin.updateMaterial(scene.material);
    }
  }
  updateObject(obj) {
    const opts = this._objectOptions;
    obj.castShadow = opts.castShadow;
    obj.receiveShadow = opts.receiveShadow;
  }
  onModelLoaded(e) {
    if (typeof e === "object" && e != null && "scene" in e && isObject3D(e.scene)) {
      this.onObjectCreated(e.scene);
      e.scene.traverse((o) => this.updateObject(o));
      this.updateMaterial(e.scene);
      this.notifyChange(this);
    }
  }
  setupMaterial(material) {
    material.clippingPlanes = this.clippingPlanes;
    if (material.userData.originalOpacity == null) {
      material.userData.originalOpacity = material.opacity;
    }
    this.setMaterialOpacity(material);
  }
  updateCameraDistances(context, obj) {
    const plane = context.distance.plane;
    if (obj.visible && "geometry" in obj && isBufferGeometry(obj.geometry)) {
      const geometry = obj.geometry;
      if (geometry.boundingBox == null) {
        geometry.computeBoundingBox();
      }
      const bbox = tmpBox3.copy(nonNull(geometry.boundingBox)).applyMatrix4(obj.matrixWorld);
      const distance = plane.distanceToPoint(bbox.getCenter(tmpVector));
      const radius = bbox.getSize(tmpVector).length() * 0.5;
      this._distance.min = Math.min(this._distance.min, distance - radius);
      this._distance.max = Math.max(this._distance.max, distance + radius);
    }
  }
  dispose() {
    this._tiles.removeEventListener("load-model", this._listeners.onModelLoaded);
    this._tiles.removeEventListener("tile-visibility-change", this._listeners.onTileVisibilityChanged);
    this._tiles.removeEventListener("dispose-model", this._listeners.onTileDisposed);
    this._pointCloudParameters.pointCloudColorMap.removeEventListener("updated", this._listeners.onColorMapUpdated);
    this._tiles.dispose();
  }
};
export {
  DEFAULT_TILES3D_POINTCLOUD_ATTRIBUTE_MAPPING,
  Tiles3D as default,
  isLayerNode
};
//# sourceMappingURL=@giro3d_giro3d_entities_Tiles3D__js.js.map
